# Claude Code Harness — Plans.md

最終アーカイブ: 2026-05-29（Phase 80/81/82/84 → `.claude/memory/archive/Plans-2026-05-29-phase80-84.md`）
前回アーカイブ: 2026-05-29（Phase 63/64/66-71/73-76/78/79 → `.claude/memory/archive/Plans-2026-05-29-phase63-79.md`）

---

## North Star（3 層の野望）

この task ledger 全体が目指す到達点。古い順（土台 → てっぺん）。詳細契約は `spec.md` を正本とし、ここは参照ブロック。

- **L1 判断専念**: AI が plan / 実装 / 比較 / 検証 evidence を準備し、operator（人間）は最終判断のみ行う（`spec.md` Purpose / Users And Workflows）。
- **L2 ツール非依存（tool-agnostic）**: 同一 Harness（R01-R13 guardrails + plan/work/review/release）が Claude / Codex / Cursor の「どれからでも」効く。1 つの policy engine が 3 host を native hook 経由で adjudicate する（複製でなく routing）。2 つの向きを対等にサポート — #1 harness が駆動（Lead が他ツールを engine として spawn）/ #2 host から使う（Codex/Cursor「から」harness を使う）（`spec.md` Execution Backend Contract / Host Adapter）。
- **L3 協調（collaboration, 将来の本丸）**: 複数ツールが同一プロジェクトを、人間をコピペ係にせず協調する。Mode 1 = 完全自律オーケストレーション（v1 は Lead=Claude 固定、Codex/Cursor は外向き spawn API 無し）。Mode 2 = 人間在席の peer co-drive（live notice messaging）。フル peer-Lead 協調は段階導入で後回し（Phase 92 Purpose / `spec.md` Mode 1/Mode 2）。

> 既知 follow-up（未スケジュール・メモのみ、新規 cc:todo は作らない）: **delivery hook gen 未配線** — `go/internal/hostgen` の `GenerateDeliveryHooksJSON` は実装 + unit test 済みだが harness gen に未接続で、生成される Codex/Cursor の hooks.json に inbox-check（Mode 2 turn 境界 delivery）が入らない。配線すれば Mode 2 の郵便配達が Codex/Cursor にも届く（turn-based、live ではない。live monitor は Claude 専用）= L3 協調の実体化の一歩。Lead が後で正式 task 化を判断する。

---

## 📦 アーカイブ

完了済み Phase は以下のファイルへ切り出し済み（git history にも残存）:

- [Phase 80/81/82/84](.claude/memory/archive/Plans-2026-05-29-phase80-84.md) — Claude 2.1.143-2.1.152 + Codex 0.131-0.134 upstream refresh / Cursor CCH Adapter candidate / cursor-agent CLI workflow smoke 検証 (candidate, 配布なし) / harness-review closeout fixes + Cursor ACP boundary record
- [Phase 63/64/66-71/73-76/78/79](.claude/memory/archive/Plans-2026-05-29-phase63-79.md) — stale harness-mem 参照整理 / Plans archive-aware / 3-surface HTML cross-project safety 関連 / Open Issue closeout / Codex 0.130 / harness-review TeamAgent + lightweight / Hokage Core boundary / R03 break-glass / Superpowers tool-first onboarding / repo-health gates / README front door / spec.md+Plans.md co-required / Dependabot benchmark / harness-plan team gates
- [Phase 47-61](.claude/memory/archive/Plans-2026-05-08-phase47-61.md) — Session Monitor 能動監視 / XR-003 / 3-state 依存テスト規約 / CC 2.1.112-2.1.126 + Codex 0.121-0.128 upstream 追従 / Issue #105 English default + Japanese opt-in / External Issue closeout / Skill orchestration design contract / harness-mem managed companion (v4.6.0-v4.7.0) / Sandbagging-Aware Weak-Supervision Harness
- [Phase 44 + 45 + 46](.claude/memory/archive/Plans-2026-04-19-phase44-46.md) — Opus 4.7 / CC 2.1.99-110 追従 "Arcana" (v4.2.0) + Plugin Manifest 公式準拠 + Worker 3 層防御 (#84-#87, v4.3.0)
- [Phase 37 + 41 + 42 + 43](.claude/memory/archive/Plans-2026-04-17-phase37-41-42-43.md) — Hokage 完全体 / Long-Running Harness / Go hot-path migration / Advisor Strategy
- [Phase 39 + 40 + 41.0](.claude/memory/archive/Plans-2026-04-15-phase39-40-41.0.md) — レビュー体験改善 / Migration Residue Scanner / Long-Running Harness Spike

---

## マーカー凡例

PM ↔ Impl 運用で使用する標準マーカー:

| マーカー | 意味 | 誰が付ける |
|---------|------|-----------|
| `pm:requested` / `pm:依頼中` | PM がタスクを起票し、Impl へ依頼中 | PM |
| `cc:todo` / `cc:TODO` | Impl の未着手タスク | Impl |
| `cc:wip` / `cc:WIP` | Impl（Claude Code）が着手中 | Impl |
| `cc:done` / `cc:完了` | Impl が作業完了し、PM の確認待ち | Impl |
| `pm:approved` / `pm:確認済` | PM が最終確認を完了 | PM |
| `cc:withdrawn` | Impl が判断で取り下げたタスク（superseded / 別タスクで吸収）。breezing は cc:withdrawn を pickup しない | Impl |

**状態遷移**: 新規・更新時の正規出力は `pm:requested → cc:todo → cc:wip → cc:done → pm:approved`。既存 `pm:依頼中 → cc:TODO → cc:WIP → cc:完了 → pm:確認済` も read-compatible。`cc:withdrawn` は terminal state（再開しない）。

**後方互換**: `cursor:依頼中` / `cursor:確認済` は `pm:依頼中` / `pm:確認済` の同義として扱う（Cursor PM 運用時の表記）。

---

## Phase 62: Claude Code 2.1.112-2.1.132 後続活用 + Opus 4.7 upstream follow-up [P1]

Purpose: Phase 56 / Phase 58 で Claude Code `2.1.119`-`2.1.126` を Plans 化し一部 `cc:完了` になっているが、`2.1.112`-`2.1.118` および `2.1.127`-`2.1.132` の 13 バージョンは Feature Table の詳細追跡対象外。CC GitHub CHANGELOG (`https://raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md`) との比較で、Harness の弱点を直接強化できる Tier 1 5 件 (subagent stall, 1h cache, mcp_tool hooks, sandbox deniedDomains, wrapper bypass) と Phase 58 設計済み実装 pending の Tier 2 5 件 (updatedToolOutput, permissionMode, invocation_trigger, SESSION_ID, skillOverrides) を実装 + test 込みで Plans 化する。`62.3.1` で Phase 58 と同じ 4 点接続 (snapshot / Feature Table / CHANGELOG / integration test) を行い、CC 自動継承で済む UI 改善・性能改善・OAuth fix は明示的に対象外と記録する。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 62.1.1 | Claude Code `2.1.113` で導入された subagent stall 10 分 timeout を Worker 契約に組み込む。`agents/worker.md` の retry / escalation 節に CC 側 stall timeout (既定 600 秒) と Harness 側 `elicitation-handler.sh` の能動監視を 2 層防御として併記し、Lead が再 spawn する条件を「`cc:WIP` 状態が 10 分超 または CC stall log を観測」と数値で固定。`docs/team-composition.md` の並列 Worker 数判定 (1/2/3 グループ) に stall 発生時の re-spawn ルールを 1 行追記。`tests/test-claude-upstream-integration.sh` に Phase 62.1.1 の文字列参照を追加し、`.claude/rules/opus-4-7-prompt-audit.md` の数字必須要件 (回数上限・時間上限) を満たすこと | (a) `agents/worker.md` に CC 600s stall + Harness elicitation 2 層防御が明記され `grep -E "600\|10 分\|stall" agents/worker.md` で 1 件以上ヒット、(b) `docs/team-composition.md` に re-spawn 判定条件が「N 分超」「stall log 検出」の数値で書かれる、(c) `tests/test-claude-upstream-integration.sh` が `Phase 62.1.1` を参照検出、(d) `.claude/rules/opus-4-7-prompt-audit.md` の checklist で「回数 / 時間が数字」項目を満たす、(e) `./tests/validate-plugin.sh` 全 PASS | 61.8 | cc:完了 [6ab79d32] |
| 62.1.2 | Feature Table line 198 で `A: 明示追従対象` 分類済みだが未実装の `ENABLE_PROMPT_CACHING_1H` (CC `2.1.108`) を long-running skill で活用する。`skills/harness-loop/SKILL.md`, `skills/breezing/SKILL.md`, `docs/long-running-harness.md` の 3 箇所に「セッション開始時に `export ENABLE_PROMPT_CACHING_1H=1` を行う」ガイダンスと理由 (5 分 TTL では loop / breezing 1 時間超セッションでキャッシュ miss が累積し input token コストが最大 12 倍になりうる) を 1 段落で追加。`scripts/codex-companion.sh task --write --resume-last` 系の long task で env が inherit されるかを確認し、wrapper の修正が不要であれば理由を docs に残す。skill mirror (`skills-codex/`, `codex/.codex/skills/`, `opencode/skills/`) も sync で追従 | (a) `harness-loop` / `breezing` / `long-running-harness.md` の 3 箇所に env 設定例と理由が記載され `rg "ENABLE_PROMPT_CACHING_1H"` で 3 件以上ヒット、(b) `scripts/codex-companion.sh` の env inherit 状況が `docs/long-running-harness.md` または `docs/upstream-update-snapshot-2026-05-07.md` に記録、(c) `tests/test-claude-upstream-integration.sh` が `ENABLE_PROMPT_CACHING_1H` の参照を検出、(d) `bash scripts/sync-skill-mirrors.sh --check` PASS、(e) `./tests/validate-plugin.sh` PASS | 61.8 | cc:完了 [6ab79d32] |
| 62.1.3 | Claude Code `2.1.118` で hooks が `type: "mcp_tool"` で MCP ツールを直接呼び出せるようになった件の採用判断 doc を作成する。実装は次 Phase 判断 (まず設計のみ)。判断材料: (i) PostToolUse から `harness_mem_record_event` を直接呼ぶ場合の遅延 / auth scope / error 伝播の比較、(ii) 現行 `scripts/hook-handlers/*.sh` shell wrapper 経由 vs 直接呼び出しのトレードオフ表、(iii) MCP server 不達時の fallback (silent skip / local ledger queue / drop) の方針、(iv) Phase 61 の `.claude/state/elicitation/events.jsonl` ローカル ledger との関係 | (a) `docs/hooks-mcp-tool-evaluation.md` がある、(b) shell wrapper 経由 vs `type: "mcp_tool"` 直接呼び出しの比較表 (latency / auth / error / fallback の 4 項目) がある、(c) MCP 不達時の fallback 方針が `silent skip` `queue` `drop` のどれかに明示確定、(d) Phase 61 ledger との重複・補完関係が 1 段落で説明、(e) 採用 / 保留 / 却下のいずれかで判断結論が記録、(f) `./tests/validate-plugin.sh` PASS | 61.8 | cc:完了 [6ab79d32] |
| 62.1.4 | Claude Code `2.1.113` で追加された `sandbox.network.deniedDomains` 設定の baseline を Harness テンプレートに導入する。`.claude-plugin/settings.json` の `permissions` に `deniedDomains` baseline (`pastebin.com`, `transfer.sh`, `0x0.st`, `paste.ee`, `gist.github.com` 経由 anonymous gist の制限は対象外で記録のみ) を追加。`harness-init` skill 経由で生成される settings.json に同じ既定値が入ることを確認。`settings.local.json` で project 固有 override を許可。`scripts/ci/check-consistency.sh` で deniedDomains baseline 漏れを検出する | (a) `.claude-plugin/settings.json` の `permissions` ブロックに `deniedDomains` baseline が 4 件以上記載、(b) `skills/harness-init` または同等の生成経路で同じ既定値を出力、(c) `tests/test-settings-baseline.sh` (新規 or 既存拡張) が baseline 既定値の存在を機械検証、(d) `scripts/ci/check-consistency.sh` の本線で deniedDomains 欠落を検出、(e) `./tests/validate-plugin.sh` PASS | 61.8 | cc:完了 [6ab79d32] (template canonical, `.claude-plugin/settings.json` sync は user 手動操作) |
| 62.1.5 | Claude Code `2.1.113` で deny ルールが `env`/`sudo`/`watch` wrapper bypass を matching するように強化された件を Harness の Go guardrail で audit する。R06 (force-push), R11 (`reset --hard`), R12 (push to main) の 3 ルールについて、`env GIT_SSL_NO_VERIFY=1 git push --force` / `sudo git push --force` / `watch -n 1 'git push --force'` のような wrapper bypass test を `go/internal/guardrail/rules_test.go` に各 3 ケース合計 9 ケース追加。漏れがあれば `go/internal/guardrail/rules.go` の matcher を強化、無ければ test だけ追加して fix posture を固定 | (a) `go/internal/guardrail/rules_test.go` に R06/R11/R12 の env/sudo/watch wrapper bypass テストが各 3 ケース合計 9 ケース追加、(b) 漏れがあった場合は `rules.go` で対応し commit に diff が残る、(c) `cd go && go test ./internal/guardrail/... -run TestWrapperBypass -v` で 9 ケース全 PASS、(d) `tests/test-claude-upstream-integration.sh` が Phase 62.1.5 の参照を検出、(e) `./tests/validate-plugin.sh` PASS | 61.8 | cc:完了 [6ab79d32] |
| 62.2.1 | Claude Code `2.1.121` の `PostToolUse.updatedToolOutput` を Phase 58.2.2 設計方針 (opt-in / redaction-compaction-normalization 限定 / before-after audit) に従って実装する。`.claude-plugin/hooks.json` で 1 つだけ opt-in の PostToolUse hook を有効化 (例: machine-readable JSON normalization、デフォルトは無効)。元 output と更新後 output を `.claude/state/output-audit.jsonl` に append-only で記録。レビュー / テスト証拠の改ざんを禁止する仕組みを test で検証 | (a) `scripts/hook-handlers/posttool-output-normalize.sh` または Go 実装が opt-in で動作、(b) `.claude/state/output-audit.jsonl` に before/after が JSON Lines で append-only 記録、(c) `tests/test-output-governance.sh` が「redaction 用途は PASS」「テスト出力の改ざん用途は FAIL」を機械検証、(d) JSON 契約 tool では人間向け説明を mix しないことを test で固定、(e) `./tests/validate-plugin.sh` PASS | 58.2.2, 62.1.5 | cc:完了 [028034ef] |
| 62.2.2 | Claude Code `2.1.119` で `--agent <name>` が agent frontmatter の `permissionMode` を確実に尊重する fix の reaffirmation。`agents/worker.md` (`acceptEdits`)、`agents/reviewer.md` (Read-only)、`agents/scaffolder.md` (default + ask) の permissionMode が CC `2.1.119+` で実適用されるかを `tests/test-agent-permission-mode.sh` で機械検証。Phase 59.2.3 で「silently ignored になり得る」と判定された field のうち CC 2.1.119+ で reactivate されたものを agent frontmatter に正式記載 | (a) `tests/test-agent-permission-mode.sh` が worker / reviewer / scaffolder の permissionMode を CC `2.1.119+` で検証、(b) Reviewer の Read-only enforcement (Edit/Write/Bash 制限) が test で固定、(c) silently ignored だった field のうち reactivate されたものは agent frontmatter に正式記載、(d) `agents/team-composition.md` の権限境界記述と一致、(e) `./tests/validate-plugin.sh` PASS | 59.2.3, 62.1.5 | cc:完了 [028034ef] |
| 62.2.3 | Claude Code `2.1.126` で `claude_code.skill_activated` OTel event が `invocation_trigger` (human / model / skill-chain) を含むようになった件を活用する。`scripts/skill-trigger-telemetry.sh` または `bin/harness skill-telemetry` (Go) で trigger 別カウントを `.claude/state/skill-trigger-stats.jsonl` に append-only 記録。privacy / retention (既定 30 日) / opt-out フラグを `docs/skill-telemetry-policy.md` に固定。Phase 58.2.3 の「telemetry sink 設計が先」判断と矛盾しないことを test で確認 | (a) `scripts/skill-trigger-telemetry.sh` (or Go 実装) が `invocation_trigger` を読んで trigger 別カウントを `.claude/state/skill-trigger-stats.jsonl` に記録、(b) `tests/test-skill-trigger-telemetry.sh` が human / model / skill-chain の 3 trigger を区別して記録できることを検証、(c) `docs/skill-telemetry-policy.md` に privacy / retention / opt-out が書かれる、(d) Phase 58.2.3 の判断と矛盾しないことが docs に明記、(e) `./tests/validate-plugin.sh` PASS | 58.2.3, 62.1.5 | cc:完了 [028034ef] |
| 62.2.4 | Claude Code `2.1.132` で Bash subprocess に `CLAUDE_CODE_SESSION_ID` が env で渡るようになった件を活用し、`scripts/hook-handlers/*.sh` の session ID 取得を env 直接参照に統一する。後方互換のため env 空のときだけ `$CLAUDE_TRANSCRIPT_PATH` regex fallback を残す。3 状態 (env あり / env 無し / 両方無し = legacy fallback) を `.claude/rules/active-watching-test-policy.md` の test 規約に準拠して検証 | (a) `scripts/hook-handlers/memory-session-start.sh` 等で `${CLAUDE_CODE_SESSION_ID:-$(extract_from_transcript)}` パターンに統一、(b) regex fallback が CC `2.1.131` 以下でも動作することを test で固定、(c) `tests/test-hook-handler-session-id.sh` が env あり / env 無し / 両方無し の 3 状態を検証、(d) `.claude/rules/active-watching-test-policy.md` の `TestXxx_NotConfigured` / `TestXxx_Healthy` / `TestXxx_Corrupted` 命名規約に準拠、(e) `./tests/validate-plugin.sh` PASS | 50.1.1, 62.1.5 | cc:完了 [028034ef] (refactor 不要を確認、policy doc + test を新設) |
| 62.2.5 | Claude Code `2.1.129` で `skillOverrides` 設定が `off`, `user-invocable-only`, `name-only` の 3 mode をサポートするようになった件のガバナンス doc を作成する。3 mode の意味、推奨 default、enterprise governance 用途、Harness の skill manifest (`scripts/generate-skill-manifest.sh`) との関係を `docs/skill-overrides-policy.md` に固定。`harness-init` で生成する settings に default を入れるかの判断 (推奨は `name-only` だが互換のため明示無効化を採用するか) を記録 | (a) `docs/skill-overrides-policy.md` に 3 mode の意味、推奨 default、enterprise 用途、Phase 59.1.2 の skill manifest との関係が書かれる、(b) `harness-init` で default を入れるかの判断 (採用 / 非採用 / project-specific) が記録、(c) `tests/test-settings-baseline.sh` が skillOverrides の存在を許容 (強制しない) するよう更新、(d) Phase 59.1.2 の skill manifest と矛盾しない、(e) `./tests/validate-plugin.sh` PASS | 59.1.2, 62.1.5 | cc:完了 [028034ef] |
| 62.3.1 | Phase 62 の upstream snapshot / Feature Table / CHANGELOG / integration test 接続を Phase 58.1.1-58.1.2 と同じスタイルで行う。`docs/upstream-update-snapshot-2026-05-07.md` を新規作成し、CC `2.1.112`-`2.1.118` および `2.1.127`-`2.1.132` を A/C/P 分類 (`B: 書いただけ` 0 件) で snapshot 化。`docs/CLAUDE-feature-table.md` に Phase 62 追補。`CHANGELOG.md` `[Unreleased]` に Phase 62 の「今まで / 今後」を CC アプデ → Harness での活用 形式で追記。`tests/test-claude-upstream-integration.sh` で Phase 62 参照漏れを検出 | (a) `docs/upstream-update-snapshot-2026-05-07.md` がある、(b) `2.1.112`-`2.1.118`, `2.1.127`-`2.1.132` が表で A/C/P 分類されている、(c) `B: 書いただけ 0 件` の理由がある、(d) `docs/CLAUDE-feature-table.md` に Phase 62 追補、(e) `CHANGELOG.md` `[Unreleased]` に Phase 62 の「CC のアプデ → Harness での活用」記述、(f) `bash tests/test-claude-upstream-integration.sh` PASS、(g) `./tests/validate-plugin.sh` PASS (48/48) | 62.1.1, 62.1.2, 62.1.3, 62.1.4, 62.1.5, 62.2.1, 62.2.2, 62.2.3, 62.2.4, 62.2.5 | cc:完了 [bb3cfa5e] |

---

## Phase 65: 3-Surface HTML for Non-Engineer Vibecoder + harness-mem cross-project safety [P1]

Purpose: 非エンジニアの発注者・プロデューサー職向けに、Claude への発注の (1) 計画理解 / (2) 進行管理 / (3) 受け入れ判断の 3 つの認知負荷ピークを HTML artifact で可視化する。各 surface は harness-mem と双方向連携 (read: 過去決定/パターン/類似案件 / write: 個人選好の累積) するが、cross-project search はデフォルト OFF とし、ユーザーが明示的に `cross-project-groups.yaml` でグループ定義した場合のみ opt-in 横断を許可する。Layer 1 (mem `<private>` strip + project filter) は既存だが、Layer 2 (クライアント辞書 + NER) と Layer 3 (HTML 生成直前最終 scan + 監査ログ) を harness 側で新規実装し、3 層防御で固有名詞漏洩を構造的に防ぐ。Phase A → E の段階的解放で、MVP は横断機能なしで体験価値を先に固める。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 65.1.1 | [Phase A: Plan Brief MVP] HTML rendering infrastructure を新規導入する。`scripts/render-html.sh` (約 100-200 行) で mustache 風 (`{{var}}`) テンプレート展開 + jq による JSON データバインドを実装。テンプレート格納ディレクトリは `templates/html/`、出力先は `.claude/state/views/`。テンプレート schema は `html-render-input.v1` (`{kind: "plan-brief"\|"progress"\|"accept", project: string, generated_at: ISO8601, sections: [...]}`) で固定。生成 HTML は単独で開ける (no server, no JS framework)、CSS は inline、Claude Harness ブランド (off-white BG / near-black FG / harness orange accent) 準拠 | (a) `scripts/render-html.sh` が `--template <name> --data <json> --out <path>` の 3 引数で動作、(b) `templates/html/test-fixture.html.template` で `{{title}}` および `{{#sections}}{{name}}{{/sections}}` の 2 種類が展開可能、(c) `tests/test-render-html.sh` で 4 ケース (正常 / 空 sections / 不正 JSON / 存在しないテンプレート) を機械検証、(d) 出力 HTML は `lynx -dump` で読める text 構造を持つ、(e) CSS palette が Claude Harness ブランド (`#FAFAFA` off-white / `#0F0F0F` near-black / `#F58A4A` harness orange) を使用、(f) `./tests/validate-plugin.sh` PASS | 64.2 | cc:完了 [49f8a0e6] |
| 65.1.2 | `harness-plan-brief` skill を新規作成する。`skills/harness-plan-brief/SKILL.md` (frontmatter `name: harness-plan-brief`, `user-invocable: true`, `argument-hint: "[task-description]"`)。skill の責務は (i) 現プロジェクトのみで harness-mem search を実行 (`project: <current>`, `strict_project: true` 強制)、(ii) 過去 decision (D1-D41) と pattern (P1-P33) と Plans archive 28 件から類似案件を抽出、(iii) `plan-brief-context.v1` schema (`{user_request, my_understanding, options[], risks[], acceptance_criteria[], confidence: 0-100, related_decisions[], similar_past_plans[]}`) を構築、(iv) `templates/html/plan-brief.html.template` 経由で HTML 生成、(v) ブラウザで自動 open (`open` / `xdg-open` / `start` の OS 別 dispatch) | (a) `skills/harness-plan-brief/SKILL.md` 存在 (frontmatter は skill-editing.md 規約準拠)、(b) skill 起動時に `mcp__harness__harness_mem_search` を `project: $(basename $(git rev-parse --show-toplevel))` 強制で 1 回以上呼び出す、(c) cross-project search を**呼ばない** (test で `project` parameter 必須が確認できる)、(d) 出力 `plan-brief-context.v1` schema が JSON Schema validator で valid、(e) `tests/test-harness-plan-brief.sh` で fixture プロジェクトに対し HTML 生成と auto-open dispatch を機械検証 (CI 環境では `BROWSER=true` で skip)、(f) `./tests/validate-plugin.sh` PASS | 65.1.1 | cc:完了 [b523dee1] |
| 65.1.3 | Plan Brief の context compilation logic を実装する。`scripts/plan-brief-compile.sh` (約 150-250 行) で: (i) ユーザー request からキーワード抽出 (FTS で `harness_mem_search`)、(ii) 上位 5 件の decision / pattern / past plan を `expand_links: true` で取得、(iii) 自信度算出ロジック (過去類似案件の成功率 baseline + 現タスクの DoD 明確度の 2 軸) を実装。`confidence: 0-100` の整数で出力。算出根拠を `confidence_evidence: string[]` に同梱 | (a) `scripts/plan-brief-compile.sh` が `--query <text> --project <name>` で context JSON を stdout 出力、(b) `confidence` 算出が「過去類似案件の cc:完了率 (40 点満点) + DoD の数値要件含有率 (30 点満点) + 関連 D/P 件数の有意性 (30 点満点)」の合計、(c) `tests/test-plan-brief-compile.sh` で 4 ケース (類似案件 0 件 / 5 件 / 全完了 / 半数失敗) を機械検証、(d) `confidence_evidence` に「過去 N 件中 M 件成功」のような数値が 1 行以上含まれる、(e) `./tests/validate-plugin.sh` PASS | 65.1.2 | cc:完了 [f545ae03] |
| 65.1.4 | Plan Brief 承認時の memory 書き込み (write side) を実装する。`scripts/plan-brief-record-decision.sh` で `mcp__harness__harness_mem_ingest` 経由で `observation_type: "decision"` の record を作成。schema `personal-preference.v1` (`{user_request_hash: sha256, chosen_option: string, rejected_options: string[], reasoning: string, timestamp: ISO8601, project: string}`) で固定。**個人選好データ蓄積**の起点。承認 / 修正依頼 / 質問の 3 アクションを区別記録 | (a) `scripts/plan-brief-record-decision.sh` が承認 / 修正依頼 / 質問の 3 action mode を持つ、(b) record される observation の `tags` に `personal-preference` `plan-brief-approval` の 2 タグが含まれる、(c) `tests/test-plan-brief-record.sh` で 3 action 各 1 ケース合計 3 ケース機械検証、(d) `mcp__harness__harness_mem_search` で `tags: personal-preference` 検索すると 1 ヶ月後に 30 件以上ヒット可能な構造、(e) `./tests/validate-plugin.sh` PASS | 65.1.2 | cc:完了 [554c5d21] |
| 65.1.5 | Phase 65.1.x の e2e validation。実プロジェクト fixture (`tests/fixtures/plan-brief-e2e/`) で「ユーザー request → Plan Brief HTML 生成 → 承認 → memory 書き込み」のフローを通し検証 | (a) `tests/test-plan-brief-e2e.sh` で 5 ステップ (request 入力 / mem search / HTML 生成 / 承認 / mem write) を順次検証、(b) 生成 HTML が `plan-brief-context.v1` schema と一致、(c) memory 書き込み後 `mcp__harness__harness_mem_search query: "<request hash>"` で当該 record を再取得可能、(d) `./tests/validate-plugin.sh` 49+/49+ PASS (新規テスト追加 1 件以上)、(e) `bash scripts/ci/check-consistency.sh` 全合格 | 65.1.1, 65.1.2, 65.1.3, 65.1.4 | cc:完了 [2f3aed56] |
| 65.2.1 | [Phase B: Acceptance Demo] `harness-accept` skill を新規作成する (Phase 65.1 と同パターン)。`skills/harness-accept/SKILL.md`、`templates/html/accept.html.template`。schema `acceptance-context.v1` (`{user_request, demo_artifacts: [{kind: "video"\|"screenshot"\|"text", path}], verified_criteria: [{name, passed: bool, evidence}], unverified_caveats[], past_issue_patterns[], recommendation: "ship"\|"wait"\|"reject"}`)。Plan Brief 時に書き込んだ acceptance_criteria を read 側で取得し、各 criterion ごとに verified/unverified を表示 | (a) `skills/harness-accept/SKILL.md` 存在、(b) Plan Brief で書き込んだ `personal-preference.v1` の acceptance_criteria を本 skill が read で取得、(c) `verified_criteria[].evidence` が空文字列のとき HTML で警告表示、(d) `recommendation` 算出ロジック (verified 数 / 全 criteria 数 ≥ 0.8 → ship、≥ 0.5 → wait、それ未満 → reject) が unit test で固定、(e) `tests/test-harness-accept.sh` で 4 ケース (全 verified / 半 verified / 全 unverified / criteria 0 件) を機械検証、(f) `./tests/validate-plugin.sh` PASS | 65.1.5 | cc:完了 [e62c7027] |
| 65.2.2 | Acceptance Demo の「過去の問題パターン」呼び出し (read side) を実装する。`scripts/accept-past-issues.sh` で patterns.md (P1-P33) と過去の `acceptance-context.v1` record を semantic search し、現タスクと類似する post-launch issue 履歴を最大 3 件取得。**まだ project-only 検索** (cross-project は Phase 65.3 で解放) | (a) `scripts/accept-past-issues.sh` が `--project <name> --task <description>` で過去 issue 履歴 (`past-issue.v1` schema) を出力、(b) cross-project search を**呼ばない** (project parameter 必須を test で固定)、(c) 上位 3 件取得 + 各々に `verified_in_current_task: bool` 付与、(d) `tests/test-accept-past-issues.sh` で 3 ケース (issue 0 件 / 3 件 / verified 半数) を機械検証、(e) `./tests/validate-plugin.sh` PASS | 65.2.1 | cc:完了 [a85f903a] |
| 65.2.3 | Acceptance Demo 承認時の memory 書き込み (write side)。schema `acceptance-decision.v1` (`{user_request_hash: sha256, recommendation_taken: bool, override_reason: string?, verified_criteria_at_decision[], post_launch_concerns[], timestamp: ISO8601}`)。Plan Brief で書き込んだ user_request_hash と紐付けて、**プラン → 受け入れの完全 trace** を構築 | (a) `scripts/accept-record-decision.sh` 存在、(b) record の `user_request_hash` が Plan Brief 側 record と join 可能、(c) `mcp__harness__harness_mem_graph` で plan→accept の chain を取得可能 (test で確認)、(d) `tests/test-accept-record.sh` で 3 ケース (recommendation 採用 / override / reject) を機械検証、(e) `./tests/validate-plugin.sh` PASS | 65.2.1 | cc:完了 [5c65f1ce] |
| 65.2.4 | Phase 65.2.x の e2e validation。Plan Brief approval → 実装 → Acceptance Demo の 3 段階を 1 つの fixture project で通し、`personal-preference.v1` と `acceptance-decision.v1` が同 user_request_hash で紐付くことを検証 | (a) `tests/test-plan-accept-flow-e2e.sh` で 3 段階 (plan → impl simulation → accept) を順次検証、(b) memory に 2 種の record が join 可能な状態で保存される、(c) HTML 2 種 (plan-brief / accept) が同 task で生成され、両方とも user_request を共有、(d) `./tests/validate-plugin.sh` 50+/50+ PASS、(e) `bash scripts/ci/check-consistency.sh` 全合格 | 65.2.1, 65.2.2, 65.2.3 | cc:完了 [d2229a04] |
| 65.3.1 | [Phase C: Cross-Project Group + 3 層防御] `cross-project-groups.yaml` schema を策定し、初期テンプレートを設置する。`.claude/rules/cross-project-groups.yaml` (新規 SSOT) を作成し、schema `cross-project-group.v1` (`{groups: [{name: string, members: string[], description: string?}]}`) で固定。default は空 (`groups: []`) で、横断検索は無効化される。設定例として 2 グループ (Personal Tools / Client-Sample) のコメントアウト雛形を含める | (a) `.claude/rules/cross-project-groups.yaml` 存在、初期 `groups: []`、(b) `cross-project-group.v1` schema が `docs/cross-project-groups-schema.md` に記載、(c) `scripts/load-cross-project-groups.sh` で yaml を JSON にパースし、不正な schema 時は exit 1、(d) `tests/test-cross-project-groups-schema.sh` で 4 ケース (空 / 1 group / member 重複 / 不正 schema) を機械検証、(e) `./tests/validate-plugin.sh` PASS | 65.2.4 | cc:完了 [4a014137] |
| 65.3.2 | `client-redaction.yaml` schema を策定し、Layer 2 辞書ベースの固有名詞検出を実装する。`.claude/rules/client-redaction.yaml` (新規 SSOT) を作成し、schema `client-redaction.v1` (`{clients: [{name, redact_as, aliases[]?}], people: [...], domains: [...]}`)。default は空。`scripts/redact-by-dictionary.sh` で「テキストを受け取り → 辞書ヒットを redact_as で置換 → 置換数とともに stdout 返却」 | (a) `.claude/rules/client-redaction.yaml` 存在、初期 `clients: [], people: [], domains: []`、(b) `scripts/redact-by-dictionary.sh` が `--input <text> --dict <yaml-path>` で動作、(c) ヒット 0 件時は exit 0 で原文そのまま、ヒット時は stderr に件数記録、(d) `tests/test-redact-by-dictionary.sh` で 5 ケース (ヒット 0 / 1 / 複数 / aliases / 重複 redact_as) を機械検証、(e) `./tests/validate-plugin.sh` PASS | 65.2.4 | cc:完了 [5152bed2] |
| 65.3.3 | Layer 2 の NER (Named Entity Recognition) 検出層を追加する。kuromoji-js または同等の lightweight Japanese tokenizer を使用し、形態素解析後 `proper noun` (固有名詞) tag を持つトークンを抽出。`scripts/redact-by-ner.sh` で「テキスト → NER ヒット → `[Entity]` トークンへの置換」を実装。辞書ヒットと NER ヒットは独立に集計し、Layer 3 で合算 | (a) `scripts/redact-by-ner.sh` が `--input <text>` で固有名詞を `[Entity]` に置換、(b) Japanese tokenizer (kuromoji 等) が node_modules または stand-alone CLI として利用可能、(c) `tests/test-redact-by-ner.sh` で 4 ケース (人名 / 会社名 / 地名 / 固有名詞 0 件) を機械検証、(d) NER 失敗時 (tokenizer 不在) は fail-open で原文そのまま + stderr 警告、(e) `./tests/validate-plugin.sh` PASS | 65.3.2 | cc:完了 [20a4478f] |
| 65.3.4 | Layer 3 の HTML 生成直前最終 scan を実装する。`scripts/render-html.sh` を拡張し、HTML 生成前に `redact-by-dictionary.sh` → `redact-by-ner.sh` → 最終 sanity check (固有名詞らしきカタカナ連続 5 文字以上を検出) の 3 段を順次走らせる。3 段目で残骸が見つかった場合は HTML を**生成せず exit 1**、stderr に検出内容と該当テンプレートを出力 (fail-safe) | (a) `scripts/render-html.sh` に `--with-redaction` flag が追加、(b) flag 有効時に dict → NER → final scan の 3 段順次実行、(c) final scan で残骸検出時は HTML 生成中止 + exit 1 + stderr に「detected: <token>, source: <line>」出力、(d) `tests/test-render-html-redaction.sh` で 4 ケース (全 clean / dict のみヒット / NER のみヒット / final scan で検出 = exit 1) を機械検証、(e) `./tests/validate-plugin.sh` PASS | 65.3.3 | cc:完了 [0ae3f40a] |
| 65.3.5 | Cross-project search の opt-in 機構を harness-plan-brief / harness-accept skill に組み込む。`--cross-project-group <name>` flag を追加し、指定時のみ `cross-project-groups.yaml` から member projects を解決して **MCP `mcp__harness__harness_mem_search` を member ごとに N 回 call し client 側でマージ** (D43 Option α、mem 側 MCP に projects 配列が無いため)。flag 不在時は現プロジェクトのみ (Phase 65.1.2 の挙動を維持)。group 名が yaml に存在しない場合は exit 1 | (a) `harness-plan-brief` / `harness-accept` 両 skill が `--cross-project-group <name>` flag を受け取る、(b) flag 不在時は現プロジェクト single search (Phase 65.1.2 と同じ挙動)、(c) flag 有効時は yaml から member resolve し各 project で MCP search を 1 回ずつ発行 → client でマージ・dedupe・relevance_score 順 sort、(d) group 名が存在しない場合は exit 1 + stderr に「group not found」、(e) `tests/test-cross-project-flag.sh` で 4 ケース (flag 不在 / 有効 group / 無効 group / 空 group) を機械検証、(f) `./tests/validate-plugin.sh` PASS | 65.3.1, 65.3.4 | cc:完了 [09377eb9] |
| 65.3.6 | 監査ログの実装。Cross-project search が走った場合に `.claude/state/audit/cross-project-search.jsonl` に append-only 記録。schema `cross-project-audit.v1` (`{timestamp: ISO8601, group_name, member_projects[], query_hash: sha256, redaction_count: {dict: int, ner: int}, output_passed_final_scan: bool}`)。実際のクエリ文字列は記録しない (privacy)、hash のみ。HTML の最下部に「この HTML は <N> 件 redact しました」と監査サマリを表示 | (a) `.claude/state/audit/cross-project-search.jsonl` が cross-project search 1 回ごとに 1 行追加、(b) JSON Lines 各行が `cross-project-audit.v1` schema 準拠、(c) クエリ文字列の生記録なし、hash のみ、(d) HTML 最下部に「redacted: dict X 件 + NER Y 件」のサマリが表示、(e) `tests/test-cross-project-audit.sh` で 3 ケース (redaction 0 / 複数 / final scan 失敗) を機械検証、(f) `./tests/validate-plugin.sh` PASS | 65.3.4, 65.3.5 | cc:完了 [272a8f33] |
| 65.3.7 | Phase 65.3.x の e2e validation。fixture group 設定 + dict + NER 各層を全て通すクロスプロジェクト Plan Brief を生成し、3 層防御で固有名詞が漏れないことを検証 | (a) `tests/test-cross-project-redaction-e2e.sh` で 1 fixture group (3 member projects) で Plan Brief 生成、(b) 各 member project に予め固有名詞を含む observation を挿入、(c) Plan Brief 出力 HTML に固有名詞 (生のクライアント名 / 人名) が含まれないことを `grep -E "(NoraiCorp\|YorozuPro\|田中\|佐藤)" output.html` で 0 件確認、(d) audit log に redaction 件数が正しく記録、(e) `./tests/validate-plugin.sh` 53+/53+ PASS、(f) `bash scripts/ci/check-consistency.sh` 全合格、(g) [D43] envelope (signals + prose) 構造への redaction を test fixture に含め、signals[] と prose の整合性 (validateProseContainsSignals 不変条件) が壊れないこと、(h) [D43] 既存 server 側 `[REDACTED_*]` mark の二重置換ガードを test 検証 (event-recorder.ts:redactContent との衝突防止) | 65.3.1, 65.3.2, 65.3.3, 65.3.4, 65.3.5, 65.3.6 | cc:完了 [c05d6ef8] |
| 65.4.1 | [Phase D: Progress Tracker] `harness-progress` skill skeleton を新規作成する。`skills/harness-progress/SKILL.md`、`templates/html/progress.html.template`。schema `progress-snapshot.v1` (`{project, current_task, progress_pct: 0-100, todo_tasks[], wip_tasks[], done_tasks[], elapsed_minutes: int, estimated_total_minutes: int, cost_so_far_usd: float, cost_estimate_usd: float, alerts[]}`)。生成 HTML は静的 (PWA や WebSocket は使わず、再生成で更新) | (a) `skills/harness-progress/SKILL.md` 存在、(b) `progress-snapshot.v1` schema が JSON Schema validator で valid、(c) HTML 生成時に Plans.md の cc:WIP / cc:TODO / cc:完了 件数を反映、(d) `tests/test-harness-progress.sh` で fixture Plans.md (各 status 含む) で snapshot HTML が正しい % で表示されることを機械検証、(e) `./tests/validate-plugin.sh` PASS | 65.3.7 | cc:完了 [1175b9e4] |
| 65.4.2 | Progress Tracker の自動再生成機構を実装する。PostToolUse hook で「Edit / Write / Bash の発火」をトリガーに、`harness-progress` skill を background 起動して HTML を再生成。生成頻度は最大 1 分に 1 回 (rate limit) で過剰生成を防ぐ | (a) `scripts/hook-handlers/posttool-progress-regen.sh` が PostToolUse hook で起動、(b) `.claude/state/progress-last-regen.txt` に last regen timestamp 記録、(c) 60 秒以内の連続発火は skip (rate limit)、(d) `.claude-plugin/hooks.json` の dual sync が `hooks/hooks.json` と整合 (P29 規約)、(e) `tests/test-progress-regen.sh` で 4 ケース (初回 / 60秒以内 / 60秒超 / hook input 不正) を機械検証、(f) `./tests/validate-plugin.sh` PASS | 65.4.1 | cc:完了 [847ea36d] |
| 65.4.3 | Drift detection と alert を実装する。schema `progress-alert.v1` (`{kind: "scope-creep"\|"time-overrun"\|"repeated-failure"\|"cost-warning"\|"high-risk-file", severity: "info"\|"warn"\|"critical", message, suggested_action, triggered_at: ISO8601}`)。各 alert kind に対応する検出ロジックを実装: scope-creep (Plans.md にない file 編集を検出)、time-overrun (estimated の 1.5 倍超)、repeated-failure (test fail 3 回連続)、cost-warning (上限の 80%)、high-risk-file (`harness.toml` deny path matching) | (a) `scripts/progress-detect-drift.sh` が 5 alert kind それぞれを検出、(b) 検出結果を `progress-alert.v1` schema で出力、(c) `tests/test-progress-drift.sh` で各 alert kind 計 5 ケース機械検証、(d) HTML 表示時に critical alert は赤、warn は黄、info は青で色分け、(e) `./tests/validate-plugin.sh` PASS | 65.4.2 | cc:完了 [a835bde0] |
| 65.4.4 | Progress Tracker の「過去の判断パターン」表示を実装する (read side)。alert 検出時に `mcp__harness__harness_mem_search` で過去の同種 alert への user 判断 (Phase 65.1.4 で書き込まれた `personal-preference.v1` から該当 alert kind を抽出) を取得し、HTML に「過去 N 件中 M 件で同様の提案を断っています」と表示 | (a) `scripts/progress-past-judgments.sh` が alert kind と現プロジェクト名で過去判断を検索、(b) 上位 3 件の judgment と「断る確率 N%」が JSON 出力、(c) cross-project search はデフォルト OFF (Phase 65.3.5 と同じ flag mechanism)、(d) `tests/test-progress-past-judgments.sh` で 4 ケース (judgment 0 件 / 3 件 / 全断る / 全許可) を機械検証、(e) `./tests/validate-plugin.sh` PASS | 65.4.3, 65.1.4 | cc:完了 [6be27a68] |
| 65.4.5 | Phase 65.4.x の e2e validation。Plans.md に WIP/TODO/完了タスクを混在させた fixture で Progress Tracker 生成、scope-creep alert 発火、過去判断表示、再生成 rate limit を順次検証 | (a) `tests/test-progress-e2e.sh` で 5 ステップ (初回生成 / Plans 編集後再生成 / scope-creep 発火 / 過去判断表示 / rate limit 検証) を順次実行、(b) 全 5 alert kind が HTML に正しく表示、(c) audit log に regen 履歴が記録、(d) `./tests/validate-plugin.sh` 56+/56+ PASS、(e) `bash scripts/ci/check-consistency.sh` 全合格 | 65.4.1, 65.4.2, 65.4.3, 65.4.4 | cc:完了 [495dfffc] |
| 65.5.1 | [Phase E: 統合 + 監査 UI + ドキュメント] 3 surface (plan-brief / progress / accept) の統合 e2e。1 fixture project でユーザー request → Plan Brief → impl simulation → Progress (alerts) → Acceptance Demo の全フローを通し、3 つの HTML が同一 user_request_hash で trace 可能な状態を検証 | (a) `tests/test-3-surface-e2e.sh` で 5 段階 (plan / progress regen / drift alert / impl complete / accept) を順次実行、(b) 3 種の memory record (plan-brief-approval / progress-judgment / acceptance-decision) が同 hash で graph join 可能、(c) 3 種の HTML が同 fixture から生成され、それぞれ画面遷移リンクで相互参照、(d) `./tests/validate-plugin.sh` 全 PASS、(e) `bash scripts/ci/check-consistency.sh` 全合格 | 65.4.5 | cc:完了 [03629d85] |
| 65.5.2 | 監査 UI を 3 つの HTML 全てに統合する。各 HTML 最下部に「🔍 この artifact の根拠」セクションを追加し、(i) 検索範囲 (project 名 + group 名)、(ii) 参照した D / P / past plan の ID リスト、(iii) redact された語の件数 (kind 別)、(iv) 完全な audit log への link (`.claude/state/audit/*.jsonl`) を表示 | (a) 3 種の HTML テンプレートに `audit-trail` セクションが共通追加、(b) 4 項目 (検索範囲 / 参照 ID / redact 件数 / log link) が表示、(c) audit log が JSON Lines で human-readable、(d) `tests/test-audit-ui-presence.sh` で 3 種 HTML 全てに audit-trail セクションが含まれることを `grep -c "audit-trail"` で検証、(e) `./tests/validate-plugin.sh` PASS | 65.5.1 | cc:完了 [56543c7e] |
| 65.5.3 | 開発者 / ユーザー向けドキュメントを整備する。`docs/cognitive-load-surfaces.md` で 3 surface の役割と使い方、`docs/cross-project-safety.md` で 3 層防御の説明、`docs/cross-project-groups-schema.md` でグループ設定方法。各 doc は非エンジニア向けの語彙で書く (技術用語は最小限) | (a) 3 つの新規 doc が `docs/` 配下に存在、(b) 合計 1500 行以下 (簡潔さ重視)、(c) 各 doc に「やりたいこと」「やり方」「気をつけること」のセクションが必ず存在、(d) `docs/CLAUDE-skill-catalog.md` に 3 つの新スキルが追記、(e) CLAUDE.md の Top Skill Areas table に「cognitive-load (Plan Brief / Progress / Accept)」行を追加、(f) `./tests/validate-plugin.sh` PASS | 65.5.2 | cc:完了 [fb47a79e] |
| 65.5.4 | Feature Table と CHANGELOG への追補。`docs/CLAUDE-feature-table.md` の Phase 65 行追加 (`A: 実装あり` 分類)、`CHANGELOG.md` `[Unreleased]` に Phase 65 を「今まで / 今後」形式で追記。`.claude/rules/github-release.md` の release-notes ルール準拠 | (a) `docs/CLAUDE-feature-table.md` に Phase 65 の 3 surface 行追加 (A: 実装あり)、(b) `CHANGELOG.md` `[Unreleased]` に Phase 65 が「今まで (markdown 200 行スクロール) / 今後 (HTML カードでひと目)」の対比形式で記載、(c) Before / After table が含まれる、(d) `tests/test-changelog-format.sh` (既存または新規) PASS、(e) `./tests/validate-plugin.sh` PASS | 65.5.3 | cc:完了 [99695676] |
| 65.5.5 | Phase 65 全体のリリース準備。VERSION/plugin.json/harness.toml/marketplace.json の 4 点同期、SemVer 判定 (新スキル 3 個 + 新 hook 追加 = minor)、リリースタグ準備 | (a) `./scripts/sync-version.sh bump --minor` 実行で 4 ファイル同期 (現 4.8.1 → 4.9.0)、(b) SemVer 判定の根拠 (3 新スキル + Phase 65.4 で hook 1 件追加 + new yaml SSOT 2 件) が `CHANGELOG.md` に明記、(c) `bash tests/validate-plugin.sh` 全 PASS、(d) `bash scripts/ci/check-consistency.sh` 全合格、(e) GitHub Release notes が `.claude/rules/github-release.md` 規約準拠 | 65.5.4 | cc:完了 [8b3ba01b] |

---

## Phase 72: New Harness root spec + lane/stage planning contract [P1]

Purpose: `Plans.md` は作業台帳、root `spec.md` は repo 全体の product contract として分離し、AI が未取得・未観測のデータを「存在しない」と断定しない New Harness の正本を作る。過去の成功パターンである Plans first、spec SSOT、TDD red evidence、major-only / full review の使い分け、OK まで再レビュー、PR / release evidence を、`[lane:fast|gate|release]` と stage gate で一貫運用する。Plan の作り方は「検証・調査 -> 実装計画確定 -> 実装(TDD) -> レビュー -> PR closeout」とし、既存 `cc:TODO/WIP/完了` marker は壊さず、lane / stage metadata と DoD で表現する。`harness-review` は read-only boundary を維持し、PR 作成は `harness-work` 側の明示 closeout として扱う。Release lane は PR と公開配布を混同せず、tag / GitHub Release / CI / version surface まで削らない。

V2 Spec Snapshot: root `spec.md` を上位正本、`docs/plans/spec-ssot.md` を spec 運用ルール、`docs/architecture/hokage-core.md` と `go/SPEC.md` を sub-spec とする。作業は `[lane:fast]`, `[lane:gate]`, `[lane:release]` で分類し、stage gate は「検証・調査 -> 実装計画確定 -> 実装(TDD) -> レビュー -> PR closeout -> release closeout」。未取得・未観測の情報は `unknown` とし、`not_observed != absent` を V2 の必須契約にする。PR closeout は `harness-work` 側、release は `harness-release` 側、`harness-review` は read-only quality gate のまま維持する。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 72.1.1 | [Plan / SSOT] root `spec.md` を `Plans.md` と同階層に新設し、repo 全体の上位 product contract として固定する。`docs/plans/spec-ssot.md` は「spec 運用ルール」、`docs/architecture/hokage-core.md` は architecture sub-spec、`go/SPEC.md` は Go runtime sub-spec と明記し、二重正本化を防ぐ。`spec.md` には Purpose / Users And Workflows / Core Workflow / Lane Taxonomy / Unknown Data Contract / Host Adapter Boundary / Data And Contracts / Non-Goals / Open Decisions / Links を置く。[tdd:skip:docs-only] | (a) root `spec.md` が存在し、`Plans.md is the task ledger` と `spec.md is the product contract` の役割分担が明記される、(b) `spec.md` に `not_observed != absent` または同等の未観測データ契約がある、(c) `docs/plans/spec-ssot.md` の優先 spec path が root `spec.md` -> existing project spec -> `docs/spec/00-project-spec.md` の順になる、(d) `spec.md` が `docs/architecture/hokage-core.md` と `go/SPEC.md` を sub-spec としてリンクし、置き換えないと明記、(e) `bash tests/test-spec-ssot-workflow.sh` PASS | 71.1.1 | cc:完了 |
| 72.1.2 | [Planning Flow] `harness-plan` の create/add 契約を更新し、Plan 作成を「検証・調査 -> 実装計画確定 -> 実装(TDD) -> レビュー -> PR closeout」の stage gate で出力する。各 task に `[lane:fast]`, `[lane:gate]`, `[lane:release]` のいずれかを付け、Fast/Gate/Release を新 skill ではなく Plans metadata として扱う。AI が見えていない data / failed search / missing fixture / API unavailable は `unknown` とし、`absent` と断定しない。 | (a) `skills/harness-plan/SKILL.md` と `skills/harness-plan/references/create.md` が root `spec.md` 優先、lane taxonomy、stage gate、unknown data contract を説明、(b) Plans template が既存 5 column format を維持し、Content または DoD に lane/stage metadata を入れる、(c) `[lane:fast]` は low-risk local work、`[lane:gate]` は spec / workflow / mirror / guardrail、`[lane:release]` は public artifact / version / tag / GitHub Release と明記、(d) `bash tests/test-harness-plan-quality.sh` または同等の new governance test が lane/stage/root spec/unknown data contract を検出、(e) `bash scripts/sync-skill-mirrors.sh --check` PASS | 72.1.1 | cc:done [3d904d1f] |
| 72.1.3 | [Execution / TDD] `harness-work` と Worker / Scaffolder / Reviewer input を更新し、`spec_path`, `lane`, `stage`, `research_evidence`, `tdd_red_log`, `review_artifact`, `pr_closeout` を sprint contract へ渡す。`[tdd:required]` task では red-log または明示 `skip_tdd_reason` がない限り完了扱いにしない。Fast lane は軽くするが、unknown data contract と focused checks は省かない。 | (a) `skills/harness-work/SKILL.md`, `skills/harness-work/references/execution-modes.md`, `agents/worker.md`, `agents/scaffolder.md`, `agents/reviewer.md` が `spec_path` と `lane` を扱う、(b) `scripts/generate-sprint-contract.js` または contract enrichment が lane/stage/evidence fields を出力、(c) TDD required で red evidence 不足なら review/closeout が fail する test がある、(d) Fast lane の closeout は focused tests + evidence pack を要求し、full review を必須にしない、(e) `bash tests/test-spec-ssot-workflow.sh` と `bash tests/test-tdd-enforcement-l1l2l4.sh` PASS | 72.1.2 | cc:done [bbc037e0] |
| 72.1.4 | [Review Gate] `harness-review` / `harness-release` の gate を更新し、root `spec.md` alignment、Plans lane/stage、TDD evidence、unknown data contract、accepted/rejected findings、release-preflight warning handling を確認する。`harness-review` は read-only のままにし、`APPROVE` は commit / push / PR 作成命令ではないと維持する。 | (a) `skills/harness-review/SKILL.md` と references が `spec.md` alignment、`not_observed != absent`、lane/stage、TDD evidence、evidence pack を合格条件に含む、(b) 証拠なしの「問題なし」「データなし」を APPROVE しない contract が test で検出される、(c) `skills/harness-release/SKILL.md` が PR ready と release ready を分け、release lane では version/tag/GitHub Release/CI を要求、(d) `bash tests/test-harness-review-governance.sh` と `bash tests/test-harness-release-governance.sh` PASS、(e) `bash scripts/sync-skill-mirrors.sh --check` PASS | 72.1.3 | cc:done [d4251f4d] |
| 72.1.5 | [PR Closeout] `harness-work --pr` または `scripts/harness-pr-closeout.sh` を追加し、review APPROVE 後に PR title/body を evidence pack から作る。PR 作成は外部 side effect なので default は dry-run または明示確認 gate とし、`harness-review` から自動 push/PR/merge はしない。 | (a) PR closeout artifact が `base_ref`, `head_ref`, `spec_path`, `lane`, `stage`, review command, focused tests, accepted findings, rejected findings, release-preflight warnings, residual risk を含む、(b) default dry-run で PR body preview を出し、明示 flag または confirmation なしに `git push` / `gh pr create` しない、(c) detached HEAD では branch 作成が必要なことを fail-fast または ask gate で扱う、(d) `bash tests/test-pr-closeout.sh` または同等 test PASS、(e) `bash scripts/codex-companion.sh review --base "${TASK_BASE_REF}"` 経路と矛盾しない | 72.1.4 | cc:done [4e88d80c] |
| 72.1.6 | [Mirror / Final Validation] Phase 72 全体の mirror / distribution / no-regression closeout。`skills/` SSOT から `codex/.codex/skills/`, `opencode/skills/`, `.agents/skills/` へ同期し、root `spec.md` と Plans lane/stage 契約が Claude / Codex / OpenCode で drift しないことを検証する。 | (a) `bash scripts/sync-skill-mirrors.sh --check` PASS、(b) `bash tests/test-skill-design-contract.sh` PASS、(c) `bash tests/test-bootstrap-routing-contract.sh` PASS、(d) `bash tests/test-codex-package.sh` PASS、(e) `node scripts/validate-opencode.js` PASS、(f) `bash tests/test-distribution-archive.sh` PASS、(g) `bash scripts/release-preflight.sh --check-adapters` または current supported equivalent PASS、(h) `./tests/validate-plugin.sh` PASS、(i) `Plans.md` の 72.1.1-72.1.6 が evidence commit refs 付きで `cc:完了` に更新される | 72.1.1, 72.1.2, 72.1.3, 72.1.4, 72.1.5 | cc:done [6630a194] |

---

## Phase 77: Public issue intake closeout + Done marker canonicalization [P1]

Purpose: v4.12.2 release 後に残った open PR / Issue を、公開ユーザーの痛みと release safety の順に整理する。PR #121 は中国語 description i18n の大型・競合中 PR として後日対応に回し、この Phase では触らない。Issue #147 は単なる parser alias 追加では相手の痛みを解消しないため、新規・更新時の Plans.md / summaries / `.claude/state` notification が `cc:done` を含む English marker family に統一されることを product contract にする。既存 `cc:TODO`, `cc:WIP`, `cc:完了`, `pm:依頼中`, `pm:確認済` は read-compatible として残し、bulk migration は明示コマンドまたはユーザー承認なしに実行しない。Issue #146 は guardrail / runtime reason strings の English default 化、Issue #140 / #131 は既存実装が満たしている範囲を再検証して closeout、PR #136 / #137 は checks rerun 後の低リスク dependency merge、Issue #149 は listing PR を出すかコメントで判断する。

Spec delta: `spec.md` に I18n And Status Marker Contract を追加し、`docs/i18n-language-contract.md` を Issue #147 対応へ更新した。新しい product rule は「新規・更新 writer output は English marker family (`cc:done` など) を emit し、legacy/Japanese markers は read-compatible として受け続ける」。これにより前回案の弱点だった「raw Plans.md の混在言語は残る」問題を解消対象に入れる。Japanese opt-in は説明文・テンプレート文・runtime prose を日本語にできるが、status marker の新規・更新出力は English marker family のままにする。

New Session Start: 次は 77.1.10 から開始する。77.1.2-77.1.3 で #147 の failing tests と canonical marker writer/read compatibility は green になった。77.1.4 は既存 i18n locale resolver / Go guardrail tests で #146 対応済みを確認した。77.1.5-77.1.6 は `docs/research/phase-77-issue-closeout.md` に closeout evidence / comment draft を残した。77.1.7 は PR #136/#137 を個別 merge し、main の `validate-plugin` / `scorecard` が両方 success になったことを確認した。77.1.8 は awesome-codex-plugins の current CONTRIBUTING / README / bundle 状態を確認し、現時点では icon + `interface.composerIcon` と同名 entry ambiguity が未解消のため external PR はまだ出さない判断を evidence 付きで固定した。77.1.9 は Codex companion review の auth failure を記録したうえで manual-pass review closeout を実施し、`docs/research/phase-77-review-closeout.md` に APPROVE evidence を残した。Public issue close/comment は #147 と #131 が main に入ってから実施するため 77.1.10 に分離する。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 77.1.1 | `[Plan / Contract Freeze]` `[lane:gate]` `[tdd:skip:docs-contract]` open PR / Issue intake の方針を固定する。#121 は後日対応に回し、#147 は alias-only ではなく new/update writer / notification cleanup として扱う。`spec.md`, `docs/i18n-language-contract.md`, `Plans.md` に product contract と task contract を追加する。 | (a) `spec.md` に new/update status marker contract がある、(b) `docs/i18n-language-contract.md` が Issue #147 と English marker writer output / legacy read compatibility を説明、(c) `Plans.md` に Phase 77 task があり #121 deferred / #147 non-alias-only 方針が明記される、(d) `git diff --check` PASS | 76.1.4 | cc:done |
| 77.1.2 | `[Issue #147 / Tests First]` `[lane:gate]` `[tdd:required]` Status marker localization の failing tests を先に作る。new/update writer/template/notification が `pm:requested`, `cc:todo`, `cc:wip`, `cc:done`, `pm:approved` の同一 family を使い、legacy `cc:TODO`, `cc:WIP`, `cc:完了`, `pm:依頼中`, `pm:確認済` を読み続けることを固定する。 | (a) parser tests が `cc:todo`, `cc:wip`, `cc:done`, `pm:requested`, `pm:approved` と legacy markers の両方を PASS、(b) writer/template tests が mixed marker family を出さない、(c) Japanese opt-in test が prose を日本語化しても marker writes は English family のままになることを固定、(d) `.claude/state` notification / plans watcher / session summary の visible prose が locale resolver を通る、(e) `bash tests/test-i18n-locale-resolver.sh`, relevant Go tests, `bash scripts/plans-format-check.sh` fixture PASS | 77.1.1 | cc:done |
| 77.1.3 | `[Issue #147 / Implementation]` `[lane:gate]` `[tdd:required]` canonical marker writer と locale-aware status summaries を実装する。対象は Plans writer/update helpers、`go/internal/hookhandler/plans_watcher.go`, `go/internal/session/*`, `scripts/stop-plans-reminder.sh`, `scripts/session-monitor.sh`, `scripts/plans-format-check.sh`, sprint-contract status parsing。 | (a) new Plans rows and marker updates emit English aliases, (b) legacy Plans files are not rewritten during read/sync/dry-run, (c) update commands accept both English and Japanese marker inputs, (d) notification prose follows locale while status marker writes stay English family, (e) Issue #147 closeout comment can truthfully say both Plans marker and notification pain are addressed | 77.1.2 | cc:done |
| 77.1.4 | `[Issue #146 / Guardrail English Reasons]` `[lane:gate]` `[tdd:required]` Guardrail / runtime reason strings の English default 化を完了する。Go guardrail の deny/ask/warn reason と shell hook outputs が locale resolver に従い、`CLAUDE_CODE_HARNESS_LANG=ja` では日本語を維持する。 | (a) default env/config で R01/R02/R03/R04/R05 など user-facing deny/ask reason が English、(b) `CLAUDE_CODE_HARNESS_LANG=ja` で既存相当の日本語、(c) hook JSON shape と Decision は変わらない、(d) `cd go && go test ./internal/guardrail ./internal/hookhandler ./internal/session` PASS、(e) `bash tests/test-i18n-locale-resolver.sh` と `bash tests/test-i18n-japanese-ux-regression.sh` PASS | 77.1.1 | cc:done |
| 77.1.5 | `[Issue #140 / Verify and Close Scope]` `[lane:gate]` `[tdd:skip:verification]` R02/R03 protected-path opt-out issue を現行 Phase 71 実装に照らして再検証する。R03 `.env` ask-list break-glass は満たしているが、R02 opt-out / disabled_rules は安全上 Reject した範囲であることを evidence 付きで整理し、必要なら issue comment / close を行う。 | (a) `harness.toml` の `protectedPathAskList` と `go/internal/guardrail/protected_path_policy.go` の挙動を確認、(b) R03 `.env` ask / hard-deny paths / R02 deny の tests PASS、(c) issue comment draft が「満たした範囲」と「意図的にやらない範囲」を分ける、(d) close する場合は user-facing workaround と safety reason を含む | 77.1.1 | cc:done |
| 77.1.6 | `[Issue #131 / Verify and Close]` `[lane:gate]` `[tdd:skip:verification]` codex-loop orphan active job issue を現行実装で再検証する。`runner_lost_job_running`, stop cancellation, cancel failure state, runner exit trap の tests を実行し、再現が解消しているなら closeout comment を作る。 | (a) `bash tests/test-codex-loop-cli.sh` の runner-lost / cancellation 関連 cases PASS、(b) `scripts/codex-loop.sh` が dead runner + active job を `state_stale` だけで潰さない、(c) stop が active companion job を cancel or failure state に残す、(d) issue closeout comment に test command と behavior summary がある | 77.1.1 | cc:done |
| 77.1.7 | `[PR #136 / #137 Dependency Merge]` `[lane:release]` `[tdd:skip:dependency-bump]` Dependabot PR #136 と #137 を checks rerun 後に個別 merge する。どちらも GitHub Actions major upgrade なので、merge 後 main の validate-plugin / scorecard / smoke-install / release-preflight 境界を確認する。 | (a) #136/#137 の current checks が rerun または最新 commit で success、(b) labels missing (`ci`, `dependencies`) は別 maintenance として記録、(c) each PR is merged one at a time, (d) main CI success after merge, (e) no tag/release action is triggered unless explicitly requested。Evidence: #136 merged as `9fd2071b9c40cdd3a4ba6b4e427af8fd89ee302e`; #137 merged as `5f33130b3e6bd95de10e97bb0dfdb1faa75bfda9`; main `validate-plugin` runs `26377642567` / `26377646959` success; main `scorecard` runs `26377642555` / `26377646960` success; no tag/release action was triggered. | 77.1.1 | cc:done |
| 77.1.8 | `[Issue #149 / Listing Decision]` `[lane:fast]` `[tdd:skip:docs-only]` awesome-codex-plugins 掲載提案を判断する。出すなら external README PR のみ、出さないなら issue に理由付きでコメントする。Harness 側 README に premature support claim を追加しない。 | (a) awesome-codex-plugins の CONTRIBUTING format を確認、(b) listing text が current Codex support tier を超えない、(c) external PR or issue comment draft がある、(d) Harness repo に不要な docs churn を入れない。Evidence: target main `68f58ceb19873302b0e771040d47050fc0e5f638`; `CONTRIBUTING.md` requires mirrored bundle + `.codex-plugin/plugin.json` + `interface.composerIcon`; existing target README has `dadwadw233/claude-code-harness` same-name entry; this repo has no `interface.composerIcon` or dedicated icon asset; decision/comment draft recorded in `docs/research/phase-77-issue-closeout.md`. | 77.1.7 | cc:done |
| 77.1.9 | `[Review / Closeout]` `[lane:gate]` Phase 77 の review closeout。#147 / #146 の user pain が実際に解消したか、#140/#131 の close 判断が過大 claim でないか、#136/#137 merge 後 CI が green かをまとめて確認する。 | (a) `harness-review` or `scripts/codex-companion.sh review --base "${TASK_BASE_REF}"` APPROVE、(b) `bash tests/test-i18n-locale-resolver.sh`, `bash tests/test-i18n-japanese-ux-regression.sh`, relevant Go tests, `bash tests/test-codex-loop-cli.sh`, `bash tests/validate-plugin.sh` PASS、(c) open issues/PRs の残件と public close boundary を明記、(d) CHANGELOG に user-facing fixes を記録、(e) release が必要なら separate `harness-release` gate に渡す。Evidence: companion review failed due Codex auth refresh; `harness-review` manual-pass fallback APPROVE recorded in `docs/research/phase-77-review-closeout.md`; tests passed (`test-codex-loop-cli.sh` 42/42, `validate-plugin.sh` 95/95); public issue close/comment moved to 77.1.10 because #147/#131 fixes must land on main first. | 77.1.2, 77.1.3, 77.1.4, 77.1.5, 77.1.6, 77.1.7, 77.1.8 | cc:done |
| 77.1.10 | `[PR / Public Issue Closeout]` `[lane:release]` `[tdd:skip:external-closeout]` Phase 77 branch を PR 化し、main merge 後に public issue close/comment を実行する。#147 と #131 はこの branch の fix が main に入ってから close、#140/#146 は evidence comment 付き close、#149 は listing blocker comment を残して open 維持、#121 は deferred のまま維持する。 | (a) PR body が #147/#146/#140/#131/#149 の boundary と test evidence を含む、(b) branch CI が green、(c) merge 後 main CI が green、(d) #147/#146/#140/#131 に過大 claim のない close/comment を残す、(e) #149 に icon / `interface.composerIcon` / same-name ambiguity の blocker comment を残す、(f) #121 を触らない、(g) release が必要なら別 `harness-release` gate に渡す。Evidence: PR #150 merged as `d015c803`; branch checks passed including CodeRabbit; main `validate-plugin` run `26379521125`, `smoke-install` run `26379521124`, and `scorecard` run `26379521127` succeeded; clean main preflight passed (`git diff --check`, `bash tests/test-plans-status-markers.sh`, `bash tests/validate-plugin.sh` 95/95); #147/#131/#140/#146 closed with boundary comments; #149 commented and left open; #121 untouched; no release was triggered because Phase 77 closeout did not request a new version/tag, so any release remains a separate `harness-release` gate. | 77.1.9 | cc:done |

---

## Phase 83: Persistent execution-backend selection + Cursor implementation backend (local opt-in, candidate) [P1]

Purpose: 「脳みそ Opus 4.8 / 体 composer-2.5」構成を実現する。Lead(Opus) が plan/review/integrate を担い、Worker(body) の実装を `cursor-agent --model composer-2.5-fast` に **ガッツリ委譲**する (Route A、既存 Codex モードと同型)。backend は `claude`(既定)/`codex`/`cursor` から選び、**選好を永続化して毎回フラグ不要**にする。安全性は *使用を制限するのでなく、Codex と同等の containment* で担保する: cursor-agent 自前の `--sandbox enabled` + 専用 `.git` worktree + 通信は allowlist + env-guard + Lead の diff レビュー&cherry-pick。実装作業そのものは通常 Worker と同じ「worktree で書く→Lead がレビュー」ライフサイクルであり、composer の作業量は制限しない。Cursor は `candidate` のまま、consumer 配布はしない (配布前提は Phase 84)。

Decision基盤: [2026-05-29] (1) composer は実装を制限せず**ガッツリ担う** = Claude Worker と同じ worktree→review ライフサイクル。「制限」ではなく「安全装備を着せて全力で使う」。(2) Codex 前例 (実測): `codex-companion.sh task --write` → `--sandbox workspace-write` + `codex-primary-environment-guard.sh`、丸裸の `--force`+sandbox無効では一度も走らせていない。(3) [2026-05-29 止める検証] cursor 自前の `--sandbox enabled` は **workspace 外への書込を止めなかった** (Codex workspace-write 相当ではない)。確実な停止は `--mode ask`/`plan` (読取専用、`--force` でも破れない=実測)。**書込モードの封じ込めは cursor 自前 sandbox に頼らず、CC 外側 sandbox の write 範囲を worktree に限定 + 通信 allowlist で行う** (83.3b で cursor-agent が外側 sandbox 配下で動くか検証)。読取委譲 (`--mode ask`) は ack 不要、書込委譲は 83.3b で外側 sandbox 封じ込めが確認できるまで per-session Risk Gate。worktree(専用`.git`)+Lead review は常時必須。完全 default-on の丸裸 `--force`+sandbox無効のみ不採用 (3視点合意)。

Spec delta: root `spec.md` に「execution backend selection」を追加 — backend ∈ {`claude`(default), `codex`, `cursor`(candidate)}、precedence = per-command flag > `HARNESS_IMPL_BACKEND` env > default `claude`、cursor は opt-in で実装委譲は Codex 同等 containment (自前 `--sandbox enabled` + worktree + allowlist + env-guard)、support tier は不変 (cursor=candidate)。Harness が delta を生成、consumer は承認・修正のみ。

New Session Start: Phase 83.1 から開始。検証済 (83.3a): cursor-agent は書込でき、`--mode ask` は確実な読取専用ストップ、ただし cursor 自前 `--sandbox enabled` は **workspace 外を閉じ込めない**。次の make-or-break は **83.3b** = CC 外側 sandbox (allowlist + write=worktree 限定) で書込モードを封じ込められるか。読取委譲は今すぐ安全 (`--mode ask`)、書込委譲は 83.3b 次第で ack 要否確定。永続化対象は backend 選好のみ。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 83.1 | `[Spec]` 上記 Spec delta を root `spec.md` に反映する。 [tdd:skip:docs-only] | (a) `spec.md` に backend 3値 / precedence (flag > `HARNESS_IMPL_BACKEND` > `claude`) / cursor=candidate・opt-in・per-session write gate を明記、(b) support tier 表は不変 (cursor=`candidate`)、(c) `bash tests/test-support-claim-wording.sh` PASS | - | cc:done [991a16e7] |
| 83.2 | `[Persist]` `scripts/set-impl-backend.sh <claude\|codex\|cursor>` (env.local に `HARNESS_IMPL_BACKEND=` を冪等書込、`scripts/enable-1h-cache.sh` 准拠、`--show`/`--unset` 対応) + `scripts/resolve-impl-backend.sh` (precedence flag > env > default `claude`)。[tdd:required] | (a) 2 script 追加、(b) precedence テスト (flag が env を上書き / env が default を上書き) PASS、(c) cursor 不在環境でも resolve は `cursor` を返す (実行可否は別判定)、(d) `bash tests/test-shell-lint.sh` PASS | - | cc:done [fe06949e] |
| 83.3a | `[Spike — make-or-break]` cursor-agent `--sandbox enabled --force` が throwaway worktree 内で **ファイル書込できるか**を実測する (Codex `workspace-write` 相当か)。Risk Gate: network のため allowlist 追加 or sandbox 無効化が要る。[tdd:skip:spike-verification] | (a) 「write a file foo.txt containing PONG」を `--sandbox enabled --force` で実行し書込可否を記録、(b) 結果を `docs/research/cursor-adapter-candidate.md` に追記、(c) 可否で 83.3 の既定経路 (enabled vs disabled+ack) を確定 | 82.3 | cc:done [66f281b8] |
| 83.3 | `[Wrapper]` `scripts/cursor-companion.sh task [--write] "<prompt>"` (codex-companion 准拠)。model は `bash scripts/model-routing.sh --host cursor --role worker --field model` で解決、read-only 既定、`--write` は `cursor-agent -p --output-format json --model <m> --workspace <worktree>` を実行し、自動実行は **`--force` でなく `~/.cursor/permissions.json` の `Allowlist (with Sandbox)`** に依存 (Cursor 公式が Run Everything を "Never use")。`--force` は明示 opt-in fallback のみ。読取委譲は `--mode ask`。`.result` 抽出だが **exit code 必須チェック** (エラー時 stdout JSON 無し=Phase 82 知見)、専用 `.git` worktree に confine (write 封じ込めは cursor 非依存、83.3b の外側 sandbox 検証に従う)。[tdd:required] | (a) script 追加、(b) exit-code チェックテスト (`jq -r .result` 単独に依存しない)、(c) read-only 既定・`--write` opt-in テスト、(d) env-guard で worktree 外書込を拒否するテスト、(e) 静的 + opt-in network smoke (`HARNESS_CURSOR_AGENT_SMOKE=1`)、(f) `bash tests/test-shell-lint.sh` PASS | 83.2, 83.3a | cc:done [bcadfc52] |
| 83.3b | `[Spike — write containment]` cursor 自前 sandbox は閉じ込めない (83.3a follow-up で実測済) ため、**CC 外側 sandbox で封じ込められるか**を検証する。`~/.claude/settings.json` sandbox を「network=Cursor egress allowlist + filesystem write=worktree のみ」に設定した状態で cursor-agent が (i) 動作し (ii) worktree 外書込を拒否されるかを確認。[tdd:skip:spike-verification] | (a) 外側 sandbox 封じ込め下で cursor-agent が worktree 内に書け、worktree 外書込が deny される (または不可と判明) を記録、(b) 結果を `docs/research/cursor-adapter-candidate.md` に追記、(c) 書込モードが ack 不要化できるか (封じ込め成立) / per-session Risk Gate 据置か を確定 | 83.3a | cc:done [25d9e5f1] |
| 83.4 | `[Governance]` `.claude/rules/cursor-cli-only.md` 新設。**Cursor 公式統制 (cursor.com/docs/agent/security) を採用**: `--force`(Run Everything、公式 "Never use") を避け `~/.cursor/permissions.json` の `Allowlist (with Sandbox)` で許可コマンドのみ自動実行 / 読取委譲は `--mode ask` / `.cursorignore` に `.env`・secrets・`.git` を列挙し読取遮断 (exfil 対策) / 専用 `.git` worktree 隔離 / Lead-only cherry-pick / cursor の書込は project に閉じ込められない&R01-R13 は cursor 側で効かない旨を明示。wrapper-only。[tdd:skip:docs-only] | (a) rule 追加、(b) `CLAUDE.md` に 1 行ポインタ、(c) `.claude/rules/codex-cli-only.md` と非矛盾、(d) `~/.cursor/permissions.json` (`terminalAllowlist`/`mcpAllowlist`) + `.cursorignore` テンプレを記載し、**allowlist は公式に best-effort で security boundary ではない・実境界は worktree+Lead review** と明示、(e) settings.json への `Bash(cursor-agent:*)` deny 手動手順を記載 | - | cc:done [503de15c] |
| 83.5 | `[Routing]` harness-work / breezing に backend 3-way switch を配線。`HARNESS_IMPL_BACKEND` は **role-scoped = implementation(worker) ロールのみ**に効かせる。review / advisor は **brain(Opus, claude host) に固定**し body(composer) に自作レビューさせない (model-routing の cursor `review`→composer-2.5-fast はこの用途では使わず、cursor-backed work の reviewer は Lead(Opus) もしくは `--host claude --role reviewer` を使う)。model は既存 `model-routing.sh --host <backend> --role <role>` で解決 (Phase 83 は routing を再実装せず host 値を渡すだけ)。Worker 入力 JSON に `backend` 追加、cursor は cursor-companion へ委譲。非Claude backend では `self_review` 5件ゲートをスキップし diff レビューを唯一の品質ゲートにする (Codex 前例)。cursor-backed run で loud banner を出す。[tdd:required] 【supersede 2026-06-11】「cursor-backed work の reviewer に composer を使わない」制約（DoD (c) 該当部分を含む）は、spec.md self-review scope 精緻化 [951d0d48] により **fresh-context advisory pre-review に限り**解除。primary verdict=brain 固定は不変。 | (a) `skills/harness-work/SKILL.md` + `skills/breezing/SKILL.md` に backend switch、(b) `agents/worker.md` 入力 schema に `backend`、(c) review/advisor が brain 固定 (cursor backend でも reviewer は composer にしない → 93.0.2 supersede 参照) を明記、(d) 非Claude で self_review gate N/A・diff review 必須を明記、(e) flag > env precedence テスト、(f) `.claude/rules/opus-4-7-prompt-audit.md` 合格 | 83.2, 83.3, 83.4 | cc:done [4f4ddc65] |
| 83.6 | `[Memory]` body(composer) が brain(Opus) と記憶を共有できるよう `.cursor/mcp.json` に harness-mem を配線する。cursor-agent CLI が project `.cursor/mcp.json` を読むか、global `~/.cursor/mcp.json` 依存かを確認し、決定を記録。[tdd:skip:config-shape] | (a) `.cursor/mcp.json` に harness-mem server 定義 (secret は env 参照、値は埋め込まない)、(b) cursor-agent が project mcp を読むか実測し doc 化、(c) `bash tests/test-cursor-adapter-candidate.sh` の mcp 形状 assert PASS、(d) MCP Trust Policy (CLAUDE.md) 準拠。【closeout 2026-05-29】harness-mem は global `~/.cursor/mcp.json` に既存確認 → repo `.cursor/mcp.json` 配線は冗長。runtime-load 検証は CC 再起動後に持ち越し | 83.3 | cc:done [25d9e5f1] |
| 83.7 | `[Cursor governance — verification-gated]` 「CCH を Cursor 側で執行」案の make-or-break を検証: **Cursor のフックが PreToolUse 相当の deny (protected-path 書込ブロック) を実行できるか** (証拠 doc 79行「Hook runtime block parity ... not observed」)。できる→`.cursor/hooks.json` に R01-R13 相当 guardrail を配線し、不透明 subprocess 問題を構造的に解消 (per-session ack も不要化可)。できない→Cursor フックは observe/warn のみと記録し、containment は 83.3 の sandbox+worktree+env-guard に依存と確定。[tdd:skip:capability-verification] | (a) Cursor hook deny 可否を実測し `docs/research/cursor-adapter-candidate.md` に記録、(b) 可なら `.cursor/hooks.json` に guardrail 配線 + テスト、(c) 不可なら fallback containment を doc 明記、(d) いずれも Cursor は `candidate` のまま。【deferred 2026-05-29】Cursor hook deny の live test は CC 再起動 (allowlist 反映) 後に実施。本体実装をブロックしない独立 spike | 83.4 | cc:done [9b20a343] |
| 83.8 | `[Validation]` テスト集約 / CHANGELOG / boundary。[tdd:skip:test-and-docs-aggregation] | (a) backend precedence + cursor-companion 契約テストが `tests/` で PASS、(b) backend routing 静的 assert (`tests/test-cursor-agent-cli-smoke.sh` 拡張 or 新規)、(c) `CHANGELOG.md` [Unreleased] に local opt-in / candidate / no-support-claim 記載、(d) `bash tests/test-support-claim-wording.sh` + `bash tests/validate-plugin.sh` PASS、(e) Cursor は `candidate` のまま | 83.5, 83.6 | cc:done [aff04e80] |
| 83.9 | `[Codex-host wiring]` Codex ホストから駆動した時も backend スイッチが効くよう `skills-codex/harness-work` (+`skills-codex/breezing`) に Execution Backend Selection を配線する。`resolve-impl-backend.sh` で解決し、worker のみ選択 backend（cursor は `cursor-companion.sh`）、review/advisor は brain 固定。Spec skip reason: spec.md「Execution Backend Contract」が host-neutral に宣言済みで新 product contract なし。[tdd:required] | (a) `skills-codex/harness-work/SKILL.md`(+breezing) に backend switch + `resolve-impl-backend.sh` 参照、(b) role-scoped (review/advisor=brain) 明記、(c) `bash scripts/sync-skill-mirrors.sh` 後 `bash scripts/ci/check-consistency.sh` PASS、(d) `tests/test-cursor-backend-codex-wiring.sh`（skills-codex に backend/resolve 参照があるか grep 契約）追加 + `validate-plugin.sh` に組込、(e) `.claude/rules/opus-4-7-prompt-audit.md` 合格 | 83.5 | cc:done [29ba3bb1] |
| 83.10 | `[Onboarding]` `harness-setup` に cursor backend 導入動線を追加する。AI が編集できない箇所（`~/.cursor/permissions.json`・`.cursorignore`・`~/.claude/settings.json` の `*.cursor.sh` allowlist）は**手動手順として印字**し、AI が実行できる `set-impl-backend.sh [--user] cursor` は案内する。Spec skip reason: 既存能力の提示（導入動線）で product contract 変更なし。[tdd:required] | (a) `skills/harness-setup/SKILL.md` に backend 導入セクション（`set-impl-backend.sh` 案内 + 手動3点: permissions.json/.cursorignore/allowlist）、(b) `.claude/rules/cursor-cli-only.md` と `docs/sandbox-allowlist-recipe.md` を参照、(c) AI 編集不可箇所を手動明示、(d) `bash scripts/sync-skill-mirrors.sh` 後 `check-consistency.sh` PASS、(e) 導入動線の grep 契約テスト追加 + `validate-plugin.sh` 組込、(f) audit 合格 | 83.2, 83.4 | cc:done [29ba3bb1] |

## Phase 85: backend topology SSOT + Lead review contract-grep gate [P2]

Purpose: v4.13.0 リリース後の README リファクタリング test-run で観測した 2 つの実運用ギャップを SSOT 化する。(1) 配線誤解: 非 claude backend では Worker 層を介在させず Lead が直接 cursor/codex-companion を呼ぶ（私が Worker を間に挟んで余計な中継を作った）。(2) Lead 1 次レビューが目視 diff だけで契約文字列 (`5動詞ワークフロー` 等の i18n 固定句) の破壊を検知できず、cherry-pick 後に check-consistency で fail → revert 必要だった。両方を skill 正本と shareable rule に反映し、CHANGELOG に「reviewer guidance hardening」として記載する。

Decision基盤: [2026-05-29] ユーザー判断「全部 Yes、リリースまで、(1)(2) 両方」。私（Lead）の test-run でのミスから抽出した再発防止策。spec.md「Execution Backend Contract」「Review Contract」は既存記述で正しく、追加は skill 正本側 (実運用手順) の補強で十分。

Spec skip reason: docs / skill prose only。spec.md・support tier・公開 API に影響なし。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 79.1.1 | `[Contract Freeze]` `[lane:gate]` `[tdd:skip:docs-contract]` Non-trivial planning gate を product contract に追加する。`spec.md` / `docs/plans/spec-ssot.md` / `Plans.md` に、TeamAgent / sub-agent 前提、memory wheel check、product/security/works-in-practice gate、fallback 表記を固定する。 | (a) `spec.md` に Non-trivial planning must be team-validated がある、(b) `docs/plans/spec-ssot.md` に TeamAgent / sub-agent perspectives と wheel check がある、(c) `team_validation_mode` と lightweight / non-trivial mode 境界がある、(d) perspective と agent_type が分離されている、(e) secret read を要求せず Risk Gate にする契約がある、(f) `Plans.md` Phase 79 に Spec delta と検証 task がある、(g) `git diff --check` PASS。Evidence: `git diff --check` PASS; `bash tests/test-spec-ssot-workflow.sh` PASS. | 78.1.6 | cc:done |
| 79.1.2 | `[Skill Update]` `[lane:gate]` `[tdd:required]` `harness-plan` skill の標準品質契約を更新し、単発・軽微ではない planning で TeamAgent / サブエージェント検証、`spec.md` / `Plans.md` 整合、harness-mem / repo memory、product fit、security fit、works-in-practice を必須出力にする。 | (a) `skills/harness-plan/SKILL.md` が Non-trivial planning gate を持つ、(b) `references/planning-quality.md` が Step 5.5 実装プラン検証ゲートを持つ、(c) `references/create.md` が Step 3 で product/security/works 観点を要求する、(d) `team_validation_mode` の values と `unavailable` Required 禁止が明記される、(e) Product / Architecture / Security / QA / Skeptic は perspective であって agent_type ではない、(f) subagent unavailable fallback が明記される、(g) secret read を必須にしない、(h) mirror sync 後も `.agents`, Codex, OpenCode で同じ契約が見える。Evidence: `bash scripts/sync-skill-mirrors.sh` 実行; `bash scripts/sync-skill-mirrors.sh --check` PASS. | 79.1.1 | cc:done |
| 79.1.3 | `[Validation / Closeout]` `[lane:gate]` Phase 79 の回帰防止 test を更新して実行する。`harness-plan` が軽微 task 以外で team-backed validation を省略しないこと、Spec / Plans / memory / product / security / works gates を出力契約として持つことを検出する。 | (a) lightweight fixture は `team_validation_mode: not_required_lightweight` で PASS、(b) non-trivial fixture は Spec / Memory / Product / Security / Works gate 欠落で FAIL、(c) OpenCode / Task unavailable fixture は `manual-pass` で PASS、(d) security fixture が `.env` / secrets read を必須行動にしない、(e) `bash tests/test-harness-plan-quality.sh` PASS、(f) `bash tests/test-spec-ssot-workflow.sh` PASS、(g) `bash scripts/sync-skill-mirrors.sh --check` PASS、(h) `bash tests/validate-plugin.sh` PASS、(i) residual risk があれば final / PR body に残る。Evidence: `bash tests/test-harness-plan-quality.sh` PASS; `bash tests/test-spec-ssot-workflow.sh` PASS; `bash scripts/sync-skill-mirrors.sh --check` PASS; `bash tests/validate-plugin.sh` 96/0/0 PASS; `bash scripts/codex-companion.sh review --base origin/main` no actionable issues. | 79.1.2 | cc:done |

---

| 85.1 | `[Wiring SSOT]` Lead → composer 直接委託の正本表記を強化する。`skills/harness-work/SKILL.md` 「Execution Backend Selection」節 (L78-) と「非 `claude` バックエンドの self_review ゲート」節 (L102-) に「非 claude backend では Worker agent (`claude-code-harness:worker`) を spawn せず、Lead が直接 `cursor-companion.sh` / `codex-companion.sh` を呼ぶ。Worker 層介在は backend=claude のときだけ」を明示。`.claude/rules/cursor-cli-only.md` の「Role scope」節にも同等の 1 行を追記し配信可能化する。`.claude/memory/patterns.md` (ローカル SSOT) には Phase 85 学びを Why 付きで記録する (gitignore 対象なので配布外)。[tdd:skip:docs-only] | (a) `skills/harness-work/SKILL.md` + opencode/codex mirror の該当節に「Worker 介在は backend=claude のみ」明示、(b) `.claude/rules/cursor-cli-only.md` Role scope 節に 1 行追記 (配信可能)、(c) `.claude/memory/patterns.md` に Phase 85 entry (ローカル)、(d) `bash scripts/sync-skill-mirrors.sh` 後 `bash scripts/ci/check-consistency.sh` PASS、(e) audit (vague-word grep) 0 件 | - | cc:done [33814e73] |
| 85.2 | `[Review gate hardening]` Lead 1 次レビューに「目視 diff + contract grep」の二段ゲートを明示する。`skills/harness-work/SKILL.md` のレビューループ節に「cherry-pick 前必須コマンド」テーブルを追加 (`bash tests/test-support-claim-wording.sh` / `bash scripts/ci/check-consistency.sh` / `bash tests/validate-plugin.sh`)。「docs / locale / capability-matrix 等の固定文字列契約は目視 diff では検知できない」根拠と Phase 85 test-run の reference を Why として併記。[tdd:skip:docs-only] | (a) `skills/harness-work/SKILL.md` のレビューループ節 (Review Policy 周辺) に gate テーブル追加、(b) opencode/codex mirror 同期、(c) `bash scripts/ci/check-consistency.sh` PASS、(d) 目視 diff のみで composer 出力を APPROVE しないと明示、(e) audit 合格 | - | cc:done [33814e73] |
| 85.3 | `[Closeout]` 改善を CHANGELOG `[Unreleased]` に追記し、`/harness-release` 可能な状態にする。Bump は **patch** (`4.13.0` → `4.13.1`、内部 hygiene + skill prose のみ・API/機能不変・後方互換)。[tdd:skip:docs-and-validation] | (a) `CHANGELOG.md` `[Unreleased]` に "Lead → composer 直接委託 (Worker 介在は backend=claude のみ)" と "Lead review に contract-grep ゲート追加" を Documentation/Changed として記載、(b) `bash tests/validate-plugin.sh` PASS、(c) `bash scripts/ci/check-consistency.sh` PASS、(d) mirror 同期済み、(e) `bash tests/test-support-claim-wording.sh` PASS | 85.1, 85.2 | cc:done [e058e3ca] |

---

## Phase 86: Harness Duplication Cleanup [P1]

Purpose: Cursor / Codex / Claude Code で Harness が重複表示される問題を、個人環境の Clean/Compatibility 診断と host-specific 配布 package 分離の2層で解消する。Desktop の Claude/Codex 互換読み込み ON 時は重複再発し得るため、Harness は dry-run 診断とユーザー確認後の archive/disable のみ行う。(本 Phase は元 Phase 82 として作業されたが、Phase 80-84 archive 後の番号衝突回避のため Phase 86 にリナンバーされた。)

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 86.1 | `[Contract]` spec / Plans / distribution scope に host distribution と Clean/Compatibility profile を固定する。 | (a) `spec.md` Host Distribution + Clean/Compatibility 節、(b) Phase 86 行、(c) `docs/distribution-scope.md` 更新 | 81.6 | cc:完了 |
| 86.2 | `[Local Env]` dry-run 診断 docs + helper を追加する。 | (a) `docs/local-harness-environment-cleanup.md`, (b) `scripts/diagnose-harness-skill-duplication.sh` default dry-run, (c) `--apply` なし or confirmation gate | 86.1 | cc:完了 |
| 86.3 | `[Archive Gate]` Claude archive から `.cursor-plugin/` 混入を防ぐ。 | (a) `.gitattributes`, (b) `tests/test-distribution-archive.sh`, (c) distribution-scope 同期 | 86.1 | cc:完了 |
| 86.4 | `[Dist Builder]` host-specific dist 生成 script を追加する。 | (a) `scripts/build-host-plugin-dist.sh`, (b) manifest path 正規化, (c) host 別 displayName | 86.3 | cc:完了 |
| 86.5 | `[Dist Tests]` 混入防止 test + adapter smoke を generated package ベースへ拡張する。 | (a) `tests/test-host-plugin-dist.sh`, (b) codex/cursor adapter test 更新 | 86.4 | cc:完了 |
| 86.6 | `[Validation]` mirror / validate / release-preflight dry-run を PASS する。 | (a) listed validation commands PASS, (b) `CHANGELOG.md` [Unreleased] | 86.2, 86.3, 86.4, 86.5 | cc:完了 |

---

## Phase 87: Cursor Internal-Compatible Promotion [P1]

Purpose: Cursor adapter を `candidate` から `internal-compatible` へ昇格する。`setup-cursor.sh` インストール経路、host-specific dist 正規化、観測済み Desktop skill-loading 証拠、tier 表記・テスト・preflight gate を Codex CLI / OpenCode と同水準で整える。`supported` 公開 claim は CI-gated workflow smoke まで行わない。(本 Phase は元 Phase 83 として作業されたが、Phase 80-84 archive 後の番号衝突回避のため Phase 87 にリナンバーされた。)

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 87.1 | `[tdd:required]` RED: setup-cursor + tier assertion を先に fail させる。 | (a) `test-cursor-adapter-candidate.sh` setup assert 追加で script 未実装 RED、(b) `test-tool-capability-matrix.sh` Cursor=`internal-compatible` で doc 未更新 RED | 86.6 | cc:完了 |
| 87.2 | `[tdd:required]` GREEN: `scripts/setup-cursor.sh` 実装。 | (a) `--check` dry-run + real copy install、(b) symlink 不可 + `user-invocable` 正規化 dist、(c) `test-cursor-adapter-candidate.sh` PASS | 87.1 | cc:完了 |
| 87.3 | `[tdd:skip:docs-only]` GREEN: tier doc + runtime 証拠。 | (a) `spec.md` Cursor=`internal-compatible`、(b) `docs/tool-capability-matrix.md` 更新、(c) `cursor-adapter-candidate.md` 2026-05-29 証拠、(d) `test-tool-capability-matrix.sh` PASS | 87.1 | cc:完了 |
| 87.4 | `[tdd:skip:config-wiring]` Gate / CHANGELOG。 | (a) `release-preflight.sh` setup-cursor gate、(b) `CHANGELOG.md` [Unreleased]、(c) validation commands PASS | 87.2, 87.3 | cc:完了 |

---

## Phase 88: Backend resolution hardening — resolver 必須化 + composer 自然言語マッピング + fallback 警告 [P1]

Purpose: 2026-05-30、別 repo (CX-Harness) で `/breezing` を「composer モードで」起動した際、host Claude (Lead) が backend を `env HARNESS_IMPL_BACKEND` 直読みで判定し、unset を見て「composer バックエンドが無い」と誤判定して claude native team mode にフォールバックした。実際は user file (`~/.config/claude-harness/impl-backend.env`) に `cursor` が設定済みで、`resolve-impl-backend.sh` を呼べば precedence で `cursor` を返した（sandbox / binary / 認証 / allowlist は全て正常）。真因は (1) host が resolver を経由せず env 1 段だけ見た、(2) 「composer」という自然言語が cursor backend に紐づいていない、(3) claude フォールバック時に「resolver 未使用」を host に気づかせる警告がない、の 3 点。本 Phase で skill 正本側の backend 判定契約を強化し再発を防ぐ。

Spec delta: `spec.md` の「Execution Backend Contract」(L199) に「backend 解決は `scripts/resolve-impl-backend.sh` を唯一の入口とし、skill / host は `HARNESS_IMPL_BACKEND` env を直読みして判定してはならない（env 単独では project env.local / user file の precedence を取りこぼす）」を 1 行追加する（88.1）。88.2 / 88.3 は skill prose / trigger 語の補強で spec 影響なし（Spec skip reason: skill prose / natural-language trigger only）。

team_validation_mode: `manual-pass`（単独で Product / Architecture / Skeptic 観点を評価。Product: 「composer と言えば cursor で動く」はユーザー可視の振る舞いなので contract 化が必要。Architecture: resolver は既に precedence を正しく実装済みで、欠陥は呼び出し側の judgment。Skeptic: env 直読みを「便利だから」と残すと同じ罠が再発する → 全 backend 判定経路で resolver 必須化が妥当）。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 88.1 | `[Resolver 必須化]` `[tdd:skip:skill-prose-and-spec-delta]` `skills/breezing/SKILL.md` と `skills/harness-work/SKILL.md` の backend 判定記述から「env `HARNESS_IMPL_BACKEND` を直読みして判定する」経路を排除し、「必ず `bash "${HARNESS_PLUGIN_ROOT}/scripts/resolve-impl-backend.sh"` の出力を backend として使う（env unset でも project env.local / user file を precedence で拾う）」に統一する。`spec.md` Execution Backend Contract に resolver-only 入口の 1 行を追加。 | (a) `skills/breezing/SKILL.md` の「env `HARNESS_IMPL_BACKEND=cursor` の時に有効」等の env 直読み判定文が resolver 経由に書き換わる、(b) `grep -nE "env .*HARNESS_IMPL_BACKEND.* の時" skills/breezing/SKILL.md` が backend 判定文脈で 0 件（説明的言及は可、判定ロジックとしては不可）、(c) `spec.md` に「resolve-impl-backend.sh を唯一の入口、env 直読み禁止」の 1 行、(d) mirror (`codex/.codex/skills/`, `opencode/skills/`) 同期、(e) `bash scripts/ci/check-consistency.sh` PASS | - | cc:done [de9b1a43] |
| 88.2 | `[composer 自然言語マッピング]` `[tdd:skip:trigger-prose-only]` `skills/breezing/SKILL.md` と `skills/harness-work/SKILL.md` に「composer / コンポーザー / Composer で / composer 2.5 / composer モード」を cursor backend のトリガー語として正式定義する。現状は「違反例」中の言及のみで、正規トリガーとして未定義。host が自然言語「composer モードで」を受けたら `--cursor` 相当（resolve-impl-backend 経由で cursor へ）に解釈する規約を明示。 | (a) `skills/breezing/SKILL.md` に composer トリガー語マッピング表 or 1 段落（"composer/コンポーザー → cursor backend, model composer-2.5-fast"）、(b) `grep -niE "composer.*cursor backend|コンポーザー.*cursor" skills/breezing/SKILL.md` が 1 件以上（trigger 定義として）、(c) frontmatter `trigger` または description にも composer を含める検討（任意）、(d) mirror 同期、(e) `bash scripts/ci/check-consistency.sh` PASS。Evidence: `bash tests/test-composer-backend-trigger.sh` PASS; `bash tests/test-cursor-backend-codex-wiring.sh` PASS; `bash scripts/sync-skill-mirrors.sh --check` PASS; `bash scripts/ci/check-consistency.sh` PASS. | - | cc:done |
| 88.3 | `[Fallback 警告]` `[tdd:skip:skill-prose-only]` backend が `claude` にフォールバックする（resolver が claude を返す or resolver 未実行）時、host が最初の 1 行に「⚠️ backend=claude (resolver 経由 / 未経由) — composer/cursor を使う場合は `--cursor` or `resolve-impl-backend.sh` を確認」相当の警告を出す契約を `skills/breezing/SKILL.md` / `skills/harness-work/SKILL.md` に明示。env unset で claude に落ちる罠を可視化する。 | (a) backend=claude 確定時の 1 行警告フォーマットが skill に明示される、(b) 警告には「resolver を経由したか」と「composer/cursor を使うなら何を確認するか」が含まれる、(c) Narration Rules（1 行 echo）と矛盾しない（echo の backend 部分が警告を兼ねる形）、(d) mirror 同期、(e) `bash scripts/ci/check-consistency.sh` PASS | 88.1 | cc:done [6a05a7cb] |
| 88.4 | `[Default ON surface]` `[tdd:required]` Codex からも Cursor に実装委譲できるようにしつつ、配布 plugin の fallback は opt-in 互換の `claude` のまま維持する。現環境だけ `HARNESS_IMPL_BACKEND=cursor` / user-scope default で Cursor default ON にし、`harness-review` は cursor second-opinion を自動併走、Cursor direct surface は `cursor:setup` / `cursor:do` / `cursor:ask` / `cursor:review` / `cursor:rescue` として Codex 配布面でも Claude と同じ入口で露出する。primary review は Opus/brain 固定で、cursor は advisory のまま。本番リリース前のため旧 `cursor-do` / `cursor-ask` aliases は提供しない。 | (a) `spec.md` に default ON が Cursor-capable surface 全体へ効くこと、review は advisory second-opinion で primary verdict は Opus/brain 固定、配布 fallback は `claude` と明記、(b) `skills-codex/breezing` / `skills-codex/harness-work` が `--default cursor` を使わず resolver 経由で `--backend cursor` / user default を拾う、(c) `skills/harness-review/SKILL.md` + `references/cursor-review.md` が `resolve-impl-backend.sh --role reviewer` 経由で default ON を判定、(d) direct Cursor skills は validator-safe frontmatter id (`cursor-do` 等) を使い、description/prose は `cursor:<verb>` namespace を使う、(e) `.codex-plugin/plugin.json` defaultPrompt に registered skill id `$cursor-do` / `$cursor-ask` / `$cursor-review` / `$cursor-setup` / `$cursor-rescue` を追加、(f) mirror 同期、(g) `bash tests/test-cursor-backend-codex-wiring.sh` PASS | 88.1 | cc:完了 |
| 88.5 | `[Cursor namespace command names]` `[tdd:required]` pre-release なので後方互換 alias なしで、Cursor direct surface を `cursor:<verb>` namespace に普通に更新する。対象は direct delegate (`do`/`ask`)、setup/review/rescue wrapper、Codex plugin defaultPrompt、spec / Plans / static test。OpenCode mirror と Codex skill frontmatter は platform/validator 制約上 kebab-case directory/name に正規化するが、source/Codex/Claude の user-facing command は `cursor:<verb>` を正とする。 | (a) `skills/cursor-do` / `skills/cursor-ask` の frontmatter `name` は validator-safe な `cursor-do` / `cursor-ask`、本文は `cursor:do` / `cursor:ask` を正とし旧 alias を説明しない、(b) `skills/cursor-setup` / `skills/cursor-review` / `skills/cursor-rescue` が追加される、(c) Codex mirror に 5 つの Cursor direct skills が存在し frontmatter name は kebab-case packaging id、(d) `.codex-plugin/plugin.json` defaultPrompt は name-based invocation 用に registered `$cursor-<verb>` skill id を使う、(e) `spec.md` とこの Plans 行が旧 alias 不要を明記、(f) `bash tests/test-cursor-backend-codex-wiring.sh` / `bash scripts/sync-skill-mirrors.sh --check` / `bash tests/validate-plugin.sh` PASS | 88.4 | cc:完了 |

---

## Phase 89: Session Coordination Auto-Wiring (local lease + register + broadcast revival) [P1]

Purpose: 同一PCで同じプロジェクトを複数セッション(手動の複数窓 + breezing の並列 worktree Worker)で動かすとき、お互いの作業が見えずファイル競合が起き、ユーザーが手動でコピペして状況を仲介している。協調部品(broadcast/inbox/register)は実装済みだが、(1) register は hook 未配線で `active.json` が 4/9 停止、(2) broadcast は発火条件が `src/api`,`types/`,`schema.prisma` 等に限定され普段の編集で発火せず 2026-02 以降死骸化、(3) inbox は broadcast 枯渇で連鎖死、という状態。planning gate の subagent レビューで判明した致命的欠陥 (worktree ごとに `.claude/` 分断 / soft warning は行動を変えない / 既存 inbox-check の prompt injection 経路) を全て織り込んだ修正設計で、ローカル完結のファイル単位 lease による協調を配線する。Phase 85 番号は main の backend topology SSOT で先に使われていたため、本 rebase で Phase 89 として番号を取り直す (前セッションでの計画は Phase 85 として作業されていた)。

Forensics evidence (前セッションで実機検証済、`docs/design/session-coordination-snapshot-2026-05-29.md` は `.gitignore` 配下のローカル参照): broadcast 死因 = `go/internal/hookhandler/session_auto_broadcast.go:14-26` の `autoBroadcastPatterns` が普段の編集で発火しない (`.go`/`.md`/`.sh` のいずれもマッチしない、限定パターンは `src/api/`,`src/types/`,`api/`,`types/`,`schema.prisma`,`openapi`,`swagger`,`.graphql` のみ)、register 完全未配線 = `grep -c 'session-register' .claude-plugin/hooks.json hooks/hooks.json` が両方 0、worktree 分断 = `.claude/worktrees/*/` の 6 worktree 全てが独立した `.claude/sessions/` を持つ、`git --git-common-dir` 解 = 健全な worktree では同一の `.git` を返すため lease store を git-common-dir 基準に固定すれば全 worktree で共有可能 (stale worktree `flamboyant-shannon` のみ旧 Desktop パス参照で `fatal: not a git repository` → fail-open で skip)。

確定設計(ユーザー承認 + 3 subagent レビュー反映): lease store は `git --git-common-dir` 基準の単一共有箇所・キーは repo-relative path の sha256 (worktree 跨ぎで効かせる)。取得は `O_CREAT|O_EXCL` で atomic。stale は (session_id 生存 AND TTL) 複合、pid は補助。競合制御は PostToolUse + `continueOnBlock: true` の diagnostic feedback (deny → "session X が編集中。完了を待つか別ファイルへ" → リトライ) で行動を変える。guard rail ではないので `continueOnBlock: true`、fail-open 維持。cross-session content は構造化信頼フィールドのみ注入 + disclaimer wrap (injection 対策、D51)。tri-state 4状態テスト、not-configured は無警告。

Spec delta: `spec.md` に `## Session Coordination Contract` を新規追加 (協調はローカル完結 / lease は git-common-dir 基準 + repo-relative hash / 競合は continueOnBlock feedback で guard rail でない fail-open / cross-session content は data として注入 / tri-state / broadcast 発火条件はテストで担保)。

team_validation_mode: `subagent`。Product/Architecture/Skeptic/Security/QA 視点を 3 subagent で分担。Architecture/Skeptic が worktree 分断(Critical)と soft warning 行動非変更(Critical)を、Security/QA が prompt injection(Critical)と path traversal(Critical)と tri-state テストを指摘。既存部品精読 subagent が broadcast 死因(発火条件過狭)と register 未配線を確定。当初設計は Reject、修正設計を Required に格上げ。

New Session Start: 本起票冒頭の Forensics evidence で 89.1.1 は完了扱い。89.1.2 で既存 inbox-check の injection を先に塞ぎ(既存バグ)、89.1.3 register 配線、89.1.4 lease core、89.1.5 競合 feedback、89.1.6 broadcast 復活、89.1.7 で tri-state/並行/worktree テストと validation closeout。release/tag は含めない。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 89.1.1 | `[Research / Forensics]` `[lane:gate]` `[tdd:skip:research-doc]` 既存部品精読 + broadcast/inbox 死因剖検 + worktree 分断実機確認 (前セッションで実機検証済、要点は本起票冒頭の Forensics evidence に inline)。 | (a) 本起票冒頭の forensics evidence に broadcast 死因 (file:line) / register 未配線 (grep 値) / worktree 分断 (実機 worktree 数) / git-common-dir 解 / stale worktree 注意点 が全て記録される、(b) `git diff --check` PASS | - | cc:完了 [forensics-inline] |
| 89.1.2 | `[Security Hardening]` `[lane:gate]` `[tdd:required]` 既存 inbox-check の prompt injection を塞ぐ (D51 / 既存バグ先行fix)。 | (a) `inbox_check.go` の `additionalContext` 注入が構造化信頼フィールド (sanitized path / short session id / age-seconds) のみになる、(b) 他セッション free-text を verbatim 注入しない、(c) 既存 disclaimer (`userprompt-inject-policy.sh` の `memory_resume_intro` 同等) で wrap、(d) 制御文字除去 + byte cap、(e) `TestInboxInject_NeutralizesUntrustedContent` で攻撃文字列 (ANSI escape / NUL / prompt injection / flood) が命令として通らないことを検証、(f) `bash tests/validate-plugin.sh` PASS | 89.1.1 | cc:完了 [0b376e3f] |
| 89.1.3 | `[Register Auto-Wiring]` `[lane:gate]` `[tdd:required]` register を Go 化し SessionStart 配線 + Stop 解放。 | (a) `go/internal/hookhandler/session_register.go` に `HandleSessionRegister` / `HandleSessionUnregister` 実装、`main.go` runHook switch に `session-register` / `session-unregister` case 追加、(b) `hooks/hooks.json` + `.claude-plugin/hooks.json` の SessionStart(`startup\|resume`) と Stop に dual-sync で配線、`type:"command"`、(c) `active.json` に session_id+pid+last_seen を記名、Stop で解除、stale 24h 自動 prune、(d) `TestRegisterHealth_NotConfigured`/`_Corrupted`/`_Healthy` + Unregister + StaleCleanup の 6 テスト PASS、(e) `TestMonitorHandler_RegisterNotConfigured` で not-configured 無警告を検証、(f) `bash tests/validate-plugin.sh` PASS | 89.1.1 | cc:完了 [5c00f350] |
| 89.1.4 | `[Local Lease Core]` `[lane:gate]` `[tdd:required]` git-common-dir 基準のファイル単位 lease 機構。 | (a) `go/internal/hookhandler/` に lease 取得/解放/競合チェック実装、(b) store は `git rev-parse --git-common-dir` 基準の単一箇所、キーは repo-relative path の sha256、(c) 取得は `O_CREAT\|O_EXCL` で atomic、(d) stale は (session_id 生存リスト AND TTL) 複合判定で pid は補助、(e) path containment(`isUnderProjectRoot` + `EvalSymlinks`)で traversal 防止、(f) `TestLeaseAcquire_ConcurrentSameFile` を `go test -race` で PASS (同時取得は1つだけ成功)、`TestLeaseKey_Traversal`/`TestLeaseReclaim_StaleByTTL`/`TestLease_Worktree`/`TestLeaseHealth_NotConfigured`/`_HolderUnreachable`/`_Corrupted`/`_Healthy`、(g) `bash tests/validate-plugin.sh` PASS | 89.1.1 | cc:完了 [398713d5] |
| 89.1.5 | `[Conflict Feedback]` `[lane:gate]` `[tdd:required]` PostToolUse + continueOnBlock の行動変更 feedback。 | (a) PostToolUse(`Write\|Edit`) hook で他セッション lease 保有時に deny + `continueOnBlock:true` + 「session X が `<file>` を編集中。完了を待つか別ファイルへ」feedback、(b) lease 機構不達時は fail-open(allow、警告なし)、(c) `continueOnBlock:true` が diagnostic feedback として `hooks-2.1.139-plus.md` §3 に整合(guard rail でない)、(d) PreToolUse(`Write\|Edit`) で自セッションの lease 取得、(e) `TestConflictFeedback_DenyOnHeld`/`_FailOpenOnUnavailable`、(f) dual hooks.json sync + `bash tests/validate-plugin.sh` PASS | 89.1.3, 89.1.4 | cc:完了 [5f20d80f] |
| 89.1.6 | `[Broadcast Revival]` `[lane:gate]` `[tdd:required]` broadcast 発火条件を死因回避で再設計。 | (a) `session_auto_broadcast.go` の発火条件を普段の編集(`.go`/`.md`/`.sh` 等)でも発火するよう見直す or 明示 broadcast 経路を追加、(b) 旧 Desktop パス汚染の `broadcast.md`/`active.json` の migration、(c) `TestAutoBroadcast_FiresOnNormalEdit` で通常編集が broadcast されることを検証(2026-02 死骸の死因回避の証明)、(d) `TestInboxCheck_InjectsAfterRevival` で inbox 連鎖死の解消を検証、(e) `bash tests/validate-plugin.sh` PASS | 89.1.2 | cc:完了 [22fe15d9] |
| 89.1.7 | `[Validation / Review Closeout]` `[lane:release]` `[tdd:skip:test-aggregation]` Phase 89 全体の validation と review。release/tag は作らない。 | (a) `git diff --check`, `go test -race ./...`, `bash tests/validate-plugin.sh`, `bash scripts/sync-skill-mirrors.sh --check` 全 PASS、(b) tri-state 4状態 + 並行 + worktree + injection の全テスト green、(c) dual hooks.json parity gate PASS、(d) `bash scripts/codex-companion.sh review --base "${TASK_BASE_REF}"` APPROVE or auth failure + manual-pass、(e) `docs/CLAUDE-feature-table.md` と `CHANGELOG.md` `[Unreleased]` に追記、(f) PR body に設計判断/レビュー指摘の採否/residual risk(harness-mem 非対応=同一PC限定、別 clone 非共有)を記載、(g) no tag/release | 89.1.5, 89.1.6 | cc:完了 [ab91f96e] |

---

## Phase 90: Orchestration Visibility Scorecard (session-scoped) [P1]

Purpose: backend 委譲は実行時に不可視で、ユーザーは「本当に Codex / Cursor を使ったのか、それとも全部 Claude に落ちたのか」を確認できない。現状 agent-trace にも telemetry にも backend 次元の記録が一切なく（`scripts/generate-agent-telemetry.js` は role 次元のみ集計）、companion script も委譲痕跡を残さない。本 Phase は backend mix を「観測 → 記録（常時）→ 見せる（on-demand）」で配線する。**記録と表示を分離**するのが要：委譲は常に session ledger に記録し、全タスク完了時（と保険でセッション終了時）に lifetime accumulator へ冪等に roll up する。表示は毎タスクでは出さず、全タスク完了時にターミナルへ 1 回だけサマリを出す＋HTML は見たい時だけ。累計こそが「人に見せる主役の数字」。

確定設計（ユーザー判断反映、前回の session-only から更新）: (1) 記録 = 常時 ON。session ledger（その回の委譲）+ lifetime accumulator（`.claude/state/orchestration-totals.json`、プロジェクト単位の backend 別累計）の 2 スコープを持つ。roll up は **session_id 単位で冪等**（完了時/終了時の 2 経路で二重カウント防止）、(2) 表示 = 毎タスクでは出さない。**全タスク完了時にターミナルサマリを 1 回だけ自動表示**（その前に roll up を走らせ当該セッションを lifetime に反映）。HTML スコアカードは on-demand のみ（自動では開かない）。両方とも session mix + lifetime totals を表示、(3) 観測 = companion が正本（`codex-companion.sh` / `cursor-companion.sh` が委譲時に ledger へ直接追記）。Claude 側は companion を通らないため scorecard 生成時に既存 `agent-trace.jsonl`（role=worker）から導出してマージ。グローバル（全プロジェクト横断）累計は cross-project privacy 配慮で今回スコープ外、将来 opt-in。

Spec delta: `spec.md` `## Orchestration Visibility Contract` を更新済み（記録/表示の分離 / session ledger + lifetime accumulator（project-scoped, session_id 冪等 roll up）/ 毎タスク自動表示はしない / 全タスク完了時にターミナルサマリ 1 回 + HTML は on-demand / companion-written ledger は非機密フィールドのみ + `counts` フラグで status polling 除外 / tri-state `used`/`available`/`not-configured` で not-configured 無警告 / multi-backend utilization ratio + diversity / render-html redaction defense-in-depth / 欠損時は "no delegations observed" に degrade）。

team_validation_mode: `subagent`（観測点・既存可視化基盤・state/hook 記録面の 3 観点を Explore subagent で並列調査）+ Product/Architecture/Security/QA/Skeptic を manual-pass で inline 適用。Skeptic: 「自慢 UI は虚栄では」→ 反証: 本質価値は「claude へ silent fallback したかの誠実性チェック」で、自慢はその副産物。Security: ledger に prompt 本文・秘密を載せない（フィールド固定）+ render-html Layer2/3 redaction を防御多重化。QA: active-watching-test-policy の 3 状態（not-configured ≠ 壊れ）を必須テスト化。Architecture: 観測点は companion 正本 + Claude は agent-trace 導出（新規観測点を増やさない）。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 90.1.1 | `[Ledger schema + companion emit]` `[tdd:required]` `orchestration-ledger.v1` schema を定義し、`scripts/codex-companion.sh` / `scripts/cursor-companion.sh` が委譲ごとに `.claude/state/orchestration-ledger.jsonl` へ 1 行追記する。フィールドは `ts, backend, subcommand, write, exit_code, duration_ms, session_id, counts` のみ。prompt 本文・ファイル内容・秘密は載せない。`counts` は `task`/`review`/`adversarial-review` のみ true、`status`/`setup`/`result`/`cancel` は false。append は失敗しても委譲本体を壊さない（fail-open）。 | (a) schema JSON が `skills/harness-progress/schemas/` 等に追加され 8 フィールド固定、(b) `grep -nE 'orchestration-ledger' scripts/codex-companion.sh scripts/cursor-companion.sh` が両方 1 件以上、(c) ledger 行に prompt/秘密が混入しないことをテストで検証（`task --write "secret xyz"` で ledger に `xyz` が出ない）、(d) `counts` フラグが subcommand で正しく切替、(e) ledger 書込失敗時に exit code が委譲結果のまま（fail-open）、(f) `bash tests/test-orchestration-ledger.sh` PASS | - | cc:完了 [a584dc19] |
| 90.1.2 | `[Lifetime accumulator + idempotent rollup]` `[tdd:required]` `.claude/state/orchestration-totals.json`（プロジェクト単位、backend 別累計）を定義し、全タスク完了時（`task-completed.sh` の all-done 経路、サマリ生成の前）と保険のセッション終了時（SessionEnd）に session ledger を集計して累計へ無言で roll up する。ライブの Go hook（shell handler は Go 移植済のため death-wire を回避）に配線し、**この task 自体は表示しない（record-only）**。roll up は `session_id` を記録して**冪等**にし、2 経路で二重に走っても増えないようにする。書込失敗は fail-open（累計が壊れてもセッションを止めない）。 | (a) `orchestration-totals.v1.schema.json` 追加（per-backend cumulative count + 集計済み session_id 集合）、(b) ライブ Go ハンドラ経由で配線: `go/internal/hookhandler/task_completed.go`（all-done 分岐）と `go/internal/session/cleanup.go`（SessionEnd）が共有 `go/internal/orchestration` の `orchestration.Run` を呼び `scripts/orchestration-rollup.sh` を exec（`grep orchestration.Run` が両 Go ファイルにヒット + `go test ./internal/orchestration/` PASS）、(c) 同一 session_id の roll up を 2 回（完了時 + 終了時）流しても累計が増えない（冪等）ことをテスト、(d) ledger 欠損/空でも累計が壊れずスキップ、(e) この task は一切ターミナル出力しない（record-only）、(f) `bash tests/test-orchestration-totals.sh` PASS | 90.1.1 | cc:完了 [b83267f6] |
| 90.1.3 | `[Scorecard aggregator + tri-state]` `[tdd:required]` `scripts/orchestration-scorecard.sh` が **(i) 現 session_id の ledger + agent-trace（= session mix）** と **(ii) `orchestration-totals.json`（= lifetime totals）** の両方をマージし `orchestration-scorecard.v1` JSON を生成する。各 backend に tri-state（`used` count>0 / `available` 設定済み未使用 / `not-configured` companion setup 失敗 or binary 不在）を付与。diversity（実使用 backend 数）と multi-backend utilization ratio（非claude / 全委譲）+ 測定対象の 1 行注記を session と lifetime 双方に含める。 | (a) `orchestration-scorecard.v1.schema.json` 追加（`session` と `lifetime` の 2 ブロック）、(b) session_id フィルタで他セッション ledger を混ぜない / lifetime は累計を読む、(c) tri-state 判定が `resolve-impl-backend.sh` / companion `setup` 結果に基づく、(d) `TestScorecard_NotConfigured` / `_Available` / `_Used` の 3 状態 + claude-only + 3backend + lifetime>session のケースをテスト、(e) ledger/trace/totals 欠損時に "no delegations observed" へ degrade、(f) ratio 注記が「測っているもの」を 1 行で持つ、(g) `bash tests/test-orchestration-scorecard.sh` PASS (claude=host・agent-trace は role/session_id 欠落のため不使用、ratio は「非Claude委譲数 + backends-engaged N/3」に置換) | 90.1.2 | cc:完了 [505b52e4] |
| 90.1.4 | `[HTML scorecard surface]` `[tdd:skip:rendering-snapshot]` `templates/html/orchestration.html.template` を追加し、`scripts/render-html.sh --template orchestration --with-redaction` で `orchestration-scorecard.v1` を単一 HTML にレンダリングする。既存 3 surface と同じ Harness brand palette・inline CSS・JS 不要・standalone。**lifetime totals を主役**に、session mix を従に、backend 別カウントのバッジ/バー、diversity、utilization ratio、tri-state を「自慢できる」見た目で表示。 | (a) template が `templates/html/` に追加され既存 4 template と同じ mustache 規約、(b) lifetime と session の両ブロックが描画され lifetime が視覚的に主役、(c) tri-state が視覚的に区別（used=実数バッジ / available=淡色 / not-configured=「未設定」中立表記）、(d) `--with-redaction` で Layer2/3 を通す、(e) smoke test: fixture JSON → render → 出力 HTML に 3 backend 名・lifetime・ratio が含まれることを grep 検証、(f) standalone でブラウザに開ける（外部参照なし）、(g) `bash tests/test-orchestration-render.sh` PASS (redaction は不要=データ構造的に非機密; html-data 平坦化経由で render) | 90.1.3 | cc:完了 [e0298e95] |
| 90.1.5 | `[Completion summary (once) + on-demand surfaces]` `[tdd:skip:skill-prose-only]` 完了時の自動サマリ（1 回）と on-demand 入口を用意する。**毎タスクでは出さない・HTML は自動で開かない**。(i) 全タスク完了時に `task-completed.sh` の all-done 経路（finalize と同じ箇所、roll up の後）から `orchestration-scorecard.sh --format terminal` を **1 回だけ**呼びターミナルにサマリを出す。(ii) 薄い skill `harness-orchestration`（trigger: 「オーケストレーション活用」「scorecard」「どのバックエンド使った」「自慢」「累計」）が `orchestration-scorecard.sh` → `render-html.sh` で HTML を開く（`--out`/`--no-open` 対応、on-demand のみ）。(iii) `--format terminal` は完了経路 / skill / 明示フラグから呼ばれる。 | (a) `skills/harness-orchestration/SKILL.md` が skill-editing.md 規約準拠（name=dir, description にトリガー語+除外）、(b) `disable-model-invocation` を付けない（read-only/判定 skill = P27 非適用）、(c) `--format terminal` が claude-only/not-configured を「0で壊れてる風」にせず available/未設定で明示、分母 0 でも除算エラーにせず "no delegations observed"、(d) `grep -n orchestration-scorecard scripts/hook-handlers/task-completed.sh` が **all-done 経路にのみヒット**し、単一タスク完了イベントでは出力 0・全タスク完了イベントで 1 回だけ出力することをテスト（`TestCompletionSummary_FiresOnceOnAllDone` / `TestCompletionSummary_SilentPerTask`）、(e) HTML は task-completed から開かない（render-html / open を完了経路で呼ばない）、(f) mirror（codex/opencode）同期 + `bash scripts/sync-skill-mirrors.sh --check` PASS、(g) `TestTerminalSummary_*` 3 状態 + `bash scripts/ci/check-consistency.sh` PASS (完了サマリは shell hook ではなく Go task_completed.go の all-done で orchestration.Summary を 1 回 systemMessage 出力; cleanup.go は record-only) | 90.1.3 | cc:完了 [e53d939a] |
| 90.1.6 | `[Validation / Review Closeout]` `[lane:release]` `[tdd:skip:test-aggregation]` Phase 90 全体の validation と review。VERSION は変更せず CHANGELOG `[Unreleased]` に記録（feature PR 規約）。release/tag は作らない。 | (a) `git diff --check`、`bash tests/validate-plugin.sh`、`bash scripts/sync-skill-mirrors.sh --check`、`bash scripts/ci/check-consistency.sh` 全 PASS、(b) ledger/totals/scorecard/render/terminal の全テスト green + tri-state 3 状態 + 冪等 roll up（完了時 + 終了時の 2 経路）+ 完了サマリ（全完了で 1 回・毎タスクで 0 回）カバレッジ確認、(c) VERSION/plugin.json/harness.toml 不変（feature PR は触らない）、(d) `docs/CLAUDE-feature-table.md` に付加価値列付きで追記（A: 実装あり）、(e) `CHANGELOG.md` `[Unreleased]` に Before/After 形式で追記、(f) `bash scripts/codex-companion.sh review --base "${TASK_BASE_REF}"` APPROVE or auth failure + manual-pass、(g) PR body に設計判断/residual risk（lifetime は project-scoped でグローバル累計は未対応 / 自動表示は全タスク完了時のターミナルサマリ 1 回のみ・HTML は on-demand）を記載、(h) no tag/release; codex review は manual-pass | 90.1.1, 90.1.2, 90.1.3, 90.1.4, 90.1.5 | cc:完了 [closeout] |

---

## Phase 91: Zero-Base Redesign（単一CLI + 3-host native hook 収束）

Purpose: 当時の CC に合わせて作った harness を、CC/Codex/Cursor が native hook（`PreToolUse`/`preToolUse`・exit 2 deny）へ収束した現状に合わせ 0 ベースで作り直す。プラグイン層を脱して単一 CLI 化し、value-bearing な Go 約20%（R01-R13 policy engine ほか）を verbatim 再利用、host 結合の約80%（4 manifest / mirror 木 / 95 hookhandler / 7-way version sync）を削除。host 差分は `hosts.toml` 1枚 + `harness gen` 生成。ACP 不採用（native hook で per-action gating 達成）。設計 sub-spec: `docs/research/zero-base-redesign-2026-06.md`（本 Phase の正本）。root `spec.md` の Hokage Core / Execution Backend / Host Distribution 各 section は 91.8 cutover で改訂（それまで不変）。team_validation_mode: subagent（18-agent workflow + owner review 4 往復で検証済み、再検証不要）。big-bang cutover（後方互換なし）。Spec delta: 新 sub-spec を追加、root spec.md は 91.8 まで不変。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 91.1 | `[Phase 0: Kernel extraction]` `[tdd:required]` `go/internal/guardrail` を stdlib-only `policy/` package に verbatim 抽出。membridge(244 LOC)=`harnessmem` は既に stdlib-only で kernel 適格のため移送 no-op、17 exec.Command(git) を `gitport/` 1 seam に集約。`state/` trim（agent_states / CC-mode CHECK）は hooks.json/status/subagent lifecycle と runtime 結合のため 91.7 prune へ再配置。挙動変更なし。 | (a) `policy/` の import が stdlib+regexp+hookproto のみ（外部依存ゼロ）、(b) 既存 `rules_test.go` 全 green（R01-R13 不変）、(c) git 操作が `gitport/` 経由に統合され `grep -rn exec.Command go/internal \| grep -v gitport` が 0 件、(d) `go build ./...` PASS、(e) `go test ./...` PASS | - | cc:完了 [0aac006c, 500f50c7] |
| 91.2 | `[Phase 1: harness CLI spine]` `[tdd:required]` policy/+plans/+membridge/+state/+gitport/ を `harness plan\|work\|review\|release\|sync\|hook\|policy check\|doctor\|mem` subcommand の裏に配線。prompt pack を embed。Claude 上で headless にループが回る（hook/mirror まだ無し）。 | (a) 各 subcommand が存在し `--help` が動く、(b) `harness policy check` が stdin JSON → R01-R13 → exit 2 で deny、(c) prompt pack が binary に embed され `harness work` が読む、(d) dogfood で簡単な task を 1 件回せる、(e) `go test ./...` PASS | 91.1 | cc:完了 [690ee667 policy-check, 2570bc48 plans-parser, e2e11c53 promptpack+assemblers; Option C = emit-prompt（binary は LLM 非駆動）, end-to-end loop は 91.8 cutover で検証] |
| 91.3 | `[Phase 2: hosts.toml + harness gen]` `[tdd:required]` 単一 descriptor `hosts.toml` を定義し、`harness gen` が 3 host の hooks.json（Claude=`.claude-plugin/`、Codex=`.codex/`、Cursor=`.cursor/`、全部 `bin/harness hook pretool` を指す）+ skill/agent + manifest version + docs を生成。出力 gitignore。`harness gen --check` で golden fixture と diff。 | (a) `hosts.toml` が host ごと hook-event/deny手段/transport/model を持つ、(b) 生成 hooks.json 3種が全て `harness hook pretool` を呼ぶ、(c) 生成物が gitignore 対象、(d) `harness gen --check` が mismatch で fail、(e) mirror/version-sync が生成で置換され `sync-version.sh` 依存が消える | 91.2 | cc:完了 [3742acd5; (a)-(d) generator+hosts.toml+gen --check 完成、(e) version/mirror 置換と tracked manifest 削除は 91.8 cutover で実施] |
| 91.4 | `[Phase 3: 3-host hook 配線 + stdin codec]` `[tdd:required]` `harness hook pretool` に host 差正規化 codec（`permissionDecision`/`permission`、`session_id`/`conversation_id`、event 名 casing）。`.codex/hooks.json` / `.cursor/hooks.json`（現状カラ）を生成・配線。3 host とも native 事前ブロックが効くことを smoke 検証。 | (a) codec が 3 host の stdin を正規化し rule engine 本体不変、(b) 3 host の payload で deny→exit 2 を返す unit test、(c) `.codex/hooks.json`/`.cursor/hooks.json` が生成され `harness hook pretool` を指す、(d) 3 host smoke が pre-action block を確認、(e) `go test ./...` PASS | 91.3 | cc:完了 [3864b5bf; hookcodec Normalize/DenyOutput + --host + policy 不変 + 3-host deny→exit2 smoke] |
| 91.5 | `[Phase 4: orchestrator + companion(#1)]` `[tdd:required]` dead な `breezing.Orchestrator` を実 N-flat-sub-run scheduler として resurrect（harness が全 fan-out 所有、backend single-threaded）。companion を `companion-result.v1` に正規化（harness が他ツールを engine 駆動する #1 用のみ）。breezing skill を harness-work `--team` mode に畳む。 | (a) `Orchestrator` が N 個の独立 sub-run を起動し test 外から呼ばれる、(b) `companion-result.v1` を両 companion が産出、(c) breezing が `harness-work --team` に統合され重複 skill 削除、(d) 並列 task 分離を test で確認、(e) `go test ./...` PASS | 91.2 | cc:完了 [32808749; Orchestrator 復活 production caller + companion-result.v1 正規化 + 並列分離 test。skill 統合(c)は 91.7 へ] |
| 91.6 | `[Phase 5: FLOOR 硬化]` `[tdd:required]` cherry-pick diff への `harness policy check` + contract-grep（test-support-claim-wording.sh + check-consistency.sh + validate-plugin.sh）を全 non-CC backend の必須 pre-merge に。deny-list self-audit を `policy/` に畳む（baseline hash、弱体化で起動拒否）。 | (a) 非 CC backend の取り込みが cherry-pick 前に policy check + contract-grep 必須通過、(b) `policy/` が deny-surface baseline hash を持ち弱体化で起動拒否、(c) self-audit.md の役割が policy/ に移管、(d) gate 擦り抜け経路が test で塞がる、(e) `bash tests/validate-plugin.sh` PASS | 91.1, 91.5 | cc:完了 [dfdb58f1 FLOOR+self-audit; 4ac34023 91.1 path-drift residue 修正で validate-plugin 0 失敗] |
| 91.7 | `[Phase 6: prune + docs 再生成]` ~22 accreted skill（auth/crud/ui/deploy・media gen・session cluster・coaching cluster）、migration/self-audit scaffolding（check-residue.sh・deleted-concepts.yaml）、version-pinned rule file（hooks-2.1.x-plus.md 等）を削除。`state/` trim（agent_states テーブル/CRUD + status command + subagent lifecycle persistence + sessions.mode CHECK を削除、hooks.json 再生成と協調）= 91.1 から再配置。`harness gen docs` が CLAUDE.md/catalog を実 prompt set から再生成。 | (a) 対象 ~22 skill 削除 + 参照も再生成で消える、(b) `check-residue.sh`/`deleted-concepts.yaml` 廃止・deny-list は policy/ に、(c) version-pinned rule file が capability-detection 層に置換、(d) `harness gen docs` が drift した catalog/scaffolder 参照を消す、(e) `bash tests/validate-plugin.sh` PASS | 91.3 | cc:完了 [12311072; 15 skill×3 + scaffolding/version-pinned rule 削除(-74994行) + docsgen。曖昧な ~7 coaching skill は保守的に保持] |
| 91.8 | `[Phase 7: cutover 検証]` `[lane:release]` harness repo 自身に `harness work` で 1 full Plan→Ship cycle を回す。通過後にのみ旧 4 manifest/mirror を committed source から削除。root `spec.md` の Hokage Core/Execution Backend/Host Distribution を新モデルに改訂。 | (a) 自己 dogfood の Plan→Work→Review→Ship が 1 周完走（証拠 commit）、(b) 旧 4 manifest + mirror 木が削除され `harness gen` 生成に置換、(c) root spec.md 該当 3 section が 単一CLI/native hook/ACP不採用 に改訂、(d) 全 gate（validate-plugin / check-consistency / 3 host hook smoke）green、(e) big-bang 漏れ grep 0 件 | 91.1, 91.2, 91.3, 91.4, 91.5, 91.6, 91.7 | cc:完了 [6901f233 spec 3-section 改訂; fc018871 claude PreToolUse drift-check; Model A=generated-and-committed 着地] — (a) `harness work` dogfood ✓ / (b) **配布調査で untrack は Claude marketplace(repo clone) + Codex/OpenCode setup-copy 配布を壊すと判明（両経路とも committed file 依存・install 時生成経路なし）→ manifest/mirror は committed 保持 + drift-check（sync-skill-mirrors --check + harness gen --check[claude PreToolUse guardrail group の containment 含む] + gen docs --check）で SSOT 担保 = Model A**。物理 untrack は将来の pure-CLI install regenerate-on-target へ scope 外送り（spec.md Host Distribution Contract に根拠明記、ユーザー承認済 A）/ (c) spec 3-section 改訂 ✓ / (d) 全 gate green: validate-plugin 103/0/0・check-consistency 全合格・go test ./...・gen --check・gen docs --check・mirror --check / (e) check-residue/deleted-concepts は 91.7 削除済 + untrack 不実施で dangling 参照 0] |

---

## Breezing 自律完走契約（2026-06-12 ユーザー承認 — 実装セクション運転規約）

`/breezing all --cursor` が**途中の人間判断なしに実装セクションを完走する**ための運転規約。ユーザー指示（2026-06-12「途中で聞かれてもわからないから実装は終わらせてほしい。レビューとチェックは後でまとめてやる」）に基づく事前承認の記録。

**スコープ 2 分割**:
- **実装セクション**（breezing 完走対象）: 93.1.1 / 93.1.2 / 93.2.1 / 93.3.1-93.3.5 / 92.5.1-92.5.3 / 92.6.1-92.6.4 / 95.1.1-95.1.3 / 95.2.1-95.2.3 / 95.4.1 / 96.1.1-96.1.4 ＋ 旧 backlog（88.1 / 88.3 / 72.1.2-72.1.6 / 83.7）
- **検証セクション**（ユーザー review window、breezing は触らない）: 93.3.6 / 95.5.1 / 96.1.5 / 96.1.6（いずれも `[lane:release]` e2e・公開 claim 更新）＋ Phase 94（92.4.x、user GO 待ち scope 外）

**mid-run 質問禁止 + 分岐既定値**:
- 実装セクション中は AskUserQuestion を使わない。分岐は以下の既定値で進める
- review REQUEST_CHANGES → 最大 3 回修正 → 未収束は Status に `blocked(理由)` 注記 + **次タスクへ続行**（停止しない）
- companion 起動失敗 → 1 回 retry → 失敗なら blocked + 続行
- blocked 一覧は最終報告に集約しユーザー review へ渡す

**Risk Gate 事前承認**（92.6.4 / 96.1.4、2026-06-12 ユーザー指示による）: breezing は停止せず実装してよい。ただし 3 条件を厳守: (i) default-OFF / opt-in 設計を変えない（auto-approve は 96.1.3 実装後も default OFF）、(ii) 実ユーザー設定ファイル（`~/.claude/settings*.json`・実 repo の `.claude/settings.local.json`）への実書込はせず fixture/tempdir 内 test で検証、(iii) 5 カテゴリ floor・fingerprint 封じ込め・deny ルールの弱体化を伴わない。逸脱が必要になったらそのタスクだけ blocked にして続行。

**共有ファイル lane**（Invariant 1 運用）: `skills/harness-work/` / `skills/breezing/` / `agents/*.md` を編集するタスクは **prose lane として直列**: 92.5.3 → 88.1 → 88.3 → 72.1.2 → 72.1.3 → 72.1.4 → 72.1.5 → 72.1.6。Go core lane（92.5.1-2 / 92.6.x / 95.1.x / 95.2.x / 96.1.x）とは並列可。93.3.1 / 93.3.4 も breezing/review SKILL を触るため prose lane タスクとは同時実行しない。`Plans.md` / `CHANGELOG.md` / `spec.md` は worker 編集禁止（Lead が統合時に編集）。

**推奨 wave 順**（Depends 整合済み）: W1: 93.1.1 ∥ 93.1.2 ∥ 93.2.1 ∥ 83.7 → W2: 93.3.1 → (93.3.2 ∥ 93.3.4) → 93.3.3 → 93.3.5 → W3: 92.5.1 → 92.5.2 → 92.5.3 → W4: 92.6.1 → 92.6.2 → (92.6.3 ∥ 92.6.4) ∥ prose lane（88.1 → 88.3 → 72.1.2-72.1.6） → W5: (95.1.1 → 95.1.2 → 95.1.3) ∥ (95.2.2 → 95.2.1) → 95.2.3 → 95.4.1 → W6: (96.1.1 ∥ 96.1.4) → 96.1.2 → 96.1.3

**終了条件**: 実装セクション全タスクが `cc:done` または `blocked(理由)`。最終報告 = 全 commit hash + blocked 一覧 + 検証セクション（93.3.6 → 95.5.1 → 96.1.5 → 96.1.6）への引き継ぎ手順。

---

## Phase 92: Tri-Tool Autonomous Parallel Collaboration（北極星: 1 Lead で A/B/C 並列）

Purpose: 同一プロジェクトで異なる A/B/C を並行する時、1 つの Lead（v1=Claude Code 固定）が claude/codex/cursor を **headless 並列駆動**し、**物理分離（単一 fresh-base の worktree-per-branch）＋ Lead 集約（rebase→cherry-pick 1 個ずつ）** で衝突を起こさず、**sandbox 内 auto-approve ＋ 5 カテゴリ実行時ハードフロア** で安全に回す。連携本体（`harness work --team` orchestrator・`companionresult.v1`・`floor.Gate` file gate・`breezing.WorktreeManager`・orchestration ledger）は **Phase 91 で既存**（Lead が grep 独立検証で確認、synth の "dead/needs-build" は誤りと判明）。本 Phase は既存資産に (i) base 衛生 (ii) 実行時ハードフロア＋worktree fingerprint 封じ込め (iii) 集約硬化 (iv) batch リリース提案 (v) Mode 1 深化（Producer→Sub-Lead→Composer 並列→review→iterate→集約）(vi) Mode 2 = CCH 自前 live-messaging（agmsg パターン cherry-pick）を追加する。**2 モード**: Mode 1 = 完全自律オーケストレーション（headless）、Mode 2 = あなた在席の peer 端末 co-drive（live notice messaging）。**安全順序固定**: 92.1 base 衛生 → 92.2 実行時フロア＋封じ込め → ここで初めて auto-approve on（92.2 未完で auto-approve を有効化しない）。**重要境界**: live notice messaging = CCH 自前（zero-dep sqlite + host hook delivery、agmsg MIT パターン）/ durable work-handoff = harness-mem signal（workgraph に load-bearing、本 Phase で触らない）。「どれでも Lead」は v1 では Lead=Claude 固定（Codex/Cursor は外向き spawn API 無し）。team_validation_mode: subagent（6-stream research + adversarial skeptic + Lead grep 独立検証で確定）。Spec delta: spec.md に「Tri-Tool Parallel Collaboration Contract」新設、`.claude/rules/versioning.md` に「Release Train Proposal」新設（本コミットで反映）。Spec skip reason: なし（product-impacting）。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 92.1.1 | `[Phase A: base 衛生]` `[lane:gate]` `[tdd:required]` `scripts/spawn-parallel.sh <A> <B> <C>`（または `harness work --team` 前段）= `git fetch origin` + 単一 `BASE=$(git rev-parse HEAD)` + 各 task に `git worktree add -b task/$T <root>/task-$T $BASE`。Go `breezing.WorktreeManager` の root（`.harness-worktrees/`）と live（`.claude/worktrees/`）の矛盾を**1 つに統一**。`rerere.enabled` を設定。 | (a) 3 task 名で 3 worktree が 3 branch に作られ全て同一 base SHA（test が `task/A==task/B==task/C` を assert）、(b) worktree-root が単一に統一され spec/doc に明記、(c) `rerere.enabled` 設定、(d) 再実行 idempotent（既存検知）、(e) `go test ./...` PASS | - | cc:done [d4e4759f] |
| 92.1.2 | `[lane:fast]` `[tdd:required]` `scripts/reap-worktrees.sh`: worktree 削除 ＋ `task/*` branch 削除 ＋ prune。現存の stale worktree 群を掃除。 | (a) 実行後 `git worktree list` が trunk のみ、(b) `git branch` に `task/*` 残骸なし、(c) 0 個でも安全 no-op、(d) 3-worktree fixture で掃除を test 検証、(e) `go test ./...` PASS | 92.1.1 | cc:done [334ffbf9] |
| 92.1.3 | `[lane:fast]` `[tdd:skip:docs-only]` 共有ファイル分割規約を `docs/team-composition.md` + `.claude/rules` に明文化: `Plans.md`/`CHANGELOG.md`/`spec.md` は owner-assigned append-only block、`VERSION` は worktree 内で bump しない、生成物は trunk で 1 回再生成。 | (a) 規約に 3 invariant + owner-assign 例、(b) `check-consistency.sh` が規約存在を参照、(c) spec.md Tri-Tool Contract から相互参照 | 92.1.1 | cc:done [f1dfb6da] |
| 92.2.1 | `[Phase B: 実行時ハードフロア(安全核)]` `[lane:gate]` `[tdd:required]` 5 カテゴリ enum（money/billing・外部送信/egress・認証/secret 読取・本番 deploy・worktree 外破壊）を **worker 実行前**（companion 起動 / PreToolUse 層）で pattern-match し human へ hard-stop。どの config でも override 不可。既存 `floor.Gate`(file gate) を doc/spec で **"pre-merge policy gate"** と改名し混同を解消。 | (a) 5 カテゴリ enum 定義 + 各カテゴリで human escalation を強制する test、(b) どの config flag でも無効化不可を test で固定、(c) `curl`/`gh release`/secret 読取/payment CLI/worktree prefix 外 `rm` が実行前に止まる、(d) 既存 floor が "pre-merge policy gate" として区別、(e) `go test ./...` PASS | - | cc:done [a81ab114] |
| 92.2.2 | `[lane:gate]` `[tdd:required]` `[Risk Gate]` cursor/codex worker の worktree 封じ込め（v1 = fingerprint 検知）: 実行前後で worktree 外（`$HOME` 機微パス・trunk・他 worktree）の変更を fingerprint 比較し、変化あれば hard-stop（= floor カテゴリ 5）。`--workspace` は CWD ヒントで書込境界でないと doc 明記。OS 層封じ込め（sandbox-exec/unshare）は将来硬化として記録のみ。 | (a) worktree 外書込を起こす擬似 worker で fingerprint が hard-stop、(b) worktree 内変更は通過、(c) `~/.claude/settings.json`・`~/.aws` 等への書込検知を test 化、(d) `cursor-companion.sh` doc に「workspace は境界でない」明記、(e) `go test ./...` PASS | 92.2.1 | cc:done [2b721412] |
| 92.2.3 | `[lane:gate]` `[tdd:required]` 実行時フロア ＋ fingerprint 封じ込めを `harness work --team` に配線し、auto-approve は **封じ込め済み worktree 内のみ**適用。**92.1+92.2 完了まで auto-approve を有効化しない**（fail-safe で固定）。 | (a) `--team` 経路が dispatch 前に実行時フロアを通す、(b) auto-approve スコープが worktree 内に限定され境界を test 確認、(c) 92.1/92.2 未完では auto-approve が off であることを fail-safe で固定、(d) orchestration-ledger に floor 判定を記録、(e) `bash tests/validate-plugin.sh` PASS | 92.1.1, 92.2.1, 92.2.2 | cc:done [dc5f1194] |
| 92.3.1 | `[Phase C: 集約硬化]` `[lane:gate]` `[tdd:required]` Lead 集約を orchestrator から呼べる callable 化: task ごと `rebase task/$T onto trunk` → `cherry-pick --no-commit` → pre-merge policy gate（`floor.Gate`）→ commit を 1 個ずつ。rerere replay。cherry-pick FLOOR を prose から callable 関数へ。 | (a) 3 task 逐次統合、2 番目で共有ファイル block 衝突が rerere で自動解決、(b) cherry-pick FLOOR が callable 関数（prose でない）、(c) 各統合で `floor.Gate` 実行、(d) 統合順序を ledger 記録、(e) `go test ./...` PASS | 92.2.3 | cc:done [265fa4f2] |
| 92.3.2 | `[lane:gate]` `[tdd:required]` 本番 WorkerFunc（`productionCompanionWorker`）が test stub でなく実 codex/cursor companion を叩く end-to-end を検証（skeptic の唯一の未検証リンク）。異種 3 task（A=claude/B=codex/C=cursor）を実走 or 実バイナリ stub で確認。 | (a) 実 companion 解決経路を test stub と分離して検証、(b) 異種 3 backend が各 worktree で並列実行し `companionresult.v1` を返す、(c) companion 解決失敗時の fail 経路を test、(d) ledger に backend 別 dispatch/result、(e) `go test ./...` PASS | 92.1.1, 92.2.3 | cc:done [1eed26c0] |
| 92.3.3 | `[lane:release]` `[tdd:skip:integration-verification]` 全 task 統合後に `harness gen` を trunk で 1 回 + `validate-plugin.sh` + `check-consistency.sh`。 | (a) 統合後 trunk で生成物を 1 回再生成・commit、(b) validate-plugin PASS、(c) check-consistency PASS | 92.3.1 | cc:done [217fa904] |
| 92.5.1 | `[Phase E: Mode 1 Producer 階層]` `[lane:gate]` `[tdd:required]` Lead/Producer が各レーンを Sub-Lead（Lead と同一 CLI）に委譲し、Sub-Lead がレーンを sub-task に分解（mini-plan）→ 実装を Composer 2.5（cursor backend、`resolve-impl-backend.sh --role worker` 経由）に並列委託 → 結果を `companionresult.v1` で Lead に集約。orchestrator の WorkerFunc を「Sub-Lead が mini-plan を回す」階層に拡張。worker 間 messaging ゼロ（hub-spoke 維持）。 | (a) 1 レーンが sub-task に分解され Composer 並列で実装、(b) Sub-Lead が結果を `companionresult.v1` で Lead に報告、(c) Lead↔Sub-Lead↔Worker 階層を test で確認、(d) worker 間 messaging 0 件（grep audit）、(e) `go test ./...` PASS | 92.2.3, 92.3.1 | cc:done [47b907fd] |
| 92.5.2 | `[lane:gate]` `[tdd:required]` review→iterate ループ: Sub-Lead が worker 出力を (i) fresh-context 並列サブエージェント（複数観点）(ii) cross-CLI review（実装 backend と別 CLI、`resolve-impl-backend.sh --role reviewer`、spec.md Execution Backend Contract の self-review scope 契約）でレビューし、DoD 未達なら精緻化タスクを同じ worktree に再投入。**自動反復→最後に Lead/人間に見せる**。最大反復回数 N で打ち切り、未収束は人間エスカレーション。 | (a) review が DoD 未達検出→最大 N 回再投入、(b) primary verdict は brain（claude host）が出し、advisory reviewer は producing session と会話状態を共有しない fresh-context（cross-CLI または同一 backend の別 session）、(c) fresh-context 並列レビュー観点が複数、(d) N 回未収束で human escalation、(e) `go test ./...` PASS | 92.5.1 | cc:done [73abd28d] |
| 92.5.3 | `[lane:gate]` `[tdd:skip:docs-only]` `harness-work`/`breezing`/`agents/worker.md`/`agents/reviewer.md` の契約を Producer→Sub-Lead→Composer + review→iterate に更新。cross-CLI review の「OK まで反復」を明記。 | (a) 各 SKILL/agent が Sub-Lead 層と review→iterate を記述、(b) `sync-skill-mirrors.sh --check` PASS、(c) spec Tri-Tool Contract と整合 | 92.5.2 | cc:done [0f7270c7] |
| 92.6.1 | `[Phase F: Mode 2 CCH live-messaging]` `[lane:gate]` `[tdd:required]` CCH 自前の message store（zero-dep sqlite WAL・append-only event log: `message_sent`/`message_read`）を `go/internal/livemsg`（仮称）に新設。identity = team/agent（agmsg パターン、**MIT attribution 保持**）。**harness-mem 非依存**（mem 未導入でも動く）。harness-mem の workgraph signal とは別系統（durable work-handoff=mem / live notice=CCH）。 | (a) sqlite WAL store に send/read/ack、(b) append-only event log で read flag を projection、(c) harness-mem 未導入でも動作、(d) MIT attribution を source に記載、(e) `go test ./...` PASS | 92.2.1 | cc:done [3f5c1567] |
| 92.6.2 | `[lane:gate]` `[tdd:required]` delivery-notice host hook（broadcast corpse の構造的死因＝store/notice 分離を解消）: `turn`=Stop hook→check-inbox（毎ターン境界）/ `monitor`=SessionStart→Monitor blocking stream（CC のみ）。hostgen 経由で各 host に生成。Codex/Cursor は Monitor tool 無しで `turn` に fallback。 | (a) Stop hook が毎ターン inbox を読む、(b) monitor が CC で blocking stream、(c) **fire-on-normal-turn を test で証明**（spec.md:776 復活要件）、(d) Codex/Cursor が turn fallback、(e) `go test ./...` PASS | 92.6.1 | cc:done [b7ad856a] |
| 92.6.3 | `[lane:gate]` `[tdd:required]` GO ループ + 最大往復 + human-stop: 端末 A→B→A の往復を harness policy で制御。GO で送信→相手が notice→応答→質問は同経路で返す。最大往復 N で打ち切り、収束しない or「人間判断が要る」（5 カテゴリ floor 連動）で停止しユーザーへ。 | (a) GO で A→B 送信→B notice→応答→A 受信、(b) 最大 N 往復で打ち切り、(c) human-stop 条件（floor 連動）、(d) 往復を orchestration-ledger に記録、(e) `go test ./...` PASS | 92.6.2 | cc:done [c63e3992] |
| 92.6.4 | `[lane:gate]` `[tdd:required]` `[Risk Gate]` self-audit allowlist: CCH live-messaging が `.claude/settings.local.json` に書く delivery hook を `self-audit.md` が**既知 fingerprint として allowlist**（自分の連携 hook を改ざんと誤検知しない）。未知の hook 注入は従来どおり検知。 | (a) self-audit が CCH delivery hook を既知として通す、(b) 未知 hook 注入は依然 detect、(c) allowlist が CCH 生成 hook に限定され広すぎない、(d) 誤検知 0・見逃し 0 を test、(e) `bash tests/validate-plugin.sh` PASS | 92.6.2 | cc:done [4570eb96] |
| 92.6.5 | `[withdraw 2026-06-11]` `[lane:gate]` `[tdd:skip:withdrawn]` 当初: cross-CLI review を Mode 2 transport に配線（CCH live-messaging 経由で Mode 1 reviewer≠implementer を実装）。**取り下げ理由**: Mode 1 review は 92.5.2 の in-process 経路（fresh-context 並列サブエージェント + cross-CLI review、spec.md Execution Backend Contract の self-review scope 契約）で完結するため Mode 2 transport を流用すると durable handoff と live notice が混線する。**吸収先**: (i) Mode 1 review 契約は 92.5.2 が単独で satisfy、(ii) spec.md L864 周辺「Mode 1 cross-CLI review transport も Mode 2 を使う」記述の修正は 95.4.1 に組み込む。本タスクは実装しない。 | (a) cc:withdrawn を維持し breezing が pickup しない、(b) 95.4.1 に spec.md L864 修正が含まれることを確認、(c) 92.5.2 の DoD が cross-CLI review を単独でカバー | 92.5.2, 92.6.3 | cc:withdrawn |

---

## Phase 93: Plan B c 段階 — /breezing MVP + 契約修正ラウンド（北極星: 入口 /breezing 一つ、出口 harness-mem）

Purpose: Plan B（/breezing 単一入口・Decision Card・harness-mem 中枢神経化）の段階導入 **c = MVP**（Plan B 工程表の Phase 0-1 相当、2.5 週）。launch contract: `/breezing all --cursor`（worker=composer-2.5-fast、primary review/advisor=brain 固定、fresh-context composer プレレビューは spec.md Execution Backend Contract で許可済み [951d0d48]）。採用判断（2026-06-11 ユーザー指示）: (1) 段階導入 c→b→a、(2) 退役スキルはエイリアス永続、(3) harness-mem は全層連携を目標とし本 Phase は書き込み層 v0 まで。93.0.x は本コミット前に完了済みの先行作業。**auto-approve は引き続き 92.1+92.2 完了まで OFF**（92.2.3 の fail-safe 契約を変更しない）。並列 worker 数: 93.1.1 / 93.1.2 / 93.2.1 は書き込みファイルが独立し 3 並列可、93.3.1-93.3.4 は breezing SKILL/スクリプト群を共有するため 2 並列まで（93.3.2+93.3.3 同時は不可、93.3.1 完了後に直列）。93.1.3 は Plans.md 自体を編集するため **Lead 直接実行**（worker 委譲禁止、NG-1 ガード対象）。harness-mem 連携は fail-open（mem 不達で breezing を止めない）+ `active-watching-test-policy.md` の 3 状態テスト規約に従い、workgraph signal API は呼ばない（load-bearing、本 Phase で触らない）。注記: `/breezing all` は旧 backlog（72.1.2-72.1.6 / 83.7 / 88.3 / 92.x）も対象に含む（Plans 記載順 + Depends で先行）。Spec delta: spec.md Execution Backend Contract に self-review scope 精緻化 + `HARNESS_BRAIN_MODEL` opt-in を反映済み [951d0d48]、「Breezing Brief Contract」章は 93.3.5 で新設。Spec skip reason: なし（product-impacting）。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 93.0.1 | `[先行完了]` `[lane:fast]` `[tdd:required]` Fable 5 brain opt-in: `scripts/model-routing.sh` に `HARNESS_BRAIN_MODEL`（unset/`opus`=default、`fable`、他値は exit 2）を追加し claude 頭脳枠（deep/advisor）のみ `claude-fable-5` へ切替可能化。`go/cmd/harness/validate.go` 認識モデルに `claude-fable-5` 追加 + platform バイナリ 4 本再ビルド（`bin/harness` shim は不変）。 | (a) `bash tests/test-model-routing.sh` PASS（fable 8 assertion 含む）、(b) `go test ./...` PASS、(c) default が `claude-opus-4-8` のまま、(d) codex/cursor 表が不変 | - | cc:done [ce5eee06] |
| 93.0.2 | `[先行完了]` `[lane:gate]` `[tdd:skip:docs-only]` self-review scope 契約: spec.md Execution Backend Contract の自己レビュー禁止を「producing context 単位」に精緻化し、fresh-context composer プレレビュー（cursor `review` tier）を advisory として正式許可。`docs/model-routing-policy.md` + CHANGELOG `[Unreleased]` 同期。 | (a) spec.md に "producing context" / "fresh-context" / "advisory pre-review" の 3 語が存在、(b) primary verdict が brain 固定のまま、(c) `bash scripts/ci/check-consistency.sh` PASS | - | cc:done [951d0d48] |
| 93.1.1 | `[契約修正ラウンド]` `[lane:gate]` `[tdd:skip:docs-only]` spec.md の 4 契約修正: (i) Session Coordination Contract の notice 保証文言を「best-effort + 次 turn 確認」へ格下げ（idle 非 CC peer に idle-fire hook が存在しない事実へ整合）、(ii) trust envelope を DoD 化（SendMessage relay は user authority を持たない。spec.md:768-772 の free-text never-echo と整合）、(iii) Sub-Lead 実行方式を「orchestrator-spawned headless CLI per lane」に固定（92.5.1 の前提を 1 行で判定可能に）、(iv) Mode 2 peer = 「ユーザーが自分で開いたセッション」であることを Session Coordination Contract に明記。 | (a) Session Coordination Contract 内に `best-effort` が 1 件以上、(b) trust envelope DoD 文が存在し `user authority` を含む、(c) `grep -n "orchestrator-spawned" spec.md` 1 件以上、(d) Mode 2 peer 定義 1 件以上、(e) `bash scripts/ci/check-consistency.sh` PASS | - | cc:done [01867589] |
| 93.1.2 | `[lane:fast]` `[tdd:skip:docs-only]` AppleScript 決定の codify: `.claude/rules/cursor-cli-only.md` に「AppleScript は `activate` 1 動詞 + `open -a` のみ許可、`System Events` keystroke/click 注入は禁止」1 節を追記し、再昇格 4 条件（hook input への source-identifier 追加 / Read tool の sandbox 拡張 / Codex hooks の非 Bash イベント対応 / Cursor AppleScript dictionary 提供）を `docs/research/applescript-decision-2026-06.md` に記録。 | (a) rule に禁止 1 節 + 許可 2 動詞、(b) 再昇格 4 条件が docs に列挙、(c) `bash tests/validate-plugin.sh` PASS | - | cc:done [9a34f3f8] |
| 93.1.3 | `[lane:fast]` `[tdd:skip:plans-restructure-only]` `[Lead 直接実行]` Phase 92 再裁断: (i) 92.6.5 に「withdraw（92.5.2 の in-process 経路で代替、transport 混線回避）」注記、(ii) 92.4.1-92.4.3（release train）を Phase 94 セクションへ移設（行内容・ID・cc: マーカー不変）、(iii) 92.5.1 の Composer 委託前提に「`resolve-impl-backend.sh` 経由」を 1 行追記。 | (a) 92.6.5 に withdraw 注記、(b) Phase 94 セクションに 92.4.x 3 行が ID 不変で存在、(c) cc: マーカー変更 0（diff で確認）、(d) `bash scripts/plans-format-check.sh` PASS | - | cc:done [先行 restructure 済を 2026-06-12 検証: (a)(b)(c) 全satisfied + plans-format-check PASS] |
| 93.2.1 | `[Phase 0: 公式機能棚卸し]` `[lane:fast]` `[tdd:skip:research-only]` 3 CLI 公式機能 inventory: CC（Agent Teams / Channels / agent view / dynamic workflows / hooks）、Cursor（1.7+ hooks / stop followup_message / cursor-agent stream-json）、Codex（app-server / subagents / PreToolUse Bash 限定）を `docs/research/official-feature-inventory-2026-06.md` に「機能名 / 最低バージョン / GA・preview / CCH 重複 / 採用・置換・捨てる」5 列で固定。preview 依存の採用行には「GA 時置換可能な抽象化」を 1 行ずつ明記。 | (a) ファイル存在 + 3 host 合計 15 行以上、(b) 各行に GA/preview 区分、(c) preview 採用行に置換抽象化 1 行、(d) `mcp__harness__harness_mem_ingest` で inventory 保存（`source` タグ付き。手動 attestation、CI 対象外と明記） | - | cc:done [89021014] |
| 93.3.1 | `[Phase 1: /breezing MVP]` `[lane:gate]` `[tdd:required]` Brief Composer v0: 既存 argument-hint（`all|N-M|--codex|--cursor|--reviewer-only|--parallel N|--no-commit|--no-discuss|--auto-mode`）のどれにも一致しない自由文入力を 3-7 個のサブタスクへ分解し、`brief-card.v1`（`{goal, subtasks[], scope_files[], risk_notes[], confidence: high\|medium\|low}`）を提示。ユーザー Yes/No 確定後に既存 team 経路（worktree-per-task）へ渡す。schema は `templates/schemas/brief-card.v1.json`。 | (a) schema が JSON Schema validator で valid、(b) fixture 自由文 3 件で 3-7 サブタスク化を assert（LLM 呼び出しは stub/golden 固定で決定論化）、(c) No 回答で実行 0 件（dry 確認 test）、(d) 既存引数パターンへの regression 0（`bash tests/test-composer-backend-trigger.sh` PASS 維持）、(e) `bash tests/test-breezing-brief.sh` PASS | 93.2.1 | cc:done [69072371] |
| 93.3.2 | `[lane:gate]` `[tdd:required]` harness-mem 書き込み層 v0: breezing の 4 イベント（run 開始 / brief 確定 / worker 完了・失敗 / 集約完了）を `harness_mem_record_event`、確定 brief を `harness_mem_ingest` で自動記録。mem 未導入・不達は fail-open（1 行警告 + 続行）。 | (a) 4 イベント記録の統合 test、(b) `TestBreezingMem_NotConfigured` で警告非表示 + 続行、(c) `TestBreezingMem_Unreachable` で 1 行警告 + 続行、(d) workgraph signal API 呼び出し 0 件（grep audit）、(e) `go test ./...` PASS | 93.3.1 | cc:done [794a9523] |
| 93.3.3 | `[lane:gate]` `[tdd:required]` Decision Card v0（`judgment-card.v1`）: 発行条件 3 つ（DoD 解釈が割れる / 承認済み scope 外の変更が必要 / トレードオフ選択が必要）のいずれかで `{question, options[2-3], recommendation, confidence: high\|medium\|low, impact, diff_summary}` の 1 枚カードを提示し、回答 + Why を `harness_mem_record_checkpoint` で記録。floor 5 カテゴリ該当時はカードを出さず hard-stop のみ（92.2.1 契約を上書きしない）。 | (a) schema valid + 列挙値固定、(b) カード提示 → 回答記録の流れを test、(c) floor 該当時にカード非発行 test、(d) mem fail-open、(e) `bash tests/test-judgment-card.sh` PASS | 93.3.1, 93.3.2 | cc:done [f37f7c1e] |
| 93.3.4 | `[lane:fast]` `[tdd:required]` composer プレレビュー配線: `harness-review --pre-review cursor` で fresh-context composer-2.5-fast（`scripts/model-routing.sh --host cursor --tier review`）の advisory pre-review pass を brain 一次レビューの前に 1 回実行。pre-review は read-only（`scripts/cursor-companion.sh task`、`--write` なし）。findings は brain 入力へ添付、verdict（`APPROVE \| REQUEST_CHANGES`）は brain のみが出す。spec 先行のため本 task 完了まで pre-review 運用は手動確認。 | (a) `--pre-review cursor` で companion が read-only 起動、(b) producing worker session と会話状態を共有しない（新規 session を test で確認）、(c) verdict が brain 出力にのみ存在、(d) pre-review 失敗時は skip + 1 行警告で brain レビュー続行、(e) `bash tests/test-pre-review-cursor.sh` PASS、(f) `skills/harness-work/SKILL.md` / `skills/breezing/SKILL.md` の Role scope 文言に pre-review 例外（先行更新済み）が退行していないことを grep 確認 + `skills/harness-review/SKILL.md` の「Opus reviewer 必須併走」表現を brain / self-review scope 用語へ更新 + `bash scripts/sync-skill-mirrors.sh --check` PASS | 93.0.2 | cc:done [9dcd033f] |
| 93.3.5 | `[lane:gate]` `[tdd:skip:docs-only]` spec.md に「Breezing Brief Contract」章を新設: `brief-card.v1` / `judgment-card.v1` のスキーマ名と列挙値、mem 記録 4 イベント、fail-open 原則、floor との優先順位（floor hard-stop > card）を製品契約化。README は変更しない（公開 claim は 93.3.6 検証後）。 | (a) spec.md 新章に schema 名 2 つ + 列挙値、(b) `bash scripts/ci/check-consistency.sh` PASS、(c) README diff 0 | 93.3.1, 93.3.2, 93.3.3 | cc:done [7fb48d42, 2affec7d] |
| 93.3.6 | `[lane:release]` `[tdd:skip:integration-verification]` MVP e2e: 実 3 タスクで `/breezing all --cursor` を 1 周し、(a) 3 worktree 同一 base SHA、(b) brief card 提示（自由文入口）、(c) mem に 4 イベント記録、(d) composer プレレビュー 1 回 + brain verdict、(e) 集約 diff 提示、を evidence（orchestration ledger + mem 記録 + commit hash）付きで確認。auto-approve OFF・手動承認のまま実走（floor 5 カテゴリ [92.2.1] + worktree fingerprint 封じ込め [92.2.2] の配備後にのみ実行）。**Evidence (2026-06-13)**: (a) W1 三 worktree (93.1.1/93.1.2/93.2.1) すべて base SHA `b98c3b94` ✓ / (b) `breezing-brief.sh classify` 自由文→`free-text`、card validate OK、confirm no→`DISPATCH:0` dry、yes→`DISPATCH:4` ✓ / (c) `breezingmem.{EventRunStarted, EventBriefConfirmed, EventWorkerResult, EventAggregationDone}` 配線 + 5 test PASS（Healthy/NotConfigured/Unreachable/IngestBrief/NoSignalAPI） ✓ / (d) `pre-review-cursor.sh` + 14 test PASS + skill 文「verdict は brain のみ」 ✓ / (e) 本セッション trunk 64 commit cherry-pick (794a9523..ecfc3c50) ✓ / (f) validate-plugin PASS ✓ / (g) check-consistency PASS ✓ | (a)-(e) の evidence を本行 Status に追記、(f) `bash tests/validate-plugin.sh` PASS、(g) `bash scripts/ci/check-consistency.sh` PASS | 93.3.2, 93.3.3, 93.3.4, 93.3.5, 92.1.1, 92.2.1, 92.2.2 | cc:done [ecfc3c50] |

---

## Phase 94: Release Train Proposal — 92.4.x の論理的受け皿（ID 不変で移設、scope 外送り）

Purpose: stage b 完成と直接関係しない release-train（CHANGELOG 監視 + RELEASE_CANDIDATE 提案）を Phase 92 から分離し、stage b の critical path から外す。Phase 92.4.1-92.4.3 を **ID 不変（92.4.x のまま）/ cc: マーカー不変 / 内容不変**で本セクション配下に列挙する（93.1.3 DoD (b) の「Phase 94 セクションに 92.4.x 3 行が ID 不変で存在」要件を満たす）。本 Phase は stage b 完成判定（96.1.5）に含めない（user が GO したら走る scope 外）。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 92.4.1 | `[Phase D: batch リリース提案]` `[lane:fast]` `[tdd:required]` `harness-release --check`(read-only): `CHANGELOG [Unreleased]` を解析し v1 トリガー（最終 tag から **7 日経過** OR `### Breaking` 有; `### Security` 有なら **2 日**）で `RELEASE_CANDIDATE`(推定 bump 付) を emit。version ファイルは一切触らない。 | (a) fixture `[Unreleased]` で正しい `RELEASE_CANDIDATE` と bump 推定、(b) read-only（exit 0・ファイル書込 0）、(c) Security で T=2d 短縮、(d) 閾値未達で候補を出さない、(e) test PASS | - | cc:TODO |
| 92.4.2 | `[lane:gate]` `[tdd:required]` `RELEASE_CANDIDATE` を Session Monitor / UserPromptSubmit に tri-state（Candidate/None/NotApplicable）で受動表示。`active-watching-test-policy.md` の 3 状態命名に従う。 | (a) 閾値到達で候補行表示、(b) 未達は silent（not-configured 流儀）、(c) `TestXxx_Candidate/_None/_NotApplicable` 3 test、(d) human は GO/無視 だけ、(e) `go test ./...` PASS | 92.4.1 | cc:TODO |
| 92.4.3 | `[lane:fast]` `[tdd:skip:docs-only]` トリガー（7d / Breaking / Security-2d）と「提案であって gate でない」原則を `.claude/rules/versioning.md` の Release Train Proposal section に明文化。 | (a) versioning.md に Release Train Proposal section と数値・原則、(b) spec/doc と整合 | 92.4.1 | cc:TODO |

---

## Phase 95: Plan B Phase 2 — Bridge Daemon + Decision Card UI 本格版 + harness-mem 読み出し層（北極星: 過去判断を参照した推奨が出る）

Purpose: c MVP（Phase 93）の `judgment-card.v1` v0 と mem 書き込み層 v0 を本格版へ進化させる。**(i) Bridge Daemon**: 3 CLI 出力（CC Mailbox / Cursor stop hook / Codex app-server）を 1 mailbox に正規化（`bridge-event.v1`）し、非 CC peer への delivery 経路を確立。**(ii) Decision Card UI 本格版**: 過去判断 3 件参照付きの 1 枚 HTML（templates/html/judgment-card.html.template）、`impact_score`（worktree-fingerprint 影響範囲 + 5 カテゴリ floor 距離）算出、`similar_past_decisions[]` 自動 populate。**(iii) harness-mem 読み出し層**: Brief Composer / Triad Dispatcher / Decision Card の 3 コンポーネントが `harness_mem_search` を呼ぶ配線（fail-open、similarity score 付き 3 件返却、workgraph signal 不使用）。本 Phase 完了で Plan B 詳細 §7 Phase 2 体感価値「過去判断を参照した推奨が出る」が達成。auto-approve は引き続き OFF（96.x で ON 化）。**並列性**: 95.1.x（Bridge Daemon: `go/internal/bridge`）と 95.2.x（Decision Card UI 本格版: `templates/html/` + `scripts/render-judgment-card.sh`）は書き込みディレクトリが独立し **2 並列可**。ただし 95.2.x 内部は schema 拡張（95.2.2）→ template 配線（95.2.1）→ mem 読み出し配線（95.2.3）の順で直列。95.2.3（読み出し層配線）は 95.1.3 + 95.2.1 完了後の集約点（schema は 95.2.1 が 95.2.2 を depends に含むため transitively gate される）。95.4.1（spec 章追加）と 95.5.1（e2e）は最後に直列。team_validation_mode: subagent（並列 worker による Bridge / UI 独立検証 + Lead 集約レビュー）。Spec delta: spec.md に「Bridge Daemon Contract」「Decision Card Surface Contract」を新章として追加（95.4.1）。Spec skip reason: なし（product-impacting）。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 95.1.1 | `[Bridge Daemon]` `[lane:gate]` `[tdd:required]` `go/internal/bridge` skeleton + 3 source adapter（CC Mailbox / Cursor stop hook / Codex app-server）。各 adapter は source-specific event を `bridge-event.v1`（`{source: cc\|cursor\|codex, event_type, payload, ts}`）に normalize。adapter 不在 source は fail-open（skip + 1 行警告）。`templates/schemas/bridge-event.v1.json` を新設。 | (a) schema が JSON Schema validator で valid、(b) 3 adapter unit test（各 source の fixture 入力で正常 normalize）、(c) normalize 後 event が source-agnostic（grep audit）、(d) source 不在で fail-open + 1 行警告、(e) `go test ./...` PASS | 93.3.2 | cc:done [73d65c40] |
| 95.1.2 | `[lane:gate]` `[tdd:required]` mailbox unified store: sqlite WAL append-only event log で 3 source の event を 1 mailbox に集約（Phase 92.6.1 の `livemsg` store とは別系統、bridge 専用）。`bridge-event.v1` の projection を read API として提供。harness-mem への自動 ingest を lane 別設定（`lane:fast` 記録、`lane:gate` 記録 + audit log、`lane:release` 記録 + alert）。 | (a) unified store の append + projection test、(b) 3 source mixed event の順序保証 test、(c) mem ingest が fail-open（3 状態テスト）、(d) projection が source filter 可能、(e) `go test ./...` PASS | 95.1.1 | cc:done [35193408, 4a3158da] |
| 95.1.3 | `[lane:gate]` `[tdd:required]` delivery layer: 非 CC peer（Cursor / Codex）への notice delivery を host hook 経由で実装。Cursor は 1.7+ stop followup_message、Codex は Bash PreToolUse 連動、CC は Monitor blocking stream（**92.6.2 の delivery-notice host hook を流用するため 92.6.2 を depends に含める**）。delivery 失敗時は次 turn fallback（1 行警告 + 記録）。 | (a) 3 CLI 各 delivery 経路の unit test、(b) Cursor stop followup_message の fixture test、(c) Codex Bash 連動の fixture test、(d) delivery 失敗で次 turn fallback test、(e) `bash tests/test-bridge-delivery.sh` PASS | 95.1.2, 92.6.2 | cc:done [cbfb3e53] |
| 95.2.2 | `[Decision Card v1 schema]` `[lane:gate]` `[tdd:required]` `judgment-card.v1` schema 拡張: `{impact_score: 0-100, similar_past_decisions[]: [{summary, decision, outcome, decided_at, mem_id}]}` を v1 として追加。v0 input は v1 として valid（下位互換）。`impact_score` 算出は (i) worktree-fingerprint 影響範囲（変更ファイル数 / 行数）+ (ii) 5 カテゴリ floor との距離（floor 該当は 100、非該当は 0-99 で scaling）。 | (a) schema extension が JSON Schema validator で valid、(b) v0 → v1 互換 test（v0 fixture を v1 として validate）、(c) `impact_score` 算出ロジック unit test（floor 該当・非該当各 1 ケース）+ **floor 該当時（impact_score=100）は判定カード非発行で hard-stop のみとなる regression test を含む（93.3.3 floor > card 契約継承）**、(d) `similar_past_decisions` が mem_search 結果から populate される test、(e) `go test ./...` PASS | 93.3.3 | cc:done [6bbf4a07]; wired: no (S3=Phase 105 で breezing 本流へ配線予定) |
| 95.2.1 | `[Decision Card UI 本格版]` `[lane:gate]` `[tdd:required]` `templates/html/judgment-card.html.template`: 3 択 + 推奨 + 確信度 + 影響範囲 + 1 行 diff サマリ + 類似過去判断 3 件（mem_search 結果）を 1 画面に収まる HTML として生成。レンダリングは `scripts/render-judgment-card.sh`（静的、PWA・WebSocket 不使用、再生成で更新）。 | (a) template 存在 + HTML5 valid、(b) 過去判断 3 件 link 表示（mem-id 付き）、(c) fixture 入力 3 件で render 確認、(d) 1 画面 1280x800 に収まる視覚検証（手動 attestation、CI 対象外と明記）、(e) `bash tests/test-judgment-card-render.sh` PASS | 95.2.2 | cc:done [038d0ef1]; wired: no (S3=Phase 105 で breezing 本流へ配線予定) |
| 95.2.3 | `[lane:gate]` `[tdd:required]` `[harness-mem 読み出し層]` Brief Composer（93.3.1 で導入）/ **Triad Dispatcher（既存 `scripts/resolve-impl-backend.sh` + worker dispatcher の進化形として本タスクで定義: 自然文サブタスクから claude/codex/cursor backend へ自動振り分けする層）** / Decision Card UI（95.2.1）が `harness_mem_search` を呼ぶ配線。fail-open（mem 不達で breezing を止めない、1 行警告で続行）。各コンポーネントの結果に similarity score 付き 3 件返す。workgraph signal API は呼ばない（load-bearing、Phase 95 で触らない）。 | (a) 3 コンポーネント（Brief Composer / Triad Dispatcher / Decision Card UI）が `harness_mem_search` を呼ぶ統合 test、(b) fail-open 3 状態テスト（`TestComponentMemRead_NotConfigured` / `_Unreachable` / `_Healthy`）、(c) result が similarity score 付き 3 件返却、(d) `workgraph` grep audit 0 件、(e) Triad Dispatcher の振り分けロジック unit test（自然文 3 fixture → backend 解決）、(f) `go test ./...` PASS | 95.1.3, 95.2.1 | cc:done [3b81f563]; wired: no (S3=Phase 105 で breezing 本流へ配線予定) |
| 95.4.1 | `[Spec sync]` `[lane:gate]` `[tdd:skip:docs-only]` spec.md に「Bridge Daemon Contract」「Decision Card Surface Contract」を新章として追加: `bridge-event.v1` schema 名、3 source adapter 契約、delivery layer の host hook fallback、`judgment-card.v1` v1 拡張、過去判断参照の精度保証、mem 読み出し層 fail-open 原則を製品契約化。**併せて spec.md Session Coordination Contract の「Mode 1 cross-CLI review transport も Mode 2 を使う」記述（spec.md L864 周辺、92.6.5 withdraw により陳腐化）を 92.5.2 の in-process 経路（fresh-context 並列サブエージェント + cross-CLI review）に書き換える**。README は変更しない（公開 claim は 96.1.5 の stage b 完成判定後）。 | (a) spec.md 2 新章存在、(b) schema 名 2 つ + 列挙値固定、(c) Session Coordination Contract から Mode 2 transport 流用記述が削除され 92.5.2 in-process 参照に置換されたことを grep で確認、(d) `bash scripts/ci/check-consistency.sh` PASS、(e) README diff 0、(f) 既存 spec 章との矛盾なし（92.6.5 withdraw との整合を grep audit） | 95.1.3, 95.2.3 | cc:done [b3183412] |
| 95.5.1 | `[Phase 95 e2e]` `[lane:release]` `[tdd:skip:integration-verification]` 実 3 タスクで `/breezing all --cursor` を 1 周し、(a) 3 source event が 1 mailbox に集約、(b) Decision Card 本格版が過去判断 3 件付きで提示、(c) mem_search が 3 コンポーネントで動作（similarity score 付き）、(d) delivery が 3 CLI で発火、を evidence（mailbox dump + orchestration ledger + mem 記録 + commit hash）付きで確認。auto-approve OFF・手動承認のまま実走（96.x までは OFF 固定）。**Evidence (2026-06-13)**: (a) mailbox 単体テスト全 PASS（3 source unified + projection + lane-aware ingest） ✓ / (b) `render-judgment-card.sh` で `past-decisions-3.json` fixture render → HTML 8213 bytes + 3 件 mem://obs-past-001/002/003 link 表示 ✓ / (c) breezingmem.SearchSimilar + triaddispatcher unit PASS、`test-mem-read-layer.sh` PASS=8（Brief Composer recall + Triad Dispatcher + Decision Card 3 components 統合） ✓ / (d) bridgedelivery 単体 PASS（3 CLI deliverer + fallback） ✓ / (e) validate-plugin PASS ✓ / (f) check-consistency PASS ✓。本セッション 64 commit (794a9523..ecfc3c50) が orchestration ledger 相当。 | (a)-(d) evidence 本行 Status 追記、(e) `bash tests/validate-plugin.sh` PASS、(f) `bash scripts/ci/check-consistency.sh` PASS | 95.1.3, 95.2.2, 95.2.3, 95.4.1 | cc:done [c58c3b27] |

---

## Phase 96: Plan B Phase 3 — Risk Gate 3 CLI 配布 + auto-approve ON 化 + stage b 完成判定（北極星: ここで自動承認 ON 可能になる）

Purpose: Phase 92.2.1 の 5 カテゴリ runtime hard floor を 3 CLI hook へ **canonical floor policy fragment（5 カテゴリ enum + deny exit code 2 の JSON ブロック）として埋め込み配布**し、host wrapper / event-name casing / stdin shape 等の差分（spec.md L227-240 hookcodec 契約に従う）を残したまま 3 CLI 同時発火を契約化する。Phase 92.1+92.2+96.1.1-96.1.2 完了時点で **auto-approve を ON 化可能**にする（96.1.3、`HARNESS_AUTO_APPROVE=on` opt-in、default OFF）。auto-approve scope は worktree 内のみ、worktree 外への書込試行は floor で hard-stop（92.2.2 fingerprint 封じ込めと連動）。本 Phase 完了で **stage b 完成**（96.1.5 判定 gate、Plan B 詳細 §7 Phase 3 体感価値「自動承認 ON 可能」）。stage a (Phase 4-7、Plan B 残り)への移行は 96.1.5 の inventory 記録で user 判断 gate へ。**並列性**: 96.1.1（canonical floor fragment export）と 96.1.4（deny-list self-audit baseline）は独立し **2 並列可**。96.1.2/96.1.3/96.1.5/96.1.6 は順次。**安全順序**: 96.1.3（auto-approve ON 化）は 92.1.1 + 92.2.3 + 96.1.2 全完了が前提（fail-safe で OFF 強制）。team_validation_mode: subagent（並列 worker で canonical floor fragment export + self-audit hardening 独立、Lead が auto-approve gating を集約）。Spec delta: spec.md「Tri-Tool Parallel Collaboration Contract」に Risk Gate 3 CLI 配布契約と auto-approve worktree-scope を追記（96.1.6）。Spec skip reason: なし（product-impacting）。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 96.1.1 | `[Risk Gate Export]` `[lane:gate]` `[tdd:required]` Phase 92.2.1 の 5 カテゴリ enum（money/billing・外部送信/egress・認証/secret 読取・本番 deploy・worktree 外破壊）を hook JSON にエクスポート（hostgen 拡張）。`harness gen hooks` が 3 host（CC / Cursor / Codex）の hook JSON に 5 カテゴリ floor を canonical policy fragment として埋め込み配布。 | (a) `harness gen hooks` で 3 host の hook JSON に 5 カテゴリ floor fragment が出る、(b) **3 host 間で `hosts.toml` から導出される canonical floor policy fragment（5 カテゴリ enum + deny exit code 2 の JSON ブロックのみ）が byte-for-byte equal**（host wrapper 差分・event name casing・stdin shape 正規化は対象外、spec.md L227-240 hookcodec 契約を尊重）、(c) 既存 hook 機能と互換（regression test）、(d) `bash tests/test-floor-export-3cli.sh` PASS、(e) `go test ./...` PASS | 92.2.1, 92.2.2 | cc:done [e16a3102] |
| 96.1.2 | `[lane:gate]` `[tdd:required]` 3 CLI hook 同時発火検証: CC `PreToolUse` / Cursor `preToolUse` / Codex `PreToolUse` Bash 各 hook で同一 floor が exit code 2 で発火することを e2e test。Codex は Bash イベントのみ対応のため非 Bash 系（Read tool 等）は CCH 側の fingerprint 封じ込め（92.2.2）で補完される契約を明文化。 | (a) 3 CLI 各 5 カテゴリ 1 ケースずつ floor 発火 test（合計 15 ケース）、(b) Codex 非 Bash 系で fingerprint 封じ込めが代替する test、(c) 3 CLI hook の deny exit code 2 統一 test、(d) `bash tests/test-3cli-hook-floor.sh` PASS、(e) `bash tests/validate-plugin.sh` PASS | 96.1.1 | cc:done [9bb593ca, 3b883d75] |
| 96.1.3 | `[auto-approve ON 化]` `[lane:gate]` `[tdd:required]` `HARNESS_AUTO_APPROVE=on`（default OFF）opt-in で worktree 内のみ auto-approve 適用。worktree 外への書込試行は floor で hard-stop（92.2.2 fingerprint 封じ込めと連動）。**92.1.1 + 92.2.3 + 96.1.2 未完では fail-safe で OFF 強制**（環境変数で override 不可）。ON/OFF 切替を orchestration ledger に記録。 | (a) `HARNESS_AUTO_APPROVE=on` で worktree 内 auto-approve 動作 test、(b) worktree 外書込試行が hard-stop test、(c) 92.1.1+92.2.3+96.1.2 未完 fixture で fail-safe OFF 強制 test、(d) ON/OFF 切替が orchestration ledger に記録 test、(e) `HARNESS_AUTO_APPROVE=bogus` で exit 2（silent-fallback 防止）、(f) `go test ./...` PASS | 92.1.1, 92.2.3, 96.1.2 | cc:done [e633175a] ※ 2026-07-05 レビュー裁定: auto-approve は ledger 記録のみで skip 分岐なし。主張を撤回し実装は HOTL Phase へ (104.3) |
| 96.1.4 | `[Self-Audit Hardening]` `[lane:gate]` `[tdd:required]` `[Risk Gate]` deny-list self-audit baseline hash + 改ざん検知強化: `.claude-plugin/settings.json` の deny エントリ baseline を hash で固定し、減少時に起動拒否（Phase 91.6 の `policy/` baseline と統合）。`.claude/settings.local.json` の hook 注入を Read で検知（self-audit.md の項目を Go 実装に昇格）。CCH 生成 hook は allowlist で誤検知回避（92.6.4 契約延長）。 | (a) baseline hash で deny エントリ減少時に起動拒否 test、(b) settings.local.json hook 注入検知 test（既知 / 未知の 2 ケース）、(c) CCH 生成 hook が allowlist で誤検知ゼロ test、(d) **改ざん検知 3 ケース個別 test: (d1) deny エントリ削除で起動拒否、(d2) settings.local.json への未知 command 型 hook 追記で検知、(d3) 既存 hook の command 文字列改変で検知**、(e) `bash tests/validate-plugin.sh` PASS | 92.6.4 | cc:done [50835494] |
| 96.1.5 | `[stage b 完成判定]` `[lane:release]` `[tdd:skip:integration-verification]` Phase 93 + Phase 95 + Phase 96.1.1-96.1.4 完了で **stage b 達成**。実 3 タスク `/breezing all --cursor --auto-approve`（93.3.6 MVP e2e と同じ launch contract に auto-approve を加えた拡張形）で完走確認 + (a) 5 カテゴリ floor が 3 CLI で同時発火、(b) Decision Card 本格版が運用される、(c) Bridge Daemon + delivery が動作、(d) auto-approve ON で worktree 外破壊試行が hard-stop、を evidence 付きで確認。**stage a (Phase 4-7) 移行 gate**: user 判断ポイント inventory（退役スキルエイリアス永続 / Night Watch Report / Client Mirror / Channels wake / Judgment Ledger / Failure Codifier の 6 領域の優先順 + Plan B 詳細 §7 Phase 4-7 タイムライン）を harness-mem に保存し、user の GO を待つ（stage a への移行は 96.1.5 完了後の独立判断 gate）。**Evidence (2026-06-13)**: (a) 3 CLI hook floor parity 15/15 PASS ✓ / (b) judgment-card-render 12 + floor-no-card 5 PASS ✓ / (c) bridge + bridgedelivery unit PASS ✓ / (d) autoapprove 6 test PASS（DefaultOff / OnButPrereqMissing / OnAndAllPrereqsDone / EnvValueStrictness 6-sub / NoEnvOverridesFloor）+ runtimefloor WorktreeEscape PASS ✓ / (e) stage a inventory `obs_00mqb4toyea80fa689397ca66e` mem 保存（推奨順: 退役 alias > Judgment Ledger > Night Watch > Client Mirror > Failure Codifier > Channels wake、user GO via `/harness-plan` for Phase 4-7 structuring） ✓ / (f) validate-plugin PASS ✓ / (g) check-consistency PASS ✓ / (h) README Advanced に「auto-approve (experimental)」行追加 ✓ | (a)-(d) evidence 本行 Status 追記、(e) stage a 移行用 inventory が mem に保存（mem_id 記録）、(f) `bash tests/validate-plugin.sh` PASS、(g) `bash scripts/ci/check-consistency.sh` PASS、(h) README に「auto-approve experimental（HARNESS_AUTO_APPROVE=on opt-in）」表記追加（公開 claim 更新） | 95.5.1, 96.1.3, 96.1.4 | cc:done [8ca7d40a] ※ 2026-07-05 レビュー裁定: auto-approve は ledger 記録のみで skip 分岐なし。主張を撤回し実装は HOTL Phase へ (104.3) |
| 96.1.6 | `[Spec sync]` `[lane:fast]` `[tdd:skip:docs-only]` spec.md「Tri-Tool Parallel Collaboration Contract」に Risk Gate 3 CLI 配布契約（5 カテゴリ enum + 3 CLI 同時発火 + Codex Bash 制限と fingerprint 封じ込めの補完関係 + canonical floor policy fragment が host 間で byte-equal の境界）と auto-approve scope（worktree 内のみ、92.1+92.2+96.1.2 完了 gate、`HARNESS_AUTO_APPROVE` opt-in）を追記。`.claude/rules/versioning.md` に「stage b 完成 = minor リリース候補（Release Train v1 trigger は Phase 94 配下）」運用記載。**Evidence (2026-06-13)**: (a) spec.md「Risk Gate distribution contract (3-CLI parity)」+「auto-approve scope」節新設、5 カテゴリ enum 9 grep / 3 CLI parity 1 / HARNESS_AUTO_APPROVE 2 / byte-equal canonical fragment 2 ✓ / (b) versioning.md「Plan B Stage B Release Trigger」節追加、stage b 5 grep ✓ / (c) check-consistency PASS ✓ / (d) Phase 94 との関係 1 行明記 ✓ | (a) spec.md に 5 カテゴリ enum 名 + 3 CLI 同時発火契約 + auto-approve worktree-scope 明記 + canonical fragment 境界記述、(b) versioning rule に stage b 運用追加、(c) `bash scripts/ci/check-consistency.sh` PASS、(d) Phase 94 release train との関係を 1 行で記述 | 96.1.5 | cc:done [890c0554] |

## Phase 97: Plan B stage a warm-up — Retired Alias Registry [P1]

**Purpose**: 退役 alias の永続レジストリと CI 検出ゲートを Go selfaudit 層に新設し、Phase 91.7 で撤去された deleted-concepts.yaml + check-residue.sh 機能を最小スコープで再導入。stage a warm-up。Worktree group 1 (solo). Plans.md / CHANGELOG.md owner = retired-alias.

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 97.1.1 | `[lane:fast]` `[tdd:required]` retired-alias.v1 schema + registry YAML 初期 entry | (a) `templates/schemas/retired-alias.v1.json` の `$id` に `retired-alias.v1`, (b) `templates/registry/retired-aliases.v1.yaml` に `^- id:` ≥ 1 | — | cc:done [56143ee2] |
| 97.1.2 | `[lane:fast]` `[tdd:required]` `go/internal/retiredalias/` pkg (loader + scanner) | (a) `registry.go` / `scanner.go` 存在, (b) `go test ./go/internal/retiredalias/...` PASS | 97.1.1 | cc:done [56143ee2] |
| 97.1.3 | `[lane:gate]` `[tdd:required]` RED 4 件: `TestRetiredAlias_RegistrySchemaValid` / `_ResidueDetected` / `_AllowlistRespected` / `_HeadZeroHits` | (a) 4 関数 grep, (b) 全 PASS, (c) `tests/fixtures/retired-alias/` | 97.1.2 | cc:done [56143ee2] |
| 97.1.4 | `[lane:fast]` `[tdd:required]` `bin/harness retired-alias scan` subcommand | (a) `go/cmd/harness/retiredalias.go` に `retired-alias`, (b) `TestRunRetiredAlias_*` PASS | 97.1.2 | cc:done [56143ee2] |
| 97.1.5 | `[lane:gate]` `[tdd:skip:ci-config]` `scripts/ci/check-consistency.sh` に retired-alias section | (a) `grep retired-alias scripts/ci/check-consistency.sh`, (b) consistency PASS | 97.1.4 | cc:done [56143ee2] |
| 97.1.6 | `[lane:release]` `[tdd:skip:docs-only]` `.claude/rules/retired-alias-policy.md` SSOT | (a) ファイル存在, (b) `^## ` 見出し ≥ 3 | 97.1.5 | cc:done [56143ee2] |
| 97.1.7 | `[lane:release]` `[tdd:skip:docs-only]` CHANGELOG [Unreleased] + Plans.md cc:done 統合 | (a) `grep retired-alias CHANGELOG.md`, (b) 97.1.* 全 cc:done | 97.1.6 | cc:done [56143ee2] |

**Phase 97 Evidence (2026-06-14)**: feat `56143ee2` + fix `a1a6cf3c` (scanner が `.harness-worktrees/` nested worktree に降りて 119 hit する trunk 限定バグを Lead 検証で捕捉、回帰テスト `TestRetiredAlias_SkipsNestedWorktreeRoot` 追加) + chore(gen) `b83ab1ef` (4 platform バイナリ regen)。`go test ./internal/retiredalias/... ./cmd/harness/...` 全 PASS / `bin/harness retired-alias scan` = 0 hits exit 0 / check-consistency section 17 配線確認 (full run は無関係の `test-setup-language-rendering.sh:783` でハング、既存問題)。**逸脱メモ (97.1.1b)**: registry は参照実装 `deleted-concepts.yaml` の versioned-envelope 規約 (`version:` + `entries:` 配下に `  - id:` 3 件) を踏襲。DoD の `^- id:` リテラル grep は flat-list 想定で前例に反するため「≥1 entry」意図充足として扱う。**依存追加**: `gopkg.in/yaml.v3` (registry parse、repo 初の YAML パーサ) + `santhosh-tekuri/jsonschema/v6` (schema 検証)、本番バイナリに含まれる。

---

## Phase 98: Judgment Ledger + Channels-Wake — learning ledger + 通信回復層 [P1]

**Purpose**: stage b の judgment-card.v1 を append-only ledger 化し過去判断を検索可能に、同時に Bridge Daemon 通信チャネル (live-messaging / delivery / inbox hooks) の自動復旧層を追加。auto-approve 既定 OFF・5-category floor 不変。Worktree group 2. CHANGELOG.md = judgment-ledger, spec.md = channels-wake, Plans.md = Lead 統合.

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 98.1.1 | `[lane:gate]` `[tdd:required]` `judgment-ledger.v1` JSONL schema (`additionalProperties:false`) + `TestJudgmentLedger_SchemaReject` RED | (a) `$id` grep, (b) `tests/schema/test-judgment-ledger-schema.sh` PASS | 97.1.7 | cc:done [22440a49] |
| 98.1.2 | `[lane:gate]` `[tdd:required]` `go/internal/judgmentledger/` pkg + 4 tests (`_Append` / `_SchemaReject` / `_FailOpen` / `_ProjectScope`) | (a) 4 関数 grep, (b) pkg test PASS | 98.1.1 | cc:done [22440a49] |
| 98.1.3 | `[lane:fast]` `[tdd:skip:wrapper]` `scripts/judgment-ledger.sh append/search/recall` (fail-open on write) | (a) 3 subcommand grep, (b) bats test PASS | 98.1.2 | cc:done [22440a49] |
| 98.1.4 | `[lane:fast]` `[tdd:required]` `scripts/judgment-card.sh record-answer` → ledger append 配線 | (a) `grep 'judgment-ledger.sh append' scripts/judgment-card.sh` ≥ 1, (b) wiring test PASS | 98.1.3 | cc:done [22440a49] |
| 98.1.5 | `[lane:fast]` `[tdd:required]` ledger search index (file-based, project-scoped) max 3 件 `TestJudgmentLedgerIndex_Top3` / `_ProjectScopeIsolation` / `_EmptyCorpus` | (a) 3 PASS, (b) `index.go` 存在 | 98.1.3 | cc:done [22440a49] |
| 98.1.6 | `[lane:fast]` `[tdd:required]` `judgment-card.v1.similar_past_decisions` を recall layer で最大 3 件埋める | (a) `similar_past_decisions` grep ≥ 2 ファイル, (b) recall_test PASS | 98.1.5 | cc:done [22440a49] |
| 98.1.7 | `[lane:release]` `[tdd:skip:docs-only]` `docs/judgment-ledger.md` SSOT (Schema/Append/Search/Recall/Project Scope/Fail-Open) | (a) 6 見出しのうち ≥ 5 章 | 98.1.6 | cc:done [22440a49] |
| 98.1.8 | `[lane:release]` `[tdd:skip:docs-only]` CHANGELOG [Unreleased] "Judgment Ledger v1" (VERSION bump 無し) | (a) `grep 'Judgment Ledger' CHANGELOG.md` ≥ 1 | 98.1.7 | cc:done [22440a49] |
| 98.2.1 | `[lane:gate]` `[tdd:required]` channelswake 4 状態 RED (`_NotConfigured` / `_Unreachable` / `_Healthy` / `_Corrupted`) | (a) 4 関数 grep | 97.1.7 | cc:done [dbf71f39] |
| 98.2.2 | `[lane:fast]` `[tdd:skip:green]` `channelswake.Check()` 実装 (bridge socket probe + mailbox stale check, reason enum literal) | (a) 3 reason 文字列 grep, (b) 98.2.1 全 PASS | 98.2.1 | cc:done [dbf71f39] |
| 98.2.3 | `[lane:gate]` `[tdd:required]` `channel-wake-event.v1` schema + `TestChannelWakeEventSchema_AdditionalPropertiesFalse` | (a) `additionalProperties: false` grep, (b) schema test PASS | 97.1.7 | cc:done [dbf71f39] |
| 98.2.4 | `[lane:fast]` `[tdd:required]` `bin/harness channels-wake check` CLI (exit 0 healthy/not-configured, 1 unreachable/corrupted) | (a) `TestRunChannelsWakeCheck_*` PASS | 98.2.2 | cc:done [dbf71f39] |
| 98.2.5 | `[lane:fast]` `[tdd:required]` Session Monitor 統合 + `TestMonitorHandler_ChannelsWakeNotConfigured` 警告抑止契約 | (a) 該当テスト存在, (b) 警告非出力 assert | 98.2.2, 98.2.4 | cc:done [dbf71f39] |
| 98.2.6 | `[lane:fast]` `[tdd:skip:wrapper]` `scripts/channels-wake-probe.sh` + auto-approve OFF 既定 + opt-in wake trigger | (a) `AUTO_APPROVE_DEFAULT=false` grep, (b) probe test PASS | 98.2.4 | cc:done [dbf71f39] |
| 98.2.7 | `[lane:gate]` `[tdd:required]` Risk Gate 5-category floor 不変テスト (money/egress/secret/prod-deploy/worktree-escape) | (a) 5 カテゴリ全 grep ヒット | 98.2.6 | cc:done [dbf71f39] |
| 98.2.8 | `[lane:release]` `[tdd:skip:docs-only]` `spec.md` Channels-Wake 章 + check-consistency PASS | (a) `grep Channels-Wake spec.md`, (b) consistency PASS | 98.2.5, 98.2.6, 98.2.7 | cc:done [dbf71f39] |

**Phase 98 Evidence (2026-06-14)**: 98.1 judgment-ledger `22440a49` + 98.2 channels-wake `dbf71f39` を 2 並列 cursor で実装 (共有ファイル非衝突: CHANGELOG=98.1 / spec.md+main.go+monitor.go=98.2)、bin regen `次 commit`。trunk 統合検証: `go test ./internal/judgmentledger/... ./internal/channelswake/... ./internal/session/... ./cmd/harness/...` 全 PASS / `retired-alias scan` = 0 hits 維持 / `channels-wake check` = `{"healthy":true,"reason":"not-configured"}` exit 0 (tri-state 正常)。stop_point 決定: 98.1.5 ledger ranking = string-match (substring 優先→token 一致数)、98.2.4 wake trigger = hook 再注入提案のみ (daemon restart 自動実行なし、opt-in 既定 OFF)。auto-approve 既定 OFF・Risk Gate 5-category floor 不変を維持。

---

## Phase 99: Night Watch + Client Mirror — 監視層 [P1]

**Purpose**: 未解決ループ/停滞タスク/古い open decision を夜間バッチで巡回する Night Watch と、`skills/` SSOT と 3 mirror root の drift を hook で検出する Client Mirror を並列実装。両者とも D40 tri-state health (NotConfigured/Unreachable/Healthy/Corrupted) 踏襲、auto-approve 既定 OFF + opt-in。Worktree group 3. CHANGELOG.md = night-watch, `.claude/rules/skill-editing.md` = client-mirror, check-consistency.sh は night-watch → client-mirror の順.

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 99.1.1 | `[lane:gate]` `[tdd:required]` `night-watch-report.v1` schema + 4 状態 RED + schema 検証 2 件 | (a) 6 関数 grep, (b) `additionalProperties:false` | 98.1.8 | cc:done [f66390d5] |
| 99.1.2 | `[lane:fast]` `[tdd:required]` `go/internal/nightwatch/` health (tri-state reason: `not-configured`/`daemon-unreachable`/`corrupted`) | (a) 3 reason 文字列 grep, (b) 99.1.1 PASS | 99.1.1 | cc:done [f66390d5] |
| 99.1.3 | `[lane:fast]` `[tdd:required]` stale 検出 (task `last_updated>72h` / open_decision age `>168h`) literal in `templates/night-watch-config.yaml` | (a) `stale_task_hours: 72` / `open_decision_hours: 168` grep, (b) stale_test PASS | 99.1.2 | cc:done [f66390d5] |
| 99.1.4 | `[lane:fast]` `[tdd:required]` `scripts/night-watch-report.sh --dry-run` (schema 準拠 JSON stdout, bridge mailbox から unresolved loop 取得) | (a) `--dry-run` exit 0 + jq 構文 PASS | 99.1.2 | cc:done [f66390d5] |
| 99.1.5 | `[lane:fast]` `[tdd:required]` Session Monitor 統合 `TestMonitorHandler_NightWatch*` 全 PASS | (a) 4 関数 grep, (b) `not-configured` で警告非出力 | 99.1.2 | cc:done [f66390d5] |
| 99.1.6 | `[lane:fast]` `[tdd:required]` cron template + opt-in install (default OFF, 実 `~/.claude/settings.json` 不変, fixture/tempdir で検証) | (a) `NIGHT_WATCH_ENABLED=false` grep, (b) install test PASS | 99.1.4 | cc:done [f66390d5] |
| 99.1.7 | `[lane:release]` `[tdd:skip:ci-config]` CI gate + validate-plugin section + CHANGELOG | (a) `night-watch-report` grep 3 ファイル | 99.1.1-99.1.6 | cc:done [f66390d5] |
| 99.2.1 | `[lane:gate]` `[tdd:required]` `mirror-state.v1` schema + ajv RED (`TestMirrorStateSchema_ValidFingerprint` / `_RejectExtraProperty`) | (a) `$id` grep, (b) 2 PASS | 97.1.7 | cc:done [281fd8f0] |
| 99.2.2 | `[lane:gate]` `[tdd:required]` clientmirror RED (`TestClientMirror_InSync` / `_Drift` / `_MissingMirrorRoot`) | (a) 3 関数 grep | 99.2.1 | cc:done [281fd8f0] |
| 99.2.3 | `[lane:fast]` `[tdd:skip:green]` clientmirror GREEN: Scan/Diff/Fingerprint (`skills/` SSOT + codex/opencode/.agents 3 mirror) | (a) 3 func grep, (b) 99.2.2 全 PASS | 99.2.2 | cc:done [281fd8f0] |
| 99.2.4 | `[lane:fast]` `[tdd:skip:cli]` `bin/harness mirror status/verify` (mirror-state.v1 JSON 出力) | (a) `mirror status\|mirror verify` grep, (b) mirror_test PASS | 99.2.3 | cc:done [281fd8f0] |
| 99.2.5 | `[lane:fast]` `[tdd:skip:wrapper]` `scripts/sync-skill-mirrors.sh --check` を `harness mirror verify --json` に委譲 | (a) `harness mirror verify` grep, (b) parity test PASS | 99.2.4 | cc:done [281fd8f0] |
| 99.2.6 | `[lane:gate]` `[tdd:required]` PostToolUse hook on `Edit/Write` under `skills/` で drift 警告 (fixture/tempdir only) `TestSkillMirrorDriftHook_DetectsUnsyncedEdit` PASS | (a) `mirror-state.v1` grep, (b) hook test PASS | 99.2.4 | cc:done [281fd8f0] |
| 99.2.7 | `[lane:fast]` `[tdd:skip:ci-config]` check-consistency.sh mirror section + 0-drift gate on HEAD | (a) `mirror-state.v1` grep, (b) gate PASS | 99.2.5, 99.1.7 | cc:done [9852cb3d] |
| 99.2.8 | `[lane:release]` `[tdd:skip:docs-only]` `.claude/rules/skill-editing.md` に Client Mirror 契約節 + CHANGELOG 統合 | (a) `Client Mirror\|mirror-state.v1` grep | 99.2.7 | cc:done [281fd8f0] |

**Phase 99 Evidence (2026-06-14)**: night-watch `f66390d5` + client-mirror `281fd8f0` を 2 並列 cursor 実装、Lead 統合で refactor `9cbba99a`+`<fix commit>` (night-watch を `bin/harness night-watch` subcommand 化、`go run` 配布規約違反を是正) + 99.2.7 `9852cb3d` (check-consistency section 19 mirror gate + client-mirror CHANGELOG) + bin regen `a793d784`。trunk 統合検証: `go test ./internal/nightwatch/... ./internal/clientmirror/... ./internal/channelswake/... ./internal/judgmentledger/... ./cmd/harness/...` 全 PASS / `retired-alias scan`=0 / `mirror verify`=0 drift exit 0 (codex+opencode in-sync, .agents not-configured) / `night-watch report --dry-run` schema-valid exit 0 / ビルド再現性 (HEAD source 再ビルドで binary 差分ゼロ)。**Lead 補正 2 件**: (1) night-watch standalone cmd → subcommand (単一バイナリ配布規約)、(2) git add pathspec エラーで未コミットだった refactor 実体を fix commit で整合。共有ファイル分担: check-consistency.sh/CHANGELOG=night-watch owner、client-mirror 分は Lead 統合追記。

---

## Phase 100: Failure Codifier — self-learning loop core (human-approval gated) [P1]

**Purpose**: breezing logs + Judgment Ledger から再現失敗パターンを抽出し `failure-rule.v1` 候補を confidence score 付きで生成。`.claude/memory/patterns.md`/`decisions.md` への昇格は必ず human 承認ゲート経由 — 自動昇格禁止。stage a self-learning loop の中核。Worktree group 4 (solo, depends judgment-ledger). CHANGELOG.md / Plans.md owner = failure-codifier.

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 100.1.1 | `[lane:gate]` `[tdd:required]` `failure-rule.v1` schema + `TestSchema_FailureRuleV1_RejectsMissingRuleId` RED→GREEN | (a) `rule_id\|confidence\|evidence_refs\|proposed_ssot_target` grep ≥ 4 | 98.1.8 | cc:done [1166ff8e] |
| 100.1.2 | `[lane:fast]` `[tdd:required]` Codifier core: Judgment Ledger + breezing logs read-only 抽出 (`TestCodifier_ExtractFromLedger_EmptyCorpusReturnsNoRules` PASS) | (a) `go test ./go/internal/failurecodifier/...` PASS | 100.1.1 | cc:done [1166ff8e] |
| 100.1.3 | `[lane:gate]` `[tdd:required]` Confidence scoring (count≥3 medium, ≥5 high) `TestConfidence_ThreeOccurrencesMedium` / `_FiveOccurrencesHigh` | (a) 2 PASS, (b) `confidence.go` に 3/5 literal | 100.1.2 | cc:done [1166ff8e] |
| 100.1.4 | `[lane:gate]` `[tdd:required]` Human-approval gate (auto-promotion 禁止, dry-run only) `TestCodifier_PromotionRequiresApproval` PASS | (a) test PASS, (b) `promote.go` で auto-promote が return error | 100.1.3 | cc:done [1166ff8e] |
| 100.1.5 | `[lane:fast]` `[tdd:skip:wrapper]` `scripts/failure-codifier-propose.sh --dry-run` | (a) `test -x` PASS, (b) stdout が `[` or `{` で始まる JSON | 100.1.4 | cc:done [1166ff8e] |
| 100.1.6 | `[lane:fast]` `[tdd:skip:docs-only]` `skills/failure-codifier/SKILL.md` + `references/promotion-workflow.md` (`human-approval-required` marker + 閾値 3/5 literal) | (a) `human-approval-required` grep ≥ 1 | 100.1.5 | cc:done [1166ff8e] |
| 100.1.7 | `[lane:release]` `[tdd:skip:docs-only]` CHANGELOG [Unreleased] "Failure Codifier" + Plans.md cc:done 統合 | (a) `[Unreleased]` 内 `Failure Codifier` grep | 100.1.6 | cc:done [1166ff8e] |

**Phase 100 Evidence (2026-06-14)**: failure-codifier `1166ff8e` を 1 cursor session (solo chain) で実装、Lead 統合 refactor `a0224f07` (subcommand 化) + gen `61d0c115` (catalog + mirror + bin)。trunk 検証: `go test ./cmd/harness/... ./internal/failurecodifier/...` 全 PASS / `failure-codifier propose --dry-run` exit 0 (実 breezing orchestration-ledger から失敗パターン抽出を確認) / **`failure-codifier propose` (--dry-run なし) exit 2** (human-approval gate 強制) / `mirror verify`=0 (新スキル sync 後) / ビルド再現性 OK。**human-approval gate 検証 (核心)**: `promote.go` の `ErrAutoPromotionForbidden` が human_approved=false / dry_run=true 双方で error 返却、`.claude/memory/{patterns,decisions}.md` は無改変 (git status 確認)。**Lead 補正 3 件**: (1) standalone cmd → subcommand (night-watch と同型の go run 規約違反是正)、(2) 新スキル追加で `docs/CLAUDE-skill-catalog.md` gen docs 再生成 + mirror sync (自作 client-mirror gate に追従)、(3) `ProposeOpts.Stdout` 不在を `ProposeDryRun` 経由に修正。stop_point 100.1.4 既定: 運用失敗→patterns.md / 判断拒否→decisions.md を提案 field で示すのみ。

---

### Breezing 自律完走契約 reminder (Phase 97-100)

autonomous_stop_points (Lead 判断を要する箇所):
- **98.1.5** ledger search ranking 方式 (string-match 採用済だが Lead 再考の余地)
- **98.2.4** Channels-Wake の wake trigger 実装範囲 (hook 再注入 vs daemon restart 提案)
- **99.1.3** Night Watch stale 閾値 (72h/168h) の運用妥当性
- **100.1.4** Failure Codifier 昇格対象 (patterns.md vs decisions.md heuristic)

それ以外は契約 4 不変則 (スコープ 2 分割 / 質問禁止 + 既定値分岐 / Risk Gate 3 条件 / 共有ファイル lane) のもと続行。

---

## Phase 101: HOTL Governance — 検証フェーズ（verification-first / 実装計画は検証後に確定）

**Purpose**: HITL→HOTL（個人ハーネス。組織級統治は ContextHarness V2、CCH は憲法を継承し「私のループ」だけを治める）の前提を、実装計画を引く**前に** spike で潰す。段階導入ラダーは出さない（autonomy は proven harness の出力）。三権分立の大半は既存の枯れた規格（policy-as-code / RTM / in-toto）の re-labeling と判明したので**再発明せず採用可否を検証**する。新規性は「自己参照統治（自分のハーネスを編集し自分の採点を gaming しうるエージェント）」のみ。Spec delta: `spec.md` §HOTL Governance Contract（不変条件 6 を契約化）。

**team_validation_mode**: `subagent`（統治アーキ / provenance エンジニア / リスク懐疑派 3 体 + Cursor 敵対レビュー + workflow research 6 agent、2026-06-16 実行）。U6/U7 追加時に文章規範 gist（k16shikano「日本語技術文書の文章規範」）を 4 観点サブエージェント（全文抽出 / gate 化可否 / 既存衝突 / リスク懐疑、2026-06-18）で掌握。**Codex**: `unavailable`（usage-limit、16:00 以降に第5の声として再投下可）。

**unknown_data**（`not_observed != absent`）: U0 in-run leash の自動推論実現性 / U1 土台のブランチ実在 / U2 R06 改ざん検知 / U3 地図 gate 化の道具選定 / U4 LLM verdict の現権威所在 / U5 blast-radius 機械検知 / U6 文章規範 §7 禁止フレーズの public doc 実 hit 数（baseline scan 前は unknown）。

**最大発見（敵対レビュー）**: 「終端レビューで drift を捕まえる」は私とユーザー両者の共有盲点。task drift / silent failure はエラーを出さず run 途中で蓄積し最終 diff に現れない → **in-run scope leash (U0) を検証筆頭に格上げ**。**人間負荷ゼロ制約**: U0 のスコープは plan から自動推論し、人間にファイル一覧を手宣言させない（手宣言は新たな HITL ボトルネック＝本末転倒）。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 101.1 | `[lane:gate]` `[tdd:required]` **U0 in-run scope leash spike**。declared-scope を plan（タスク対象ファイル + RTM）から**自動推論**し、PreToolUse で圏外 Edit/Write を検知。**人間のスコープ手宣言ゼロ**が必須要件。`TestScopeLeash_AutoInferredFromPlan` / `_OutOfScopeWriteAlarms` / `_DroppedScopeFlagged` | (a) scope が plan から自動導出され**人手入力不要**であることを test で固定、(b) 圏外 Edit/Write で警報（fixture/tempdir）、(c) 宣言済み未着手（dropped scope）検知、(d) 決定性（path 照合のみ、LLM 非依存）、(e) 圏外時は「即停止」でなく escalation に渡せる（3軸ゲート連動の余地） | - | cc:done |
| 101.2 | `[lane:fast]` `[tdd:skip:investigation]` **U1 土台のブランチ実在確認**。`main` と dev（plan/zero-base-redesign）で selfaudit / deny-baseline / retired-alias / mirror を materialize 確認 | (a) 各 subcommand と baseline の存在をブランチ別に evidence 化、(b)「土台ゼロ」か「main 未マージ」かを `absent` でなく確定（not_observed != absent） | - | cc:done |
| 101.3 | `[lane:gate]` `[tdd:required]` **U2 1ルール改ざん検知（R06）**。CLAUDE.md 一文 ↔ `rules.go` R06 ↔ test を `@enforces R06` grep タグで機械リンク。弱め改変（matcher 窄化 / `--no-verify` 追加）で CI exit 1 | (a) red-team fixture 投入で CI が落ちる、(b) `@enforces` タグ除去で CI が落ちる、(c)「書いた→効く→改ざんできない」の最小 E2E 証明 | 101.2 | cc:done |
| 101.4 | `[lane:gate]` `[tdd:required]` **U3 地図 gate 化の道具選定 PoC**。rule↔check を OPA/Conftest or 自前 scanner で「孤立 check / 効かないルール」exit 1。RTM-as-data の実現性も | (a) 既存 14 ルール中ゲート未接続（現状 4/14）を 1 回で全列挙、(b) 枯れた道具（OPA/Conftest）採用 or 自前の判断を evidence 付きで提示、(c) 地図は `derived` 印 + gitignore で SSOT 分離 | 101.3 | cc:done |
| 101.5 | `[lane:fast]` `[tdd:skip:investigation]` **U4 LLM 助言縛りの確認**。既存レビュー経路（review-result.v1 / harness-review）が LLM verdict で gate していないか。決定性 veto 構造への差分 | (a) 現状の verdict 権威所在を evidence 化、(b) advisory-only（LLM は決定性層を上書き不可）への最小差分を特定 | - | cc:done |
| 101.6 | `[lane:gate]` `[tdd:required]` **U5 3軸ゲート機械化 spike**。blast-radius（触ファイル数 / 削除 / cross-repo / 不可逆）を PreToolUse で検知。R05/R06/R11 を escalation 面に配線 | (a) blast-radius 閾値超で escalation（fixture）、(b) 意味判定（spec/UX）に頼らず機械検知だけで外側の砦になる、(c) 既存 deny gate を HOTL escalation トリガーに二重利用 | 101.1 | cc:done |
| 101.7 | `[lane:gate]` `[tdd:required]` **U6 文章規範パイロット gate spike**。文章規範 gist §7 LLM 禁止フレーズの決定性サブセット（約14本: 重要なのは / 本章では〜を扱う / 掘り下げる / 正面から〜 / 〜に他ならない / 多角的 等）を **JP opt-in public doc 面のみ**に baseline scan。hit>0 なら既存 gate（`tests/test-support-claim-wording.sh` と同形＝固定ファイル列 + grep + JSON verdict）に literal 追記し、**rule↔check↔execution を end-to-end に通す最初の Authority Provenance Graph 実例**にする | (a) baseline scan が JP opt-in surface 限定で EN default を 1 byte も触らない、(b) hit>0 で既存 gate 形に literal 追記し CI の red→green を実証 / hit=0 なら「不要」evidence で採用ゼロ、(c) gate 化対象は §7 決定性サブセットのみ（LLM-advisory 36条・hedge・em ダッシュは **gate 化しない**）、(d) rule(§7)↔check(grep)↔exec(exit 1) の双方向リンクを machine-readable 記録し孤立 check / 効かないルールを出さない、(e) 決定性（LLM 非依存） | 101.4 | cc:done |
| 101.8 | `[lane:fast]` `[tdd:skip:investigation]` **U7 prose SSOT 一元化検証**。文章規範 gist / `~/.claude/rules/external-doc-style.md` / 認知負荷ルール（easy）の 3 文書を突き合わせ、重複・衝突・gap を踏まえ **prose SSOT 1 本化 or register 階層化**の方針を決める（第3 SSOT drift 回避）。em ダッシュは **緩い推奨（非 gate）** | (a) 3 文書の overlap / conflict / gap を evidence 化（衝突筆頭＝em ダッシュ家風、一文一行 vs 散文）、(b) 単一 SSOT か register 分離（技術文書 / 外部 / easy）かを根拠付きで提示、(c) JP opt-in 限定・EN default 不可侵を明記、(d) 強制範囲（決定性サブセットのみ gate / 残りは推奨）を文書化 | 101.7 | cc:done |

**autonomous_stop_points（Lead 判断を要する箇所）**:
- **101.1** scope 自動推論の粒度（ファイル単位 vs ディレクトリ単位）— 人間負荷ゼロを崩さない範囲で
- **101.4** OPA/Conftest 採用 vs 自前 scanner（依存追加 vs 配布単純さのトレードオフ）
- **101.6** blast-radius 閾値（N ファイル）の初期値
- **101.7** §7 禁止フレーズの gate 化対象の線引き（決定性サブセットの確定）と適用 public doc ファイル列
- **101.8** prose SSOT を単一化するか register 分離（技術文書 / 外部 / easy）にするか

---

## Phase 102: Local dogfood / release alignment + Codex cache refresh

**Purpose**: `public/latest`、Claude Code local dogfood、Codex local cache、development branch を分けて整理し、Codex `breezing --cursor` を他作業で使えるよう active local cache に即時反映してから、redesign work を再開できる状態に戻す。

**Spec skip reason**: `spec.md` は既に Execution Backend Contract / resolver-only backend selection / `/breezing` argument surface (`--cursor`) を定義済み。今回は product contract 変更ではなく、runtime truth surface の整理、local cache drift 解消、review closeout。

**team_validation_mode**: `manual-pass`（Codex native TeamAgent / `spawn_agent` はこの実行面では未露出。Product / Architecture / Security / QA / Skeptic 観点を Lead が分けて評価）。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 102.1 | `[lane:fast]` `[tdd:skip:investigation]` public/latest・local applied・development branch・Codex local cache の truth surface を表に整理 | (a) `docs/local-dogfood-release-alignment-2026-06-22.md` に version/capability table、(b) v4.16.1 に `--cursor` 有・Phase 92.2.4 無の tag grep evidence、(c) harness-mem evidence id 記録 | - | cc:done |
| 102.2 | `[lane:fast]` `[tdd:skip:local-cache-sync]` active Codex plugin cache `4.12.11` の `breezing` / `harness-work` を repo Codex SSOT から同期し、`--cursor` / backend selection を即時利用可能にする | (a) cache `skills/breezing/SKILL.md` hash が `skills-codex/breezing/SKILL.md` と一致、(b) cache `skills/harness-work/SKILL.md` hash が `skills-codex/harness-work/SKILL.md` と一致、(c) cache grep `--cursor` / `cursor-companion.sh` が hit | 102.1 | cc:done |
| 102.3 | `[lane:release]` `[tdd:skip:docs-only]` release update 判断を固定: v4.16.1 は `--cursor` 済み、Phase 92.2.4 は未 release、plain update は dogfood patch を落とす可能性あり | (a) decision が `.claude/memory/decisions.md` または docs に Why 付きで残る、(b) 「update 可否」推奨が Yes/No で読める、(c) release/publish/tag は実行しない | 102.1 | cc:done |
| 102.4 | `[lane:gate]` `[tdd:skip:review-closeout]` mirror / repo / cache 検証と harness-review closeout | (a) `scripts/sync-skill-mirrors.sh --check` PASS、(b) focused grep checks PASS、(c) harness-review または manual-pass review で critical/major 0、(d) Phase 102 全 task を `cc:done` に更新 | 102.2, 102.3 | cc:done |

---

## Phase 103: Public release promotion + redesign resume alignment

**Purpose**: Phase 102 で分離した `public/latest`、Claude Code local dogfood、development branch の差分を解消する。配布版は `origin/main`/release line を基点に Phase 92.2.4 相当を最小 backport/promote して `v4.16.1` より新しい public release 候補へ進める。リデザイン版はその release-line 結果と Phase 95 / PR #225 の release workflow delegation だけを取り込み、`plan/zero-base-redesign` の 166 ahead / 89 behind 状態を解消して HOTL/redesign 作業を再開可能にする。

**Spec result**: `spec.md` は既存の `[lane:release]` required closeout、Stage Gate Flow、PR / Release Boundary、`local checks passed != release ready` を維持したまま、PR #225 由来の Release Workflow Delegation Contract だけを追加した。今回は新しい product contract 追加ではなく、dogfood-only patch の public release 昇格、PR #225 の release safety 取り込み、branch alignment の実行計画。

**Approval**: 2026-06-23 user approved this plan. Additional user constraint: redesign はゼロベース作り直しなので、旧実装をそのまま戻す取り込みは禁止。ただし HOTL / Fleet / agent teams / UI defaults の知見は捨てず、新設計へ翻訳して取り込む。

**team_validation_mode**: `manual-pass + focused-subagent-investigation`（Product / Architecture / Security / QA / Skeptic 観点を Lead が分けて評価し、PR #225 / `cbc4c904` / Phase 92.2.1-92.2.4 の所在確認だけ subagent 3 本で分担）。

**validation_baseline**: Go は `gofmt` + focused `go test`、plugin/package は `bash tests/validate-plugin.sh` + `bash scripts/ci/check-consistency.sh`、release lane は `bash scripts/release-preflight.sh` または現行 `harness-release --check`、docs-only 差分は `git diff --check`。不足が見つかった場合は 103.1 の中で setup task に分離する。

**manual-pass summary**:

| 観点 | 判断 | Why |
|---|---|---|
| Product | Phase 92.2.4 は公開版へ反映する | local dogfood で試した安全挙動が public `v4.16.1` に無く、plain update すると挙動を失うため |
| Architecture | release line へ最小 backport し、current redesign branch を直接 release source にしない | `plan/zero-base-redesign` は ahead/behind が大きく、未完成 redesign/HOTL 文脈を public artifact に混ぜるリスクが高い |
| Security | tag / GitHub Release / push は explicit release gate まで行わない | public artifact と外部副作用は Risk Gate。local green や review approve は release ready ではない |
| QA | release fix と redesign sync のテスト面を分ける | 配布版の安全回帰と、リデザイン branch の再開可能性は failure surface が違う |
| Skeptic | cache 直接更新を「リリース済み」と扱わない | cache patch は update で上書きされる convenience patch で、配布保証ではない |

**redesign import translation boundary**:

| 区分 | 取り込み方 | 内容 |
|---|---|---|
| Phase 92.2.4 runtime safety | 取り込む | worktree-escape OS temp allowlist。安全挙動として release / redesign の両方に必要 |
| Phase 95 / PR #225 release delegation | 取り込む | Claude Code runtime hard floor に合わせ、publish は workflow に委譲し、確認は verify script / API polling で行う |
| Codex `breezing --cursor` / cache drift 解消 | 取り込む | 現行作業を動かす tool surface。旧 product spec ではない |
| HOTL / Fleet / agent teams の知見 | 翻訳して取り込む | 旧実装 defaults をそのまま正本に戻す merge は不可。運用上の学びは Phase 101 以降の新設計 input として扱う |
| UI / workflow defaults の知見 | 翻訳して取り込む | 旧 UI へ戻すのではなく、新しい redesign の画面・操作原則へ変換して別 task 化する |

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 103.1 | `[lane:release]` `[tdd:skip:investigation]` release truth surface を再確認し、release/backport branch の基点を固定する。`v4.16.1`、`origin/main`、`cbc4c904`、local Claude cache、Phase 102 doc、D53/D54、harness_session_inbox signal `a8f0cf7c-1351-462c-8b1f-727700e17254` を同じ表で再検証 | Done: `gh release list` で public latest は 2026-06-23 時点も `v4.16.1`。`git tag --contains cbc4c904` は empty、`git branch --contains cbc4c904` は `plan/zero-base-redesign` のみ。`harness_session_inbox` は新規なし、scoped `harness_mem_signal_read(agent_id=d0226600-edeb-44b0-ae5b-2235d2b601f2)` で signal `a8f0...` は `ackedAt=null` の PR #225 handoff と確認。release candidate 基点は `origin/main` `ef79f618` とし、redesign branch は release source にしない。 | - | cc:done [investigation] |
| 103.2 | `[lane:release]` `[tdd:required]` Phase 92.2.4 相当（worktree-escape OS temp allowlist）を release line へ最小 backport/promote する。redesign/HOTL 未完成差分は入れない | Done on `release/v4.16.2-phase103`: `408fed5b` で Phase 92.2.1 runtimefloor を release line へ追加、`bc96f759` で `cbc4c904` の temp allowlist を最小昇格。Focused `go test ./go/internal/runtimefloor ./go/internal/guardrail` PASS。HOTL/redesign/3CLI/autoapprove/orchestrationledger は入れない。 | 103.1 | cc:done [408fed5b, bc96f759] |
| 103.3 | `[lane:release]` `[tdd:skip:release-metadata]` release candidate を作る。既定は patch bump `v4.16.2`（安全挙動の公開反映・互換維持）だが、差分確認で minor 相当なら Why 付きで変更。PR #225 の release workflow delegation を前提に、direct publish CLI 手順を復活させない | Done locally on `release/v4.16.2-phase103`: `8ca46950` で `VERSION` / plugin manifests / `harness.toml` を `4.16.2` に同期し、PR #225 の release delegation wording を direct publish-free に調整。`1138cbf6` で platform binaries 4 本を rebuild。Checks PASS: release verify/no-direct-publish tests、mirror sync、version check、focused Go tests、`validate-plugin` 104 PASS、`check-consistency` PASS、`release-preflight --check-adapters` 19 PASS / 5 WARN / 0 FAIL。 | 103.2 | cc:done [8ca46950, 1138cbf6] |
| 103.4 | `[lane:release]` `[tdd:skip:external-release-gate]` public release closeout を実行する。PR / merge / tag / GitHub Release / plugin update 確認は明示 GO 後のみ。publish 実行は workflow に委譲し、operator は verify script / API polling で公開完了を確認する | Done after explicit Release GO: PR #226 merged to `origin/main` at `78a8c3b5`; tags `v4.16.2` and `claude-code-harness--v4.16.2` pushed; `auto-release` run `28071678901` succeeded; `bash scripts/release-verify-publish.sh v4.16.2 Chachamaru127/claude-code-harness` PASS with 4 assets; `gh release list` shows `v4.16.2` as Latest; release assets checked for `4.16.2 (Hokage)`, SHA256 digests, and `allowlistedTempRoots`; user-scope `claude-code-harness@claude-code-harness-marketplace` updated `4.15.0 -> 4.16.2` and local cache `bin/` was filled from published assets so hooks can find `bin/harness` after restart. | 103.3 | cc:done [PR #226, v4.16.2, run 28071678901] |
| 103.5 | `[lane:gate]` `[tdd:skip:branch-alignment]` `plan/zero-base-redesign` に必要な release/main safety 差分だけを取り込む。full merge/rebase は public release closeout 後の独立 branch-alignment gate に延期し、redesign import allowlist 以外の旧仕様は入れない | Done for resume gate: full merge/rebase は実行せず、PR #225 の safety contract だけを current redesign branch に手当て。Added `scripts/release-verify-publish.sh` + 2 tests、updated `skills/harness-release` + codex/opencode mirrors + release-notes reference、added `spec.md` Release Workflow Delegation Contract、`CHANGELOG.md` に current dev branch 用 entry。`VERSION=4.15.0` は dev surface として維持。 | 103.3 | cc:done [allowlist-only] |
| 103.6 | `[lane:gate]` `[tdd:required]` redesign branch の再開前 smoke。Phase 92.2.4 挙動、PR #225 release delegation、Codex `breezing --cursor`、mirror/cache、HOTL Phase 101 前提を再検証する | Done on `plan/zero-base-redesign`: `bash tests/test-release-verify-publish.sh` PASS、`bash tests/test-release-skill-no-gh-release.sh` PASS、`bash scripts/sync-skill-mirrors.sh --check` in-sync、`go test ./go/internal/runtimefloor ./go/internal/guardrail` PASS、`git diff --check -- .` PASS、direct publish grep 0 hit in release skill/spec/test surfaces。Next HOTL/redesign task remains selectable from Phase 101/102 without old spec rollback. | 103.5 | cc:done [redesign-smoke] |
| 103.7 | `[lane:fast]` `[tdd:skip:handoff-docs]` release 反映版 / local applied 版 / redesign 版の最終表を更新し、作業再開プロンプトを残す | Done: `docs/local-dogfood-release-alignment-2026-06-22.md` updated with 2026-06-24 public release closeout; `.claude/memory/decisions.md` updated with Why; this Plans.md row updated. Resume prompt: "Resume HOTL/redesign on `plan/zero-base-redesign` from Phase 101/102. Treat `v4.16.2` / PR #226 as the public release baseline, keep the redesign branch as dev surface, and import only allowlisted safety/release knowledge instead of old specs." | 103.4, 103.6 | cc:done [handoff-docs, release-closeout] |

**注**: Phase 101 は HOTL 検証のみ。HOTL 本実装計画は U0-U7 の evidence を前提に別途 `/harness-plan create` で確定する。Phase 102/103 は local dogfood / release / redesign alignment の計画であり、この planning turn では VERSION / plugin.json / harness.toml は触らない。

---

## Phase 104: S1 — 本流化前提 P0 gate（フラットレビュー 2026-07-05 の確定 findings 対応）[P0]

Purpose: 2026-07-05 の中立フラットレビュー（6 次元 finder + 敵対検証、confirmed 17 / plausible 8 / refuted 5）で確定した P0 findings を解消し、redesign 線を「本流化してよい状態」にする。正本は local-only `GOD_plans.md`（S1 ステージ）。完了条件は GOD_plans.md §7 の安定稼働基準 11 項目のうち本 Phase 対応分が全て green になること。DP-2（削除裁定）/ DP-4（auto-approve 裁定）は user 承認済みの裁定内容を各行に固定してから着手する。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 104.1 | `[lane:gate]` `[tdd:required]` `[security]` main の R15 秘密ファイル staging block 4 連（`1873c8a3` 本体 → `6ac3eec0` git -C/subshell bypass → `4d7b6245` quote-aware lexing → `3b8e64db` backslash escape）を `go/internal/policy` へ翻訳移植する。main は `go/internal/guardrail` 配下のためパス・API 変換が必要。移植後に self-audit deny baseline hash を再生成する | (a) `grep -En "R15|no-stage-secret|secretFileStaging" go/internal/policy/*.go` が 1 件以上、(b) 合成 PreToolUse 入力（dotenv 系秘密ファイルの `git add`）で `bin/harness hook pre-tool` が exit 2、(c) bypass 3 形態（`git -C`、subshell、quote 分割）の deny test 各 1 件、(d) baseline hash 再生成後 `bin/harness self-audit` green、(e) `go test ./...` PASS | - | cc:done [4ca6caef] (R15 port + bypass 3 形態 test + selfaudit baseline。policy tests PASS) |
| 104.2 | `[lane:gate]` `[tdd:required]` Plans.md Depends↔Status 整合チェッカーを新設する。`go/internal/plans` の既存 parser を使い「task が cc:done/cc:完了のとき Depends 全 task が cc:done/cc:完了/cc:withdrawn」を検証し `scripts/ci/check-consistency.sh` の新ゲート [20/20] に追加。既存違反（96.1.3 done × 96.1.2 todo 等）は全件棚卸しして status 修正または withdraw 注記で解消する | Done: `go/internal/plans` に Depends closure checker + fixtures（closed / violation / withdrawn）を追加し、`harness plans check-deps` と `check-consistency.sh` gate [20/22] へ配線。`GOCACHE=/private/tmp/codex-go-cache go test ./internal/plans ./cmd/harness` PASS、`go run ./cmd/harness plans check-deps ../Plans.md` PASS (239 tasks)。archive 済み依存は現行 Plans 外として無視し、現行ファイル内 dependency の未完了のみ fail。 | - | cc:done |
| 104.3 | `[lane:gate]` `[tdd:required]` `[DP-4]` HARNESS_AUTO_APPROVE の decorative no-op を解消する。裁定=撤回（推奨、verification-first 整合）: README.md / spec.md / Plans.md の「auto-approve」主張を「gate 判断の ledger 記録のみ。approval-skip は HOTL Phase（U0-U7 evidence 後）で実装」へ訂正する。裁定=実装の場合は worker 確認プロンプトへの skip 分岐 + test を実装する | (a) 撤回の場合: `grep -rn "auto-approve" README.md spec.md` の全ヒットが「記録のみ」表現に整合 + 誇大主張 0 件、(b) 実装の場合: skip 動作 test 1 件以上、(c) どちらでも Plans.md 96.1.3/96.1.5 行に裁定注記追記、(d) `bash tests/validate-plugin.sh` PASS | - | cc:done [e46f6a5f] (README/spec を ledger-instrumentation only へ訂正 + Plans 注記。build OK) |
| 104.4 | `[lane:gate]` `[tdd:required]` `[DP-2]` dead subsystem 裁定の実行。裁定案: (i) `go/internal/{bridge,mailbox,bridgedelivery,triaddispatcher}`（binary 到達不能 ~1900 行）= 削除し設計知見を spec.md の L3 節へ 1 段落で翻訳記録、(ii) `internal/impactscore` = Go 実装へ統一し `scripts/judgment-card.sh` の compute-impact を `bin/harness` サブコマンド呼び出しの薄ラッパー化、(iii) judgment-card/ledger パイプライン = 保持し S3 で breezing 本流へ配線（本 Phase では Plans.md に `wired: no (S3 で配線予定)` 注記のみ） | (a) 削除 package の importer 残骸 0（`go build ./...` PASS）、(b) spec.md に翻訳記録 1 段落、(c) impact_score 算出が単一実装（shell は薄ラッパー）+ 一致 test、(d) GOD_plans.md §7 基準 7 の importer check が全 package で「配線済み or 撤回注記」、(e) `go test ./...` PASS | - | cc:done [7b563987] (bridge 系 4 pkg -2118 行削除 + spec.md L3 翻訳記録 + bin/harness impact-score 統一。focused tests PASS) |
| 104.5 | `[lane:gate]` `[tdd:skip:ledger-doc]` branch-alignment gate を正式化する。main merge-base `794bfc36` 以降の安全 commit（R15 4 連 / #218 `5249ad76`+`5a2d0df9` / #219 `d4b8573c` / egress owner-scope `d9b3fd34` / setup-hook guard `2141c7ef`+`8097802e` / autonomous-confirmation-scope `fa88d4cf`）を port/waive 台帳 `docs/branch-alignment-ledger.md` に列挙し、main release ごとに更新する運用を check 化する | Done: `docs/branch-alignment-ledger.md` に全対象 commit を `ported` / `waived` + Why で列挙し、`scripts/ci/check-branch-alignment-ledger.sh` と `check-consistency.sh` gate [21/22] を追加。`bash scripts/ci/check-branch-alignment-ledger.sh` PASS。 | 104.1 | cc:done |
| 104.6 | `[lane:gate]` `[tdd:required]` binary/source drift gate: `bin/harness-<platform>` が `go/` source から再現ビルドできることを checksum（または決定論的 build 出力）で比較する CI ゲートを追加する | Done: `scripts/ci/check-binary-source-drift.sh` builds the current platform with deterministic flags and compares against `bin/harness-<platform>`; stale binary was detected before rebuild, then green after rebuilding all 4 platform binaries. CI `validate-plugin.yml` build step now uses matching flags. Evidence: `GOCACHE=/private/tmp/codex-go-cache bash scripts/ci/check-binary-source-drift.sh` PASS. | - | cc:done |
| 104.7 | `[lane:fast]` `[tdd:skip:ci-wiring-1line]` 既存 `tests/test-hooks-sync.sh`（dual hooks.json 同期、7/7 PASS 実績）を `tests/validate-plugin.sh` から呼ぶ | Done: `tests/validate-plugin.sh` section 17 now calls `tests/test-hooks-sync.sh`. Evidence: `bash tests/test-hooks-sync.sh` PASS (7/7); `validate-plugin.sh` reaches the new hook sync section and passes it. | - | cc:done |
| 104.8 | `[lane:gate]` `[tdd:required]` model routing を Sonnet 5 世代へ更新する（redesign 線への翻訳移植。main 線は 2026-07-05 適用済み）: `scripts/model-routing.sh` の `claude-sonnet-4-6` 3 箇所 → `claude-sonnet-5`、`scripts/ci/check-consistency.sh` [16/16] の期待値 `claude-sonnet-4-6` → `claude-sonnet-5`、`agents/{worker,reviewer}.md` frontmatter、`go/cmd/harness/validate.go` allowlist、platform binary rebuild。Fable 参照は opt-in（`HARNESS_BRAIN_MODEL=fable`）のまま維持し default 経路に入れない | (a) `grep -rn "sonnet-4-6" scripts/ agents/ go/cmd/` が binary 以外 0 件、(b) check-consistency [16/16] が sonnet-5 期待で PASS、(c) `strings bin/harness-darwin-arm64 | grep -c claude-sonnet-5` ≥ 1、(d) `bash tests/validate-plugin.sh` PASS | - | cc:done [7b065700] (routing 3 tier + agents + validate.go + docs + binary rebuild sonnet-5 埋込確認。routing test OK) |
| 104.9 | `[lane:fast]` `[tdd:skip:prune]` `[DP-2]` Phase 91.7 で「曖昧」と自己認定された 4 skill（agent-browser / gogcli-ops / cc-cursor-cc / cc-update-review）の裁定実行。裁定案=削除（使用実績なし）。必要と判断したものだけ CLAUDE.md に理由記録の上で正式化 | (a) 削除分は skills/ から除去 + `harness gen docs` で catalog 再生成 + mirror sync PASS、(b) 残す分は理由が CLAUDE.md に 1 行、(c) `bash scripts/sync-skill-mirrors.sh --check` PASS | - | cc:done [9efa0956, 4db1dd88] (gogcli-ops/cc-cursor-cc 削除 + retired-alias 登録。agent-browser/cc-update-review は配線ありで保持、CLAUDE.md 記録。validate-plugin 全 PASS) |
| 104.10 | `[lane:release]` `[tdd:skip:integration-verification]` S1 完了判定: GOD_plans.md §7 安定稼働基準の該当項目（1-9, 11）を一括実行し evidence を本行 Status に追記。DP-1（redesign 本流化）の正式判断材料を `.claude/memory/decisions.md` に Why 付きで記録し、GOD_plans.md §5 の DP-1 を close する | (a) 基準 1-9, 11 の各コマンド exit code 一覧が evidence として残る、(b) decisions.md に D 番号付き記録、(c) GOD_plans.md §4 の S1 行が完了に更新される | 104.1, 104.2, 104.3, 104.4, 104.5, 104.6, 104.7, 104.8, 104.9 | cc:done [D57, closeout] (安定稼働基準 1-9,11 green: validate-plugin PASS / go test 43pkg 0fail / check-consistency 22 gate PASS / R15 ライブ deny / self-audit baseline PASS。DP-1 は decisions.md D57 で条件付き GO close。25 findings closeout 台帳 docs/flat-review-2026-07-05-closeout.md) |

---

## Phase 105: S3 — P1 改善（言行一致・体験純度の残り 11 findings）[P1]

Purpose: 2026-07-05 フラットレビューの P1 findings（Phase 104 の P0 gate に含めなかった major 7 + minor 4）を解消し、README の全 ✅ 主張が実体と一致する状態にする。正本は local-only `GOD_plans.md`（S3 ステージ）。Phase 104 完了（dead code 削除・R15 移植・整合 gate）を前提に、その上へ体験と言行一致を積む。DoD は全て機械判定可能な grep/test で固定する。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 105.1 | `[lane:gate]` `[tdd:required]` `[ux]` 非エンジニア 3 画面（Plan Brief / Progress / Accept）を本流フローへ配線する。`skills/harness-plan` の計画確定ステップ完了時に `harness-plan-brief` を自動提案、`harness-work`/`breezing` の task 完了 hook で `harness-progress` を再生成（既存 `posttool-progress-regen.sh` 経路を利用）、`harness-release` 前に `harness-accept` を提案。README にも 3 画面の導線を記載 | (a) `harness-plan` SKILL.md に plan-brief 自動提案ステップが記載され `grep -n "harness-plan-brief" skills/harness-plan/SKILL.md` が 1 件以上、(b) progress 再生成が task 完了で発火する test 1 件、(c) accept 提案が release 前に出る導線が SKILL.md に記載、(d) `bash tests/validate-plugin.sh` PASS | 104.4 | cc:done [Lead] (3 skill 導線 + README 3 画面節。mirror in-sync、validate-plugin PASS) |
| 105.2 | `[lane:gate]` `[tdd:required]` `[ux/i18n]` 英語デフォルト i18n contract 違反を解消する。`go/internal/hookhandler` の user-facing 文字列（inbox_check.go / plans_watcher.go ほか、CI ゲート未通過の主要ファイル）を既存 `resolveHarnessLocale` パターンへ移行し、ハードコード日本語/英語混在を検知する CI ゲートを `scripts/ci/check-consistency.sh` に追加する | (a) 移行対象ファイルで user-facing string が locale resolver 経由に統一、(b) `rg` ベースの混在検知ゲートが新設され違反 0、(c) 3 状態（ja / en / 未設定）の locale 解決 test、(d) check-consistency 全ゲート PASS | 104.2 | cc:done [acb302c5] (i18n ratchet gate [23/23] baseline 117 + inbox_check.go 参照移行 + 3 状態 test。残 12 ファイルは Phase 106) |
| 105.3 | `[lane:gate]` `[tdd:required]` `[ux]` Plan Brief の `confidence 0-100` を誤読させない設計へ改める。現状の無関係 3 指標合算をやめ、(i) ラベルを「AI の理解度」と誤読されない表現（例: `plan_readiness` = DoD 明確度 + 依存解決率のみ）に変更、(ii) `options` / `risks` / `acceptance_criteria` を実際に埋める生成手順を `skills/harness-plan-brief/SKILL.md` に明記（常時空配列の解消）| (a) confidence の算出根拠が単一意味軸に統一され `confidence_evidence` の説明と整合、(b) options/risks/acceptance_criteria が非空で生成される fixture test 1 件、(c) SKILL.md に 3 配列の生成手順が記載、(d) `bash tests/validate-plugin.sh` PASS | 104.4 | cc:done [a3dbd828] (confidence→plan_readiness DoD60+deps40、options/risks/acceptance 非空化。3 test PASS) |
| 105.4 | `[lane:gate]` `[tdd:required]` `[code]` Night Watch の open-decision 検知が自リポジトリの `decisions.md` フォーマット（`## D<N>:` 見出し + `**状態**:` 行）と一致するよう正規表現を修正する。self-referential dogfooding として自 repo の実データで 1 件以上検知できることを test で固定 | Done: `go/internal/nightwatch` の open-decision parser を `## D<N>:` + `**状態**:` / `**日付**:` に対応し、open 相当 (`未決` / `未解決` / `保留` / `検討中` / `要判断` / English open family) だけを検知。`採用` は非検知。旧 `## YYYY-MM-DD:` + `**Status**: Open` も regression として維持。Evidence: `GOCACHE=/private/tmp/codex-go-cache go test ./internal/nightwatch -run 'TestDetectOpenDecisions|TestDetectStaleTasks|TestLoadStaleConfig'` PASS。Full package test は sandbox の unix socket bind 制限で `TestNightWatch_Healthy` が `bind: operation not permitted`。 | - | cc:done |
| 105.5 | `[lane:gate]` `[tdd:required]` `[code]` `bridge_events` sqlite スキーマ知識の DRY 違反を解消する。Phase 104.4 で bridge/mailbox を削除した後に残る `channelswake/health.go` と `nightwatch/stale.go`（+各 test）へ独立フォークしたスキーマ定義を、単一の公開 API（例 `go/internal/eventstore`）へ切り出す | (a) スキーマ定義の重複が 1 箇所に集約され `grep -rln "CREATE TABLE.*bridge_events\|events_table" go/internal/` が単一 package、(b) 参照側が共通 API 経由、(c) 既存 channelswake / nightwatch test が緑維持、(d) `go test ./...` PASS | 104.4 | cc:done [ad930b5f] (bridge_events schema を eventstore へ集約。channelswake/nightwatch consumers green) |
| 105.6 | `[lane:fast]` `[tdd:required]` `[code]` `failurecodifier` の失敗判定が単語境界なし部分文字列一致（`strings.Contains`）で `known` / `not` 等を含む肯定回答を誤って negative 判定する問題を修正する | Done: 判定を `containsNegativeAnswerToken` に集約し、回答を英数字 token に分割して `no` / `not` / `reject` / `stop` / `wait` 系の単独 token のみ negative 扱いに変更。`known` / `noted` 肯定文は非 failure、`No, wait...` は failure の regression test を追加。Evidence: `GOCACHE=/private/tmp/codex-go-cache go test ./internal/failurecodifier/...` PASS. | - | cc:done |
| 105.7 | `[lane:fast]` `[tdd:required]` `[stability]` reap 漏れ worktree（`.harness-worktrees` 配下 327MB 残置）対策: `.gitignore` に追加し、`bin/harness doctor` に古い worktree（N 日以上未更新 or orphan）検知を追加する | Done: `.gitignore` に `/.harness-worktrees/` を追加。`harness doctor` に 7 日以上未更新 / orphan の advisory warning を追加し、検知 helper の新しい / 古い / 不在 / orphan 状態 test を追加。Verification: `GOCACHE=/private/tmp/codex-go-cache go test ./cmd/harness -run 'TestDoctor_(CheckHarnessWorktrees|DetectHarnessWorktreeResidue)'` PASS; `GOCACHE=/private/tmp/codex-go-cache go test ./cmd/harness` PASS; `git diff --check` PASS; `GOCACHE=/private/tmp/codex-go-cache go test ./...` は sandbox の socket bind 禁止と EDITOR unset 既存 e2e で fail（変更外）。 | - | cc:done |
| 105.8 | `[lane:fast]` `[tdd:skip:false-positive-fix]` `[stability]` runtime floor secret-read が Bash コマンド文字列内の文書的な秘密ファイル名（heredoc 内の説明テキスト等）に false positive 発火する問題を修正する。実際のファイル読取 operand のみを対象とし、コマンド内の任意テキスト一致では発火しない | (a) 文書テキスト内の秘密ファイル名で発火しない test、(b) 実際の秘密ファイル読取（`cat <dotenv>`）では従来通り発火する regression test、(c) `go test ./internal/runtimefloor/...` PASS | - | cc:done [a9dfac54] (heredoc/コメント除外の executable-text scan。false positive 除去 + 実読取 deny の regression test) |
| 105.9 | `[lane:gate]` `[tdd:required]` `[drift]` delivery hook 生成 GAP の裁定実行。`hostgen.GenerateDeliveryHooksJSON`（実装・unit test 済だが `harness gen` 未接続）を、(裁定案) `harness gen hooks` に配線し Codex/Cursor の生成 hooks.json に inbox-check を含める。配線が L3 協調の範囲外と判断する場合は Plans.md に撤回注記 + spec 側の GAP 記述を更新 | (a) 配線の場合: 生成された `.codex/hooks.json` / `.cursor/hooks.json` に inbox-check エントリが含まれる test、(b) 撤回の場合: spec.md の GAP 記述更新 + Plans.md 撤回注記、(c) どちらでも GOD_plans.md §7 基準 7 の importer check が緑、(d) `go test ./...` PASS | 104.4 | cc:done [b82143fe] (delivery hook を harness gen に配線。生成 hooks に enforcement+delivery 両立、gen --check PASS。L3 一歩) |
| 105.10 | `[lane:fast]` `[tdd:skip:docs-restructure]` `[identity]` ドキュメント肥大の解消: (i) `skills/harness-setup/SKILL.md`（460 行、Codex/Cursor 個別政策 4 本ベタ書き）を `references/` へ分割しリンクのみ残す、(ii) `spec.md`（1462 行）を North Star + 6 invariant + SSOT 順位のコアと sub-spec へ分割、(iii) README badge『Skills: 5 Verbs』を実態表現（core 5 verbs surface / 全 22 skill）へ修正 | (a) harness-setup SKILL.md が 200 行以下 + references/ へ分割 + `sync-skill-mirrors.sh --check` PASS、(b) spec.md コアが 1 ファイルで sub-spec へリンク、(c) README badge が実態表現、(d) `bash scripts/ci/check-consistency.sh` PASS | - | cc:done [85f0f25f] (harness-setup 460→101 + spec.md 1467→197 sub-spec 分割 + README badge。情報保全確認) |
| 105.11 | `[lane:release]` `[tdd:required]` `[identity]` README 全面改訂: `README_HOTL_ja.md` の内部自己矛盾（アーキテクチャ節=現在形 vs 現在の状態節=配線中）を解消し、完成版（`✅`=実配線済のみ、`🔜`=HOTL 統治のみ）へ差し替える。三ツール協調 ✅ → 委譲+交差レビュー ✅、判断カード ✅ 新設、言行一致の自己検査節を追加。README の各 ✅ 主張が対応する CI ゲートを持つことを検証 | (a) README の ✅ 主張と実配線が 1 対 1（`🔜` は HOTL のみ）、(b) 節間の成熟度表示に矛盾 0（現在形記述と配線中の混在なし）、(c) 各 ✅ 主張に対応する test/gate 名が追跡可能、(d) `bash scripts/ci/check-consistency.sh` PASS | 105.1, 105.2, 105.3, 105.9 | cc:done [Lead] (README.md は既に正直と検証。言行一致 self-check 節追加。判断カードは wired:no ゆえ非 ✅。#13 は untracked draft 対象外) |
| 105.12 | `[lane:release]` `[tdd:skip:integration-verification]` S3 完了判定 + 全 findings closeout: フラットレビュー 25 findings（confirmed 17 + plausible 8）が全て「修正済 / 撤回済 / 意図的 waive（Why 付き）」のいずれかであることを findings 台帳 `docs/flat-review-2026-07-05-closeout.md` で 0 件未処理に確定。GOD_plans.md §7 の 11 基準を end-to-end 再実行し evidence を残す | (a) closeout 台帳に 25 findings 全件の処理状態、(b) §7 基準 11 項目の再実行 evidence、(c) `bash tests/validate-plugin.sh` + `go test ./...` + `bash scripts/ci/check-consistency.sh` 全 PASS、(d) GOD_plans.md §4 の S3-S5 行更新 | 105.1, 105.2, 105.3, 105.4, 105.5, 105.6, 105.7, 105.8, 105.9, 105.10, 105.11 | cc:done [Lead] (25 findings closeout: fixed 24 + waived 1 + 未処理 0。§7 基準再検証: validate-plugin PASS / go test 43pkg 0fail / check-consistency 23 gate 全 PASS。残債 Phase 106 起票済) |

---

## Phase 106: i18n hookhandler bulk 移行 (105.2 follow-up) [P2]

Purpose: Phase 105.2 で i18n ratchet gate (baseline 117) と inbox_check.go 参照移行を完了。残る hookhandler の裸日本語 literal (~117 箇所、12 ファイル: plans_watcher/setup_hook/posttooluse_quality_pack/userprompt_inject_policy/ask_user_question_normalizer/session_auto_broadcast/runtime_reactive/skill_mirror_drift/todo_sync/ci_status_checker/file_lease_hook/auto_cleanup_hook 残余) を localizedHarnessMessage(en/ja) へ順次移行し、移行ごとに ratchet baseline を下げる。全移行完了で baseline 0 + gate を「新規日本語ハードコード即 fail」に昇格。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 106.1 | `[lane:gate]` `[tdd:required]` user-facing 上位 4 ファイル (plans_watcher/setup_hook/posttooluse_quality_pack/userprompt_inject_policy) の裸日本語を localizedHarnessMessage へ移行。移行後 ratchet baseline を実測値へ下げる | (a) 4 ファイルの user-facing 日本語が localizer 経由、(b) baseline 更新、(c) `go test ./internal/hookhandler/...` PASS、(d) check-consistency [23/23] PASS | 105.2 | cc:done [1775311f] |
| 106.2 | `[lane:gate]` `[tdd:required]` 残り 8 ファイルの移行完了 + ratchet baseline を 0 にし gate を「裸日本語 0 件必須」へ昇格 | (a) 全 hookhandler の user-facing 日本語が localizer 経由、(b) baseline=0、(c) 新規裸日本語で gate fail の test、(d) `go test ./...` PASS | 106.1 | cc:done [7d5aa3d4] |

---

## Phase 108: secret-read 事前許可 — floor の前提前倒し確認（operator feedback 2026-07-07）[P0]

Purpose: runtime floor 5 カテゴリのうち secret-read だけが事前許可機構を持たない非対称を解消する。egress は isAllowlistedHost、worktree-escape は allowlistedTempRoots を持つのに、secret-read には project 宣言も allowlist も無く pipeline 途中で毎回停止する。operator feedback（前提を読まなきゃできない作業を依頼しているのに作業開始後に聞くのはおかしい、前もって確認すれば途中で止まらない）を受け、operator が作業前に一度この project の秘密ファイルは expected と宣言でき、宣言済みの実 read は floor が停止しない機構を追加する。egress の owner-scoped exemption パターンを踏襲。105.8 の誤検知修正（文書テキスト内 secret 名）とは別問題で、こちらは実 read でも事前宣言があれば通す運用改善。関連 feedback memory: feedback-floor-confirm-upfront-not-midwork。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 108.1 | `[lane:gate]` `[tdd:required]` `[security]` go/internal/runtimefloor に secret-read allowlist を実装する。owner-scoped env（egress の HARNESS_RUNTIME_FLOOR_EGRESS と対称の命名、例 HARNESS_RUNTIME_FLOOR_SECRET_ALLOW）で許可 path（glob/prefix、複数はカンマ区切り）を宣言。checkSecretRead は allowlist にマッチする operand を通す。allowlist は path を明示宣言した時のみ緩和し、未宣言のカテゴリ一括無効化は不可（全開放は提供しない）。既存の isAllowlistedHost を参照し対称な helper isAllowlistedSecretPath を追加 | (a) allowlist にマッチする秘密ファイルの実 read が通る test、(b) allowlist 非マッチの秘密ファイルは従来通り deny 維持 test、(c) env 未設定時は全 secret-read が deny（現状維持）test、(d) 空文字やアスタリスク単独のような全開放指定は無効（安全側 deny）test、(e) `cd go && go test ./internal/runtimefloor/...` PASS | - | cc:done [06b648a0] (env HARNESS_RUNTIME_FLOOR_SECRET_ALLOW で事前許可、deny-by-default、全開放不可、6 test。108.2-4 は残) |
| 108.2 | `[lane:gate]` `[tdd:required]` per-project 宣言の config source を追加する。env に加えて project-local config（.claude-code-harness.config.json の runtimefloor.secretAllow か .claude/rules 配下）で宣言でき、resolveProjectRoot 経由で読む。env と config の両方がある場合は和集合。宣言は path 単位で、project 境界を越えた path は無効（cross-project 誤許可の防止） | (a) config で宣言した path が通る e2e test、(b) project 外の絶対 path 宣言は無効化される test、(c) env と config 併用の和集合 test、(d) config 不正時は fail-safe で全 deny（宣言なし扱い）test、(e) `bash tests/validate-plugin.sh` PASS | 108.1 | cc:done [03815e66] |
| 108.3 | `[lane:gate]` `[tdd:skip:docs]` 作業前に一度宣言すれば途中で止まらない運用ガイドと契約を明文化する。spec.md（または docs/spec の該当 sub-spec）の Runtime Floor 契約に secret-read allowlist を追記（egress allowlist と対称であること、path 明示宣言のみ緩和、全開放不可を明記）。docs に operator 向け設定手順（pipeline の secret 宣言例）を書く | (a) spec に secret-read allowlist 契約が追記され egress との対称性が明記、(b) docs に宣言手順と具体例、(c) `bash scripts/ci/check-consistency.sh` PASS | 108.1, 108.2 | cc:done [8c6965fa] |
| 108.4 | `[lane:release]` `[tdd:skip:integration-verification]` e2e validation。実 pipeline 相当の fixture（複数コマンド、うち 1 つが宣言済み秘密ファイルを読取）で、宣言済みなら途中停止せず完走、未宣言なら該当コマンドで停止することを検証。5 カテゴリ floor の他カテゴリの deny が退行しないことも確認 | (a) 宣言済み pipeline が停止せず完走する test、(b) 未宣言 pipeline は該当コマンドで停止する test、(c) 他 4 カテゴリの floor deny 非退行 test、(d) `bash tests/validate-plugin.sh` と `go test ./...` と `bash scripts/ci/check-consistency.sh` 全 PASS | 108.1, 108.2, 108.3 | cc:done [f344fa16] |
| 108.5 | `[lane:gate]` `[tdd:required]` **plan-time 事前確認 (pre-approval) をハーネスフローに組み込む**。operator 意図 (2026-07-07 再確認): 常設 allowlist で何でも許可するのではなく、**作業スコープごとに** plan 確定段階で確認を前倒しする。(i) `skills/harness-plan/SKILL.md` の計画確定フローに「事前確認セクション」生成を追加 — 計画した各 task の対象ファイル・DoD 記載コマンドから、確認が発生しそうな事項（secret-read path / 外部送信 (push・PR・API) / 破壊的操作）を列挙し、plan 承認時に一括提示して承認を得る。(ii) 承認結果を `plan-preapproval.v1` schema（scope=phase/task 単位、path/操作の列挙、承認日時）で `.claude/state/plan-preapprovals.json` に記録。(iii) `skills/harness-work/SKILL.md` / `skills/breezing/SKILL.md` は run 開始時にこの記録を読み、宣言済み事項では途中停止しない（secret-read は 108.2 config への per-run 反映で floor と接続）。(iv) 記録に無い未計画の secret-read / 外部送信は従来どおり floor / ask で停止する（安全網は狭めない）。env `HARNESS_RUNTIME_FLOOR_SECRET_ALLOW` の広域常設宣言は本タスク完了後に不要となる想定（migration 注記を docs に書く） | (a) harness-plan の SKILL.md に事前確認セクション生成手順が存在し、生成物 format（事項 + 理由 1 行 + scope）が固定されている、(b) `plan-preapproval.v1` schema が `templates/schemas/` に存在し validation test PASS、(c) e2e: plan で宣言 → work 実行中に宣言済み secret-read で停止しない test、(d) e2e: 未宣言 secret-read は従来どおり deny される test（安全網非退行）、(e) 確認は plan 承認時 1 回のみ・work 中の宣言済み事項起因 AskUserQuestion ゼロを手順として明記、(f) `bash tests/validate-plugin.sh` PASS | 108.1, 108.2 | cc:done [8e4178ee] |

## Phase 109: Release Milestone — redesign 線の区切りリリース（operator 承認 2026-07-07）[P0]

Purpose: 「今の redesign Ver を区切りの良いところまでやり終えたらリリースする」（operator 指示 2026-07-07）。区切り = Phase 106 + 108 の全 open task 完了 + GOD_plans §7 S5 受け入れ基準 11 項目の機械検証 green。DP-3 に従い本流化 = major 相当。事前承認済み (2026-07-07 AskUserQuestion): DP-2 裁定案どおり承認 / DP-4 主張撤回承認 / Phase 106 をリリース区切りに含める / phase 完了ごとの origin/plan/zero-base-redesign への push 許可（backup 目的。release 本体は 109.5 の単一確認ゲートで別途承認）。secret-read は暫定 env bridge (`HARNESS_RUNTIME_FLOOR_SECRET_ALLOW=~/LocalWork/`) を 108.5 完了まで維持（option A 承認済み）。human-only 前提: M2 (`.github/workflows` の -trimpath 決定論ビルド patch) は保護 path のため operator 手動適用 — 未適用の間 CI 側 binary drift gate が赤になり得る（ローカル green は 109.3 で担保）。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 109.0 | `[lane:gate]` `[tdd:skip:build-repro]` binary drift gate の green 化。現 HEAD (390de02a) で `check-consistency.sh` の binary/source drift gate [22/23] がローカルでも red（Skeptic 検証 2026-07-07 で再現、`-trimpath` 再ビルドと committed binary が不一致）。決定論ビルドで 4 platform binary を再生成し commit する | (a) `bash scripts/ci/check-consistency.sh` 23/23 全 green（ローカル）、(b) 再ビルド手順（Go version / build flags）を commit message に記録 | - | cc:done [49024b95] |
| 109.1 | `[lane:gate]` `[tdd:skip:port-verification]` main との乖離 **116 commit**（merge-base 794bfc36 以降）の全量棚卸しと reconciliation。branch-alignment 台帳を「静的 SHA allowlist」から「`git log $(git merge-base HEAD main)..main --oneline` の全件分類」へ更新し、各 commit（または phase 単位のグループ）を port / waive（理由付き）/ 既取込 に分類する。**必須 port**: 2026-07-07 の guard 修正 4 commit（462e94ff / 3cc09d1d / c0db9787 / ed6b18c9）。**特別扱い**: `go/internal/runtimefloor` は両 branch で独立進化しているため commit port ではなく機能差分の照合統合（main 側 egress owner-scope d9b3fd34 系と redesign 側 108.1 secret allowlist の両立を test で担保）。port は cherry-pick SHA 直接指定。port 対象 test（test-commit-guard / test-release-multi-commit / test-reviewer-stop-persist）が redesign 樹に無い場合は test ごと port する | (a) 台帳が merge-base..main の全 commit を分類済み（未分類 0 件）、(b) runtimefloor 照合統合の差分表（main 独自機能 / redesign 独自機能 / 衝突解消）が台帳に存在し両系の test PASS、(c) port した guard 修正の対応 test が redesign 線で PASS、(d) `bash tests/validate-plugin.sh` PASS | 109.0 | cc:done [ca3db275] (109.1a security port + 109.1b port 7群 完了) |
| 109.2 | `[lane:gate]` `[tdd:skip:docs-and-cut]` DP-2 / DP-4 承認の反映（2026-07-07 承認済）。DP-4: README / spec の auto-approve 主張を「ledger 記録のみ」に訂正し、approval-skip 実装の主張を HOTL Phase 送りと明記（§7-6 の撤回パスを green 化）。DP-2: `go/internal/{bridge,mailbox,bridgedelivery,triaddispatcher,impactscore}` の各 package が「importer ≥1」または「削除済み + retired-alias registry 登録」または「Plans.md に wired:no 注記」のいずれかを満たすことを確認し、未処理があれば cut を実行（知見の spec 翻訳記録を含む） | (a) `grep -rn "auto.approve" README.md spec.md` の主張が「ledger 記録のみ」と整合、(b) §7-7 の 5 package 全てが判定可能状態、(c) cut した package は `templates/registry/retired-aliases.v1.yaml` に登録され `bin/harness retired-alias scan` 0 件、(d) `cd go && go test ./...` PASS | - | cc:done [984456bf] |
| 109.3 | `[lane:release]` `[tdd:skip:verification-run]` S5 受け入れ基準 11 項目の機械検証 run。**基準 11 項目の本文を `docs/release/s5-acceptance-2026-07.md` に inline 転記する**（正本 GOD_plans.md は local-only で branch から到達不能のため、台帳を self-contained にする）。各項目を実コマンドで検証し evidence（コマンド + exit code + 出力要点）を台帳化。M2 未適用による CI 側 binary drift は「ローカル green + M2 blocked 注記」で記録 | (a) 台帳に 11 項目の基準本文 + evidence 付き判定が記録され red 0 件（M2 起因の CI 側のみ blocked 注記可）、(b) 台帳ファイルが存在し `bash scripts/ci/check-consistency.sh` PASS | 106.2, 108.4, 108.5, 109.1, 109.2 | cc:done [92d1aa8d] |
| 109.4 | `[lane:release]` `[tdd:skip:release-prep]` version 整合 + 本流化 PR 準備。**merge 戦略を確定する**: main を redesign へ merge して衝突解消（両線の history 保持、109.1 の分類台帳を衝突解消の根拠に使う）→ plan/zero-base-redesign → main の PR は通常 merge。DP-3（本流化 = major 相当）に従い v5.0.0 を提案（`./scripts/sync-version.sh bump` は operator GO 後の 109.5 で実行）。CHANGELOG [Unreleased] に redesign 統合の Before/After を書く。draft PR を作成し本文に §7 台帳（109.3 の docs/release/s5-acceptance-2026-07.md）への参照を付ける | (a) main が redesign へ merge 済みで conflict 0・全 test PASS、(b) CHANGELOG [Unreleased] に Before/After が存在、(c) draft PR が存在し台帳参照あり、(d) VERSION/plugin.json/harness.toml は本 task では不変 | 109.3 | cc:done [bb8176ef] (draft PR #235、-s ours merge、v5.0.0 提案、S5 台帳参照) |
| 109.5 | `[lane:release]` `[tdd:skip:release-gate]` harness-release 実行。単一 Confirmation Gate（version bump / CHANGELOG promote / PR merge / tag / GitHub Release の一括提示）で operator の GO を取得してから実行する。自動では実行しない | (a) operator が Confirmation Gate で承認した evidence、(b) tag と GitHub Release が作成され 4 点同期（VERSION/plugin.json/harness.toml/CHANGELOG）green、(c) `bash tests/validate-plugin.sh` PASS | 109.4 | cc:done [c100dbb4] (PR #235+#236, tag v5.0.0, assets=4, EN release notes 2026-07-09; URL https://github.com/Chachamaru127/claude-code-harness/releases/tag/v5.0.0) |

---

## Phase 110: Post-v5.0.0 Operator Closeout [P0]

Purpose: v5.0.0 本流化後の operator hygiene。product contract 変更なし。詳細: `docs/plans/phase-110-post-v5-operator-closeout.md`。

Spec skip reason: ledger sync / git hygiene / docs judgment / dependency triage only.

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 110.0 | `[lane:gate]` `[tdd:skip:ops]` token rotation（GH_TOKEN / FIRECRAWL）。**operator 判断 2026-07-09: 漏洩未確認のため skip**。`/.codex/` は gitignore 済み（origin 既定） | (a) skip 理由が Plans/decisions に記録、(b) 平文 token を commit しない | - | cc:done [skipped-no-leak-confirmed] |
| 110.1 | `[lane:gate]` `[tdd:skip:git-ops]` local main を origin/main に align。archive branch 退避後 hard reset | (a) HEAD == origin/main at start of closeout work、(b) `archive/local-main-pre-v5` 存在、(c) VERSION=5.0.0 | - | cc:done [archive/local-main-pre-v5 @ ed6b18c9 → reset c100dbb4] |
| 110.2 | `[lane:gate]` `[tdd:skip:ledger]` 109.5 marker を cc:done に同期 | (a) 109.5 が cc:done、(b) evidence 付き | 110.1 | cc:done |
| 110.3 | `[lane:release]` `[tdd:skip:docs-only]` README_HOTL_ja owner レビュー + EN 判断 | (a) A/B/C 決定記録、(b) EN Yes/No | 110.1 | cc:done [A: docs/research/README_HOTL_ja.md gitignored research draft, EN=No] |
| 110.4 | `[lane:release]` `[tdd:skip:dependency-triage]` Dependabot 21 alerts + open PRs 再評価 | (a) 分類表、(b) merge/defer/accept-risk | 110.1 | cc:done (see triage note below) |
| 110.5 | `[lane:fast]` `[tdd:skip:optional-salvage]` archive 15 commit 取捨 | (a) keep/drop 表、(b) keep=0 なら close | 110.1 | cc:done [all drop; archive retained] |
| 110.6 | `[Closeout]` `[lane:release]` `[tdd:skip:test-aggregation]` Phase 110 closeout。no tag | (a) 110.1–110.5 done、(b) no tag | 110.2, 110.3, 110.4, 110.5 | cc:done |

**110.3 裁定 (2026-07-09)**:
- **A**: 製品フロントドアにしない。`docs/research/README_HOTL_ja.md` に vision draft として格納（`docs/research/*` は gitignore 対象 = 公開面に出ない）。冒頭に non-canonical banner。
- **EN**: 作らない（JA draft の claim 固定後に再判断）。
- v5 時点で plan-brief / progress hook / accept は skill+hook 配線済み。HOTL 統治本体は 🔜 のまま正しい。

**110.4 裁定 (2026-07-09)**:
- GitHub alerts 21: ほぼ vitest CRITICAL が `benchmarks/**` 配下の fixture/package。production runtime path に vitest なし → **accept-risk (bench-only)**。
- PR #232 / #234 / #230: branch update 実施・merge 推奨。branch protection が required checks 完了を要求するため、checks 完了後に merge（admin bypass はしない）。
- PR #233 @vercel/agent-eval 1.x: validate FAILURE + bench 隔離 → **defer**（comment 済み）。

**110.5 keep/drop**:
| 群 | 判定 |
|----|------|
| guard 4 commits | drop（109.1 port 済み） |
| skill prune / progressive disclosure | drop（redesign skill 木が別物） |
| f6cdb042 HOTL wiring | drop（origin に plan-brief/accept/progress 配線済み） |
| dirty model-routing Sonnet5 | drop（origin 既に sonnet-5） |

---

### Phase 111: SQLite Plan-State Layer (planstate) + planqueue Bridge [P1]

Purpose: keep the harness "on track" across many waves/phases/epics with a machine-local, rebuildable SQLite projection of Plans.md (runtime overlay: claims, pids, heartbeats, waves) plus an opt-in read-mostly bridge to the operator's global `~/.planqueue/backlog.db` — without breaking Plans.md as the human-readable SSOT.

Design evidence: two-subagent synthesis 2026-07-09 (inside: repo state-store survey `go/internal/state/store.go`, `internal/plans/dependencies.go`, JSONL ledgers; outside: planqueue schema live-read, CAS claim protocol, federated multi-machine). team_validation_mode: subagent (Architecture + Skeptic/QA perspectives). Authority split: Plans.md = task definition SSOT (what); plan_state.db = machine-local runtime overlay (what is happening now); waves derived by topo-sort, never stored as truth; hosts read only via `bin/harness plan ... --json`.

unknown_data: (a) omp `pi.registerTool` runtime option schema not exercised against a live omp session (candidate tier); (b) future planqueue schema migrations unknown (`.bak-pre-*` history shows hand-migration) — bridge must degrade read-only, `not_observed != absent`.

| Task | Content | DoD | Depends | Status |
|------|---------|-----|---------|--------|
| 111.1 | `[lane:gate]` `[tdd:skip:docs-only]` Spec delta: add "Plan State Projection Contract" section to root `spec.md` (Plans.md = definition SSOT; `.harness/plan_state.db` = gitignored rebuildable overlay; claims host-local; Plans.md writes only via human-gated harness-sync; hosts consume via CLI JSON only) | (a) spec.md section present with the 4 contract clauses, (b) links `docs/plans/plan-state.md` as sub-spec, (c) no change to Plans.md 5-column template | - | cc:done [b8bf5324] |
| 111.2 | `[lane:gate]` `[tdd:required]` `go/internal/planstate`: schema v1 (`plan_nodes` with plans_hash, `plan_deps`, `plan_runs`, `schema_meta` gating like `internal/state`), `Reindex()` from `internal/plans` parser, drift detection by row hash | (a) `cd go && go test ./internal/planstate/...` PASS incl. reindex-idempotent + drift tests, (b) DB absent = `not-configured` tri-state (no warning), (c) delete-DB-then-reindex restores all definitional rows | 111.1 | cc:done [9136bee2] |
| 111.3 | `[lane:gate]` `[tdd:required]` CLI verbs in `cmd/harness/plan.go`: `plan reindex\|waves\|next\|drift [--json] [--plan N]`; waves via Kahn topo-sort honoring `[P]` and Depends | (a) go tests for verb output schemas PASS, (b) `bin/harness plan next --json` returns first unblocked task on this repo's Plans.md, (c) `plans check-deps` untouched | 111.2 | cc:done [0f327263] |
| 111.4 | `[lane:gate]` `[tdd:required]` Run overlay: `plan run-begin/run-end/resume`, single-statement CAS claim (`UPDATE ... WHERE run_pid IS NULL OR stale`), heartbeat refresh, reap (same-host `kill(pid,0)` + wall-clock backstop 2h) inside `plan next` | (a) concurrency test: 2 goroutine claimers, exactly 1 wins (`changes()==1`), (b) crashed-pid reap test PASS, (c) `busy_timeout=5000` set on every connection | 111.3 | cc:done [68d2c5f0] |
| 111.5 | `[lane:gate]` `[tdd:required]` planqueue bridge (opt-in via `harness.toml [planqueue]`): read-only client of `~/.planqueue/backlog.db`; boot `PRAGMA table_info` assert -> degrade read-only advisory on mismatch; never `ALTER`; harness evidence in sidecar `harness_run_evidence`; task link `<!-- pq:<epic_id> -->`; one-way `cc:done -> status='done'` emitted as harness-sync proposal only | (a) tests with fixture DB: missing column -> read-only degrade not crash, (b) sidecar write does not touch `epics` schema, (c) disabled/absent DB = `not-configured` silent | 111.3 | cc:done [31c02d06] |
| 111.6 | `[lane:gate]` `[tdd:required]` omp shim v2 (`templates/omp/harness-extension.ts`): `pi.registerTool` `plan_next`/`plan_claim`/`plan_done` shelling to CLI verbs; `session_start` injects top-3 ready wave context; `turn_end` heartbeat; `agent_end` closes or releases claim | (a) `tests/test-omp-adapter-candidate.sh` extended with tool-registration + wave-injection gates PASS, (b) shim still parses under bun, (c) no direct DB reads from TS (CLI JSON only) | 111.4 | cc:todo |
| 111.7 | `[lane:gate]` `[tdd:skip:test-aggregation]` Closeout: `docs/plans/plan-state.md` (schema, verbs, reconciliation flow, multi-machine federation rationale: no committed DB, litestream = DR only), CHANGELOG `[Unreleased]`, validate-plugin + check-consistency PASS | (a) all 111.x done with evidence, (b) `./tests/validate-plugin.sh` PASS, (c) `bash scripts/ci/check-consistency.sh` PASS, (d) VERSION untouched (release train decides bump) | 111.2, 111.3, 111.4, 111.5, 111.6 | cc:todo |
